# Tasks — stretch-public-hosting

Tier `high`: every new or changed guard carries a demonstrated failing input — a
test that fails when the guard is removed — not only a test that passes.

Every command below is runnable as written from the repository root. Where a step
needs a public host name, it is marked as the user's manual step and produces no
checked task.

## 1. The production image

- [x] 1.1 Add a `.dockerignore` excluding `var/`, `vendor/`, `.git/`, `config/jwt/` and every env file, so the repository-root build context carries neither local state nor a stray `.env.local` into an image. Verify, executed: `docker build -f .docker/php/Dockerfile --target prod -t linkboard-prod .` and the reported context size recorded before and after the file exists. **Executed:** `509.29MB` without the file and `5.84MB` with it, both measured as full transfers on freshly created buildx builders (the second run on one builder only sends a delta and reported a misleading `59.10kB`). The file also excludes `/public/bundles/` and `/public/assets/`, which are produced at container start.
- [x] 1.2 Add the `prod` stage to `.docker/php/Dockerfile` (design decision 1): `composer install --no-dev --classmap-authoritative`, the source copied in, a fixed non-root user, production PHP settings with `opcache.validate_timestamps=0`. **No step boots the kernel** (design decision 2). Verify, executed: the command in 1.1 succeeds and `docker run --rm --entrypoint sh linkboard-prod -c 'id && php -i | grep opcache.validate_timestamps'` prints the fixed user and `0`. **Executed:** the build succeeds, and `docker run --rm --entrypoint sh linkboard-prod -c 'id && php -i | grep opcache.validate_timestamps'` prints `uid=1000(app) gid=1000(app)` and `opcache.validate_timestamps => Off => Off`. `composer install` carries `--no-scripts` because `post-install-cmd` runs `cache:clear`, which boots the kernel — the decision's own rule, confirmed against `composer.json` rather than assumed.
- [x] 1.3 The build needs no secret and bakes none. Verify, executed: the build runs in a shell with `env -u APP_SECRET -u JWT_PASSPHRASE -u VISITOR_HASH_SALT` and succeeds; then `docker history --no-trunc linkboard-prod` and `docker inspect --format '{{json .Config.Env}}'` show no value for any required setting. The image does carry the committed `.env`, because the boot check reads the development defaults from it to recognise them — those are public repository content, not secrets, and an instance that still holds one refuses to start (section 3). **Executed:** `env -u APP_SECRET -u JWT_PASSPHRASE -u VISITOR_HASH_SALT -u DATABASE_URL -u REDIS_URL docker build …` succeeds; `docker history --no-trunc` matches none of the setting names or the committed values, and `Config.Env` carries only `APP_ENV=prod` and `APP_DEBUG=0` beside the base image's own.
- [x] 1.4 Development dependencies are absent. Verify, executed: `docker run --rm --entrypoint sh linkboard-prod -c 'ls vendor/bin'` lists no `phpunit`, `phpstan` or `php-cs-fixer`. **Executed:** `vendor/bin` holds `doctrine-migrations`, `patch-type-declarations`, `sql-formatter`, `var-dump-server`, `yaml-lint` — every one from a production dependency, and no `phpunit`, `phpstan` or `php-cs-fixer`.
- [ ] 1.5 Demonstrated failing input for decision 2: add `bin/console cache:warmup` to the production stage and show the build failing on the setting check once section 3 exists — the output recorded — then remove it. This is the collision the decision is about, so it is shown rather than asserted.

## 2. Start-time provisioning

- [x] 2.1 Write the **one-shot init service** that provisions (design decision 3, Gate 1 confirmation 1, finding 4): it compiles the asset map into the assets volume and runs `bin/console lexik:jwt:generate-keypair --skip-if-exists` into the keys volume, and it is the only writer of either. The web, worker and scheduler services declare `depends_on: { init: { condition: service_completed_successfully } }` and their own entrypoint warms only their private cache. Verify, executed: `docker compose --env-file .env.local -f docker-compose.prod.yml up -d` on empty volumes, with the init service's output and the three services' start order recorded. **Executed:** `docker compose --env-file .env.local -f docker-compose.prod.yml up -d` on empty volumes. init ran alone after postgres and redis were healthy, generated the keypair, applied migrations, installed the bundle assets and compiled **78** assets, then exited 0; only then did php, worker, scheduler and caddy start. `.docker/php/provision.sh` is the only writer of the jwt, assets and bundles volumes.
- [x] 2.2 **A partial pair is never served, and the next run repairs it** (Gate 1 confirmations 2 and 3, finding 4). Two file moves are two operations, so nothing here claims an atomic publication; the guarantee is the one the stack actually provides: provisioning generates into a temporary directory inside the same volume and moves both files into place, and **if it dies at any point the init service exits non-zero, so `service_completed_successfully` is false and no dependant starts**. Before deciding anything, the next run discards what it finds if the location holds one file alone, or a pair whose public key is not the one derived from its private key, and generates afresh. Implement that, and make the requirement's wording match it rather than promising atomicity. **Executed:** implemented in `provision.sh` — generation into `config/jwt/prod/.staging` inside the same volume, both files moved into place, and a repair pass that discards a lone file or a pair whose public key is not the one derived from its private key. The wording in the capability says what the stack really guarantees rather than promising atomicity.
- [x] 2.3 Verify the interruption the finding names, executed: interrupt provisioning **between the two final moves**, and record that (a) the init service exited non-zero, (b) the web, worker and scheduler services did not start, and (c) the next `up` removed the partial pair, generated a complete matching one, and only then let the dependants start — the four states recorded from `docker compose --env-file .env.local -f docker-compose.prod.yml ps` and the key diff. **Executed, all four states recorded.** With the interruption hook set: init exited **70**; `ps -a` showed `php created`, `worker created`, `scheduler created`, `caddy created` — none started — while only postgres and redis ran; the volume held `private.pem` alone. The next `up` logged `found a private key with no public key — discarding the partial pair`, init exited 0, all six services reached `running`, and the pair MATCHES.
- [x] 2.4 Demonstrated failing input for the repair. **The case this task named turned out to be impossible, and the reason is worth keeping:** generation happens in a staging directory, so `--skip-if-exists` can no longer be fooled by a leftover file — with only `private.pem` present it still generates a complete pair. The repair's unique job is therefore the *mismatched* pair, and that is what was demonstrated. **Executed:** a public key from a different keypair was placed beside the private one; with the repair removed the whole stack came up and `openssl rsa … | diff` reported **MISMATCH** — an instance serving with a key nothing can verify; with the repair restored, init logged `found a pair whose public key is not the one belonging to its private key — discarding it`, regenerated, and the pair MATCHES.
- [x] 2.5 **A concurrent clean start yields one matching pair.** Verify, executed: remove the keys volume, bring the **whole stack up at once**, then `docker compose --env-file .env.local -f docker-compose.prod.yml exec php sh -c 'openssl rsa -in config/jwt/prod/private.pem -passin env:JWT_PASSPHRASE -pubout | diff - config/jwt/prod/public.pem'` prints nothing — the pair matches — and exactly one keypair exists. Repeat it five times and record each result, because a race that only sometimes loses is still a race. **Executed five times.** Each cycle was `down -v` then `up -d` on empty volumes with the whole stack starting at once: every run gave `init exit=0`, exactly `private.pem public.pem`, and a matching pair.
- [ ] 2.6 Demonstrated failing input for the single writer: move provisioning back into the three services' own entrypoints, run 2.5's loop, and record the mismatched pair when it appears — then restore the init service.
- [x] 2.7 The keypair survives replacement. Verify, executed: obtain a token, `docker compose --env-file .env.local -f docker-compose.prod.yml up -d --force-recreate php`, use the same token and get 200 — the two responses recorded. **Executed:** a token obtained before `up -d --force-recreate php`, then used after it, answered `me: 200` — the keypair outlived the container because it lives in a volume.
- [x] 2.8 A clean host issues tokens at all. Verify, executed: `docker volume rm` the keys volume, bring the stack up, seed, and `POST /api/v1/auth/token` returns a token that an authenticated request accepts. **Executed on a clean host** (`down -v` first): the scheduler seeded the instance and `POST /api/v1/auth/token` with the published demo credential returned `200` with a JWT, requested from inside the network so the value never left the containers.
- [x] 2.9 Provisioning is idempotent. Verify, executed: restart the container twice and show the keypair's fingerprint unchanged and the assets present after each. **Executed:** the public key's SHA-256 was identical across two full stack restarts (`60a323f27278dbe9…`) and the compiled assets were present each time.
- [x] 2.10 The private key is nowhere it should not be. Verify, executed: `docker history`/layer search, `git check-ignore -v config/jwt`, and a grep of the start-up output all come back clean — the three commands and their output recorded. **Executed:** the image holds **0 files** under `config/jwt` (only the empty directory the build creates); the single `.pem` anywhere in `/app` is `vendor/composer/ca-bundle/res/cacert.pem`, a public root store; `git check-ignore -v config/jwt` names `.gitignore:49`; and the provisioning output contains no key material.

## 3. The required settings

- [x] 3.1 Implement the startup check (design decisions 2 and 6) as a service tagged `app.startup_check` over exactly `APP_SECRET`, `JWT_PASSPHRASE`, `VISITOR_HASH_SALT`, `DATABASE_URL`, `REDIS_URL`, `LOCK_DSN`, `MESSENGER_TRANSPORT_DSN`, reading the committed defaults from `.env` itself rather than from a copy in PHP. Unset, empty and still-committed each fail the boot naming the setting and never the value. **Executed:** `src/Shared/Boot/RequiredSettings.php` and `MisconfiguredSetting.php`. **The arming condition changed during implementation and the reason is worth keeping:** the first version armed on `APP_ENV=prod`, which refused two legitimate local prod-kernel runs — the benchmark recipe and `HealthDeepProdTest`, whose 14 tests went red. The only ways to keep them were to point them at other credentials (churning a passing suite) or to append something cosmetic to a DSN so the strings differ (gaming the check). The instance now declares itself a deployment (`DEPLOYMENT=true`, set by `docker-compose.prod.yml`), which is an opt-in to being checked rather than an off switch, and `testTheProductionComposeFileArmsIt` closes the only hole that leaves.
- [x] 3.2 Every setting is covered one at a time. Verify: a data-provider test boots the kernel once per setting with that one unset and asserts the failure names it — seven cases, so a setting added to the list without a case fails the count assertion. **Executed:** `tests/Integration/Shared/RequiredSettingsTest.php` boots a real kernel per case — 21 boot cases plus 5 others, 26 tests in 0.35 s. `setUpBeforeClass` drops `var/cache/prod` first, because a non-debug kernel does not rebuild its container on a source change and would otherwise boot without the new check and pass for the wrong reason.
- [x] 3.3 **The committed-default case, which is the one that actually happens.** Verify: the same provider boots with each setting set to the exact value committed in `.env` and asserts the boot fails naming it — including `DATABASE_URL`, `REDIS_URL`, `LOCK_DSN` and `MESSENGER_TRANSPORT_DSN` separately (Gate 1 round 1, finding 5). **Executed:** the committed-default case runs for all seven; for `APP_SECRET` and `JWT_PASSPHRASE` the committed value is empty, so it coincides with the empty case, which the test states rather than skipping.
- [x] 3.4 The failure discloses nothing. Verify: a test asserts the message contains the setting's name and not the value found. **Executed:** each case asserts the message contains the setting's name and does not contain the value it found.
- [x] 3.5 Demonstrated failing inputs: drop the committed-default clause and show 3.3 failing; drop any one Redis setting from the list and show 3.2's count assertion failing; drop the check's tag and show 3.2 failing. **Executed, three mutations:** dropping the committed-default clause fails **5 of the 7** committed cases (the two whose defaults are empty stay caught by the empty rule); dropping `LOCK_DSN` from the named set fails the set assertion; removing `#[AutoconfigureTag]` fails **all 21** boot cases. Restoring gives 26/26.
- [x] 3.6 Every process refuses, not only the web one. Verify, executed against the prod-like stack with one setting unset: the web container, the worker and the scheduler each exit with the named failure — the three outputs recorded. **Executed:** with `VISITOR_HASH_SALT` set to the exact value committed in `.env`, each of `php`, `worker` and `scheduler` run on its own refuses with `VISITOR_HASH_SALT still holds the value committed in .env as a local development default` — three processes, one mechanism, because the check runs from `Kernel::boot()`.
- [x] 3.7 `.env` gains the new settings with development defaults and `.env.local` stays ignored. Verify, executed: `git check-ignore -v .env.local` names the rule, and `rg -n 'DEMO_|REGISTRATION_' .env` shows development values and no secret. **Executed:** `.env` gains `DEPLOYMENT=false`, `REGISTRATION_ENABLED=true`, `DEMO_INSTANCE=false` and an empty `DEMO_PASSWORD`, each with a comment saying what turns it on; `git check-ignore -v .env.local` names `.gitignore:14`.
- [x] 3.8 **One authoritative credential per store, derived into every connection string** (design decision 6, Gate 1 confirmation 1, finding 5). Implement the production compose so `DATABASE_URL` is interpolated from `DB_USER`/`DB_PASSWORD`/`DB_NAME` and `REDIS_URL`, `LOCK_DSN` and `MESSENGER_TRANSPORT_DSN` from the Redis credential, with the servers taking the same two inputs — so no second literal exists to disagree. **Executed:** `docker-compose.prod.yml` derives `DATABASE_URL` from `DB_USER`/`DB_PASSWORD`/`DB_NAME` and the three Redis strings from `REDIS_PASSWORD`, and the Postgres and Redis servers take those same two inputs, so no second literal exists to disagree.
- [x] 3.9 **The interpolation source is named on every invocation** (Gate 1 confirmations 2 and 3, finding 5). Compose interpolation reads the shell, the project `.env` or `--env-file` — never a service's `env_file:` — so the credentials generated into `.env.local` reach the derivation only if every invocation says `--env-file .env.local`. Make that one form the only form, in `docs/how-to/deploy.md`, in every task here and in anything CI runs. Verify, executed: `rg -n 'docker compose' openspec/changes/stretch-public-hosting/tasks.md docs/how-to/deploy.md .github/workflows/` shows **every** production invocation carrying it — including the ones that only `exec` into a running container, which is where confirmation 3 caught one that did not. **Executed:** and the form changed during implementation — it is now **one self-contained file**, `docker compose --env-file .env.local -f docker-compose.prod.yml …`. Measured why: `docker compose -f a.yml -f b.yml config` **appends** volume lists, so overlaying the production file on `docker-compose.yml` kept its `.:/app` bind mount and the deployment would have run with the working tree over the image it was built to carry. Swept across the artifacts (8 invocations).
- [x] 3.10 The rendered configuration really comes from that file. Verify, executed: `docker compose --env-file .env.local -f docker-compose.prod.yml config` shows the authoritative credentials in the Postgres and Redis servers' own settings **and** in each of `DATABASE_URL`, `REDIS_URL`, `LOCK_DSN` and `MESSENGER_TRANSPORT_DSN`, and no committed default anywhere in the output. **Executed:** `config` rendered with the documented invocation carries the authoritative `DB_PASSWORD` in `POSTGRES_PASSWORD` **and** in `DATABASE_URL`, and the authoritative `REDIS_PASSWORD` in `redis-server --requirepass` **and** in each of `REDIS_URL`, `LOCK_DSN` and `MESSENGER_TRANSPORT_DSN`; no committed default appears anywhere in the output, and `DEPLOYMENT: "true"` is present. Checked programmatically so that no value had to be printed.
- [x] 3.11 Demonstrated failing input: run the same `config` **without** `--env-file` and record the committed defaults appearing in the rendered output — which is the deployment this finding describes. **Executed:** the same `config` with the guarded variables supplied but **without** `--env-file` renders `DATABASE_URL: postgresql://linkboard:localdev-only-password@postgres:5432/linkboard?…` — the committed credential, which is exactly the deployment this finding describes. The second guard then catches it: that instance would refuse to boot.
- [x] 3.12 Both effective consumers really do agree. Verify, executed on empty storage: change only `DB_PASSWORD`, bring the stack up, and show the application querying the database; then do the same for the Redis credential and show the deep probe reporting both stores reachable — the outputs recorded. **Executed on empty storage:** the whole stack was brought up with `DB_PASSWORD` and `REDIS_PASSWORD` set to values chosen for the run, and every service reached `Up` — the scheduler seeded through the derived `DATABASE_URL`, the limiter and the transport worked through the derived Redis strings, and `SELECT count(*) FROM links` answered `10`. Both servers were created with the same two inputs the application connects with, so neither could disagree.
- [x] 3.13 A hand-overridden connection string is reported, not hidden. Verify, executed: override `DATABASE_URL` so it disagrees with the server, and record the deep dependency probe reporting the database unreachable rather than a later request failing arbitrarily. **Executed — and the task's predicted mechanism was wrong, which is the finding.** It expected the deep dependency probe to report the store unreachable. What actually happens is stronger: provisioning reaches the database before anything serves, so a `DATABASE_URL` overridden to disagree fails at start-up with `SQLSTATE[08006] … FATAL: password authentication failed for user "linkboard"`, naming the store, and every dependent process stays stopped. The capability now says that instead of naming the probe.

## 4. The production stack

- [x] 4.1 Write `docker-compose.prod.yml` — caddy, init, php, worker, scheduler, postgres, redis — with the worker and the scheduler under `restart: unless-stopped`, the assets and keys volumes of decision 3, and configuration from `.env.local` rather than the committed `.env`. Verify, executed: `docker compose --env-file .env.local -f docker-compose.prod.yml config` renders, and the file is re-read whole after the last edit. **Executed:** `docker-compose.prod.yml` — caddy, init, php, worker, scheduler, postgres, redis — renders and runs. The published ports carry `${BIND_ADDRESS:-0.0.0.0}` and `${HTTP_PORT:-80}`/`${HTTPS_PORT:-443}` so the stack can be exercised on a machine whose 80 is already taken, which this one was; a deployment leaves both at their defaults.
- [x] 4.2 Write the Caddy configuration: HTTPS with automatic certificates, HTTP permanently redirected, HSTS, `/health` as the upstream check, and the assets volume served directly. Verify, executed against the stack on an internal host name: `curl -sI http://<host>/x` is a 301 to `https://`, and the HTTPS response carries `Strict-Transport-Security`. **Executed:** `http://linkboard.localhost:8080/some-slug` answers `301` with `Location: https://linkboard.localhost/some-slug` and an empty body — the link's own redirect is not performed over plain HTTP. Over HTTPS the response carries `strict-transport-security: max-age=31536000; includeSubDomains`, and `Server` is stripped. The proxy's upstream check is the service's own `/health`.
- [x] 4.3 **An asset is served through the proxy** (Gate 1 round 1, finding 3). Verify, executed: fetch a page of the web UI over the stack, take an asset URL it references, request it, and record 200 with its content type; then assert every asset the dashboard references answers 200. **Executed:** the home page renders through the stack (4 706 bytes) and **all 15** assets it references answer `200` with `text/css` or `text/javascript`, served by Caddy from the volume rather than by the application.
- [ ] 4.4 The development stack's behaviour is untouched. **The original wording of this task — "`git diff main -- docker-compose.yml` is empty" — was impossible and is corrected here**: one Dockerfile with a shared base (design decision 1) means one build context, and the prod stage's context must be the repository root, so the `base` stage copies `.docker/php/conf.d/app.ini` by its root-relative path and the dev services must name the same context. The edit to `docker-compose.yml` is exactly that — `context: .`, `dockerfile: .docker/php/Dockerfile`, `target: dev` — and nothing else. Verify, executed: `git diff main -- docker-compose.yml` shows only those three lines per service and no change to volumes, env, ports, commands or dependencies; `make up && make check` is green with no other edit; and the dev image's extension list and PHP settings are identical to `main`'s, compared with `php -m` and `php -i | grep opcache` in both.
- [ ] 4.5 A killed worker returns and loses nothing. Verify, executed: kill the worker container, show it running again, and show the messages queued meanwhile consumed — the counts before and after recorded.
- [ ] 4.6 The publicly trusted certificate is the user's manual step and is **not** claimed here: the deploy document states what is unverified and why (a public host name is required). No checked task asserts a real certificate.

## 5. Registration closed

- [x] 5.1 Implement the switch as one `kernel.request` listener (design decision 5) over both registration paths, taking them from a parameter in `config/services.yaml`; default on. **Executed:** `src/Auth/Registration/ClosedRegistration.php`, one `kernel.request` listener at priority 16 — after the router, so an API request already carries what its error rendering needs, and before the firewall, because a surface that does not exist does not ask who you are. Its paths come from `app.registration_paths` in `config/services.yaml`.
- [x] 5.2 Both entry points refuse. Verify: a test asserts `GET /register`, the `/register` form POST with a valid CSRF token, and `POST /api/v1/auth/register` each answer 404 with the switch off — the API one as problem details — and that no account was created by any of them. **Executed:** `tests/Web/Security/ClosedRegistrationTest.php` covers both surfaces in one file, because a test that only looked at one would pass while the other stayed open. `GET /register` and `POST /register` answer 404 and create nothing; `POST /api/v1/auth/register` answers 404 as `application/problem+json`. **The design's claim that throwing the framework's not-found exception is rendered by each surface's existing handling is now verified rather than asserted** — no second rendering was needed.
- [x] 5.3 The sign-in page stops advertising it. Verify: a test asserts the login page carries no link to `/register` with the switch off, and does carry one with it on. **Executed:** with the switch off the sign-in page carries no `a[href="/register"]`; with it on it does. The guard is a Twig global bound to the same env var the listener reads, so the rule is read twice rather than restated.
- [x] 5.4 Existing accounts are untouched. Verify: a test signs in through the web and obtains a token through the API with the switch off. **Executed:** with the switch off an existing account signs in through the web form and obtains a token through the API.
- [x] 5.5 Default-on means nothing changed. Verify, executed: the whole existing suite passes with no edit to any existing test. **Executed:** the whole suite passes with no edit to any existing test — 1 050 tests, 22 984 assertions.
- [x] 5.6 Demonstrated failing input, in both directions: restrict the listener to the web path and show the API half of 5.2 failing; restrict it to the API path and show the web half failing. **Executed, both directions.** Listing only `^/register$` fails the API case (1 failure); listing only `^/api/v1/auth/register$` fails both web cases (2 failures); restoring gives 6/6.

## 6. Seeding a declared demo instance

- [x] 6.1 Change the `prod` guard of `app:demo:seed` to an instance setting (design decision 7), off by default; no option of the command lifts it. **Executed:** the guard is now `'prod' === environment && !demoInstance`, reading `DEMO_INSTANCE` from the instance. The message says outright that no option of the command lifts it.
- [x] 6.2 Verify the refusal is unchanged where it must be: a test runs the command in `prod` with the setting unset and asserts exit 1 and nothing written. **Executed:** the unit test's collaborators expect `transactional` and `findByEmail` **never**, so a case that reached the database would fail rather than pass quietly.
- [x] 6.3 Verify no option lifts it: a test runs it in `prod`, setting unset, with `--reset` and every other option, asserting exit 1 and nothing written. **Executed:** the same command invoked with every option it accepts — `--clicks`, `--days` and `--reset` — still exits 1 and writes nothing.
- [x] 6.4 Verify the opt-in works: a test runs it in `prod` with the setting on and asserts the dataset exists and the output names the setting that allowed it. **Executed on the real stack:** with `DEMO_INSTANCE=true` the scheduler seeded a `prod` instance — 10 links and 50 000 clicks — and printed the accounts. **This run also caught a real hole:** the demo user and the administrator were given the *same* password, so a sign-in page publishing the demo credential would have published administrator access, the admin address being a constant of this repository. Fixed so the instance's password is the demo user's alone, with a test asserting the administrator's differs.
- [x] 6.5 **The command takes a non-blocking lock** (Gate 1 round 1, finding 6). Verify: a test starts a run that is held inside its transaction (the existing `InterruptingStatement` fixture drives this) and runs a second one, asserting the second exits non-zero at once without writing, says a run is in progress, and that the first completes as if alone. **Executed:** with the lock already held, a second run exits 1 at once saying a run is in progress, and touches neither the connection nor the repository. Non-blocking on purpose: a waiting run would still be there when the next scheduled one starts.
- [x] 6.6 **The demo password comes from the instance** (Gate 1 round 1, finding 1). Verify: a test seeds with the password setting in force, resets, and asserts the same credential obtains a token after the reset — the case a generated-per-run password fails. **Executed:** with `DEMO_PASSWORD` set, the first seed and a `--reset` reload both print that same password, and it obtains a token against the dataset the reload created. Two facts the test had to be shaped around and that are worth keeping: an HTTP request between the two seeds leaves the EntityManager holding the former dataset and the reload dies on a cascade, and *unsetting* the variable afterwards breaks the next test's container because `.env` declares it — it is restored instead.
- [ ] 6.7 Demonstrated failing inputs: make the guard readable from an option and show 6.3 failing; remove the lock and show 6.5 failing; restore the per-run generated password and show 6.6 failing.
- [x] 6.8 The existing all-or-nothing guarantees still hold. Verify, executed: the existing `demo-data` suite passes with no edit. **Executed:** the existing `demo-data` suite passes with no edit — its assertion that a reload produces a *new* password still holds, because that is what happens when the instance supplies none.

## 7. The demo instance's own credentials page

- [x] 7.1 The sign-in page states the demo credentials when, and only when, the instance has declared itself a demo (design decision 7). Verify: a test asserts the e-mail and password appear with the demo setting on and that the page states no credentials with it off. **Executed:** `tests/Web/Security/DemoCredentialsTest.php` — a demo instance renders `.demo-credentials` carrying the seed's own address constant and the instance's password; an instance that is not a demo renders none; and a demo with no password configured renders none either, because the seed then generates one per run and a credential nobody can read is worse than none being offered.
- [x] 7.2 Nothing about it enters the repository. Verify, executed: `rg` for the demo password value across the working tree returns nothing, and the README says the page states it rather than stating it. **Executed:** the page reads `demo_email` from the seed's constant and `demo_password` from the instance's environment; neither the value nor any placeholder for it is in the repository. The template says what to sign in with, the repository says only that the page says it.

## 8. The schedule

- [ ] 8.1 Add the scheduler service running the demo reload (`app:demo:seed --reset`) and `app:click:partitions` on their cadences, named in one place.
- [ ] 8.2 Verify the reload converges rather than accumulates: run it twice against the stack and assert identical dataset counts after each — both outputs recorded.
- [ ] 8.3 Verify the partition run keeps the horizon: run it and show the partitions ahead of the current month, the `dbal:run-sql` output recorded.

## 9. The client's address

- [x] 9.1 Set the trusted proxy for the production stack (design decision 8), and make it configurable so a deployment behind something else can name it. **Executed:** `TRUSTED_PROXIES: ${TRUSTED_PROXIES:-private_ranges}` — the compose network by default.
- [x] 9.2 Two clients are two buckets. **Executed** against the running stack with `RATE_LIMIT_REDIRECT_PER_IP=3`: two containers with different addresses each sent four redirects and each got `302 302 302 429`. Sharing a bucket would have refused the second client's *first* request.
- [x] 9.3 **The failure this task predicted does not happen, and that is the finding.** It asked to show that leaving `TRUSTED_PROXIES` at the development value collapses both clients into one bucket. **Measured: it does not.** With the proxy deliberately untrusted (`TRUSTED_PROXIES=127.0.0.1`, confirmed inside the container with `printenv`) the two clients *still* got `302 302 302 429` each. The reason is the topology: the upstream is FastCGI, so Caddy passes the connecting client's address as `REMOTE_ADDR` and the application sees the real client whatever the setting says. The design and the capability are corrected to say that rather than the claim they carried.
- [x] 9.4 **A client cannot pick its own bucket** — the real exposure this section turned out to be about. `reverse_proxy` *appends* to `X-Forwarded-For` by default, so a client sending its own would have had it believed through a trusted chain: a caller choosing which bucket counts it, which is what a per-IP limit exists to prevent. The proxy now overwrites the header with the connecting address (`header_up X-Forwarded-For {remote_host}`). **Executed:** the two-client run above was made through that configuration, and the capability carries the scenario.
- [x] 9.5 The guarantee rests on the application not being reachable except through the proxy. **Executed:** `docker compose … config` publishes ports for `caddy` only — php, worker, scheduler, postgres and redis expose nothing to the host.

## 10. Continuous integration

- [ ] 10.1 Add a CI job that runs **the same build command the deploy document gives** and pushes nothing. Verify: the job's step is byte-identical to the documented command — asserted by the script suite that already checks documented commands, so the two cannot drift.
- [ ] 10.2 Verify it fails on a broken image, reproducibly and locally (Gate 1 round 1, finding 9 — an agent cannot push, so a deliberately red CI run is not evidence it can produce): break the production stage, run the documented command, record the failure, restore it, run it again and record the success.
- [ ] 10.3 Verify it reaches no host: the job has no registry login, no deployment credential and no network step beyond the build — asserted by reading the workflow file whole after the edit.
- [ ] 10.4 The green CI run on the branch head is the user's push, as it is for every change in this repository, and it precedes Gate 2.

## 11. Documents and the plan

- [ ] 11.1 Write `docs/how-to/deploy.md`: provisioning the host, generating the required settings (commands that generate, never values), starting the stack, the first seed, the schedule, which volumes matter and why, the slower first start that decision 2 buys, and what a real operator would add that is not here. Verify: every command was run in its exact printed form, except those needing a public host, which are marked as the user's step.
- [ ] 11.2 Write the ADR for the hosting decision — a VPS with compose and Caddy, with the PaaS and Kubernetes alternatives and why they are refused. Verify: the file is `docs/adr/ADR-0NN-*.md` with the next free number and is referenced from the deploy document.
- [ ] 11.3 README gains the demo section: what the instance is, that registration is closed there, that the sign-in page states the demo credentials, and **the link left to be added when the instance exists** (design decision 9). Verify: the README is re-read whole after the edit and `rg` finds no credential and no invented URL in it.
- [ ] 11.4 **Split roadmap row 16** (Gate 1 round 1, finding 7): row 16 becomes the deployment configuration this change delivers, and a new row records publishing the instance and adding its link — the user's step. `docs/explanation/requirements.md` §7 is updated to match and §9's public-hosting item is **not** marked delivered. Verify, executed: `rg -n 'stretch-public-hosting' openspec/ROADMAP.md docs/explanation/requirements.md` shows the two rows and no claim of a live instance.
- [ ] 11.5 `docs/README.md` indexes the new how-to. Verify, executed: `scripts/pregate-verify.sh gate2 stretch-public-hosting` reports every markdown link resolving.

## 12. Wrap-up

- [ ] 12.1 `make check` green in the container with no environment override; `openspec validate stretch-public-hosting --strict` passes; commits per logical block with the agent trailer; `handoff.md` updated with the measured numbers and every recorded failing input.
