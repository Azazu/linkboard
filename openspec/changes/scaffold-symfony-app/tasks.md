## 1. Stack bump (first, so every later step runs on PHP 8.4)

- [x] 1.1 `.docker/php/Dockerfile`: base `php:8.4.25-fpm-alpine@sha256:49734670eccf414af884c2a0c2e558401e228615f8028f1c9fca30a0d4fb1bc2`; `.github/workflows/ci.yml`: `php-version: "8.4"`. Verify: `docker compose build php` succeeds; `docker compose run --rm php php -v` prints `PHP 8.4.25`; `docker compose run --rm php php -m` still lists apcu gd intl pdo_pgsql redis zip and Zend OPcache.
- [x] 1.2 Sweep stack facts 8.3/7 → 8.4/8 in `AGENTS.md` (header line and Stack), `openspec/config.yaml`, `README.md`, `.claude/rules/php.md`, `docs/how-to/local-development.md`, `docs/explanation/requirements.md` (§0 bullet, §5 row, §7 row 2), `openspec/ROADMAP.md` row 2. Verify: `rg -n 'PHP 8\.3|Symfony 7|8\.3-fpm|php-version: "8\.3"' --glob '!openspec/changes/**' --glob '!docs/adr/**' .` returns nothing (the change's own artifacts and ADR-001 legitimately describe the old value).
- [x] 1.3 Write `docs/adr/ADR-001-symfony-8-on-php-8.4.md` (context, decision, alternatives 7.4 LTS, consequences: upgrade cadence, PHP 8.4 features allowed) and link it from `docs/adr/README.md`. Verify: `scripts/pregate-verify.sh` markdown-link check passes (run at gate time) and the ADR follows `docs/adr/ADR-TEMPLATE.md` sections.

## 2. Skeleton

- [ ] 2.1 `make up`, then `make composer ARGS='create-project symfony/skeleton:"8.1.*" /tmp/skeleton --no-interaction'`; copy the generated tree into `/app` except `.env`, `.gitignore`, `README.md`; merge the generated `.env` lines into the committed `.env` and compare `.gitignore`. Verify: `git status --short` shows the expected new files; `make console ARGS='about'` prints Symfony 8.1.x and PHP 8.4.25; the commit body lists the merged `.env` lines.
- [ ] 2.2 `composer.json`: `"php": ">=8.4"`, `ext-redis`, `ext-pdo_pgsql`, `ext-intl`, `config.platform.php: "8.4.25"`, `extra.symfony.require: "8.1.*"`. Verify: `make composer ARGS='validate --strict'` passes.
- [ ] 2.3 Create the bounded-context directories `src/Link src/Redirect src/Click src/Analytics src/Auth src/Shared src/Web` with `.gitkeep`; `config/services.yaml` excludes `Entity/`, `Dto/`, `Message/` sub-directories. Verify: `make console ARGS='debug:container --env=dev'` runs without errors; `ls src/` lists the seven directories.

## 3. Persistence, messaging, logging

- [ ] 3.1 `make composer ARGS='require symfony/orm-pack doctrine/doctrine-migrations-bundle symfony/uid'` (pins: ORM 3.7, DBAL 4.4, bundle 3.3, migrations-bundle 4.0); `doctrine.yaml` for PostgreSQL 16 (`server_version: '16'`, `uuid` mapping type default), `DATABASE_URL` from `.env`. Verify: `make console ARGS='doctrine:schema:validate --skip-sync'` reports mapping OK (no entities) and `make console ARGS='dbal:run-sql "SELECT 1"'` returns 1.
- [ ] 3.2 `make composer ARGS='require symfony/messenger symfony/redis-messenger'`; `messenger.yaml` with `async` (Redis Streams) and `failed` (Doctrine) transports, `failure_transport: failed`, retry 3/1000 ms/×2; `.env.test`: `MESSENGER_TRANSPORT_DSN=in-memory://`. Verify: `make console ARGS='debug:messenger'` lists both transports; `make console ARGS='messenger:stats'` connects to Redis without error.
- [ ] 3.3 `make composer ARGS='require symfony/monolog-bundle'`; `monolog.yaml`: `dev` → `var/log` + stderr, `prod` → JSON formatter to stderr, `test` → `var/log/test.log`. Verify: `make console ARGS='debug:config monolog --env=prod'` shows the JSON formatter handler.

## 4. API layer and health

- [ ] 4.1 `make composer ARGS='require api-platform/symfony'` (4.3); discover the prefix/format keys with `make console ARGS='config:dump-reference api_platform'`; configure `/api/v1` base path, docs at `/api/docs` and `/api/docs.json`, `formats: json + jsonproblem` only, `info.title: Linkboard API`, Swagger UI on, ReDoc off, `show_webby: false`. Verify: `curl -s http://127.0.0.1:8082/api/docs.json | jq -r '.openapi, .info.title'` prints `3.1.x` and `Linkboard API`; `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:8082/api/docs` prints 200.
- [ ] 4.2 Error format: `framework.exceptions` mapping for unexpected errors; `APP_DEBUG=0` in `.env.test`. Verify (spec `api-error-format`): `curl -s -i http://127.0.0.1:8082/api/v1/does-not-exist` shows 404 + `application/problem+json`; `curl -s -i -X DELETE http://127.0.0.1:8082/api/v1` shows 405 + problem json; `curl -s -i -H 'Accept: application/ld+json' http://127.0.0.1:8082/api/v1` shows 406 + problem json; `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:8082/api/graphql` prints 404.
- [ ] 4.3 `src/Shared/Health/HealthController.php` + `HealthProbe` (database `SELECT 1` with 2 s connect timeout, Redis `PING` with 2 s timeout), route `/health` in `config/routes.yaml`, `Cache-Control: no-store`, deep probe 404 in `prod`. Verify (spec `health-check`): `curl -s -i http://127.0.0.1:8082/health` → 200 `{"status":"ok"}` with `no-store`; `curl -s -i 'http://127.0.0.1:8082/health?deep=1'` → 200 with both checks ok; `docker compose stop redis` then the deep probe → 503 with `redis: fail` within 3 s (`time curl`), then `docker compose start redis`; `curl -s http://127.0.0.1:8082/api/docs.json | jq '.paths | has("/health")'` prints `false`.

## 5. Quality tooling and tests

- [ ] 5.1 `make composer ARGS='require --dev phpunit/phpunit symfony/test-pack dama/doctrine-test-bundle zenstruck/foundry'`; `phpunit.dist.xml` (`KERNEL_CLASS`, dama extension per the installed bundle README, `APP_ENV=test`), `.env.test` committed with `DATABASE_URL` to `linkboard_test`. New Makefile target `test-db` (`doctrine:database:create --if-not-exists --env=test`, `doctrine:migrations:migrate --env=test --no-interaction --allow-no-migration`) called by `init`; CI runs `make test-db EXEC=` before `make check EXEC=`. Verify: `make test-db` creates the database (`make console ARGS='dbal:run-sql "SELECT current_database()" --env=test'` prints `linkboard_test`).
- [ ] 5.2 Tests: `tests/Unit/Shared/Health/HealthProbeResultTest.php` (status/code mapping), `tests/Integration/Shared/Health/HealthProbeTest.php` (real postgres + redis, and a wrong Redis DSN → `fail`), `tests/Api/HealthTest.php`, `tests/Api/ApiDocsTest.php` (docs.json, Swagger UI, entrypoint, 406 for ld+json, 404 for graphql), `tests/Api/ProblemDetailsTest.php` (404, 405, 500-no-trace via `config/routes/test/boom.yaml` → `tests/Api/Fixture/BoomController`). Verify: `make test` green; every scenario of the three delta specs maps to a named test (table in the commit body).
- [ ] 5.3 `make composer ARGS='require --dev phpstan/phpstan phpstan/phpstan-symfony phpstan/phpstan-doctrine friendsofphp/php-cs-fixer'`; `phpstan.dist.neon` level 8 with both extensions (`tests/object-manager.php`); `.php-cs-fixer.dist.php` with `@Symfony`, `@Symfony:risky`, `declare_strict_types`, `native_function_invocation`. Verify: `make stan` and `make cs` green; demonstrated failing inputs: a file missing `declare(strict_types=1)` makes `make cs` fail; an untyped parameter makes `make stan` fail (both recorded in the commit body, not committed).
- [ ] 5.4 `make check` green through the real tools (no `[SKIP]` lines in its output). Verify: `make check 2>&1 | grep -c SKIP` prints 0 and the target exits 0.

## 6. Docs and wrap-up

- [ ] 6.1 `docs/how-to/local-development.md`: first run is `make init` again (build, up, composer install, migrate, test-db); URLs `/health`, `/api/docs`; note that the docroot is no longer empty. `docs/reference/commands.md`: `make test-db`. Verify: both re-read whole; every command in them run in its exact form.
- [ ] 6.2 Commit per block (`chore(stack):` 1.x, `feat(app):` 2.x, `feat(persistence):` 3.x, `feat(api):` 4.1–4.2, `feat(health):` 4.3, `test:`/`chore(quality):` 5.x, `docs:` 6.1) with the agent trailer. Verify: `git log --oneline main..HEAD`.
- [ ] 6.3 `openspec validate scaffold-symfony-app --strict`, `scripts/pregate-verify.sh gate2 scaffold-symfony-app` pass; request Gate 2 (medium tier). Verify: floor output has no FAIL line.
