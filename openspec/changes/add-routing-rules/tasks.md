## 0. Gate 1 (high tier)

- [x] 0.1 `scripts/pregate-verify.sh gate1 add-routing-rules` passes; `scripts/gate-run.sh add-routing-rules 1 full`; findings fixed via `/workflow:fix-findings` and confirmed with `scripts/gate-run.sh add-routing-rules 1 confirm <round>`. Verify: the last Gate 1 record in `review.md` reads `approved` or `confirmed`.

## 1. Dependencies and configuration

- [ ] 1.1 `make composer ARGS='require matomo/device-detector:^6.5 geoip2/geoip2:^3.4'`; review the `composer.lock` diff (expected additions: `matomo/device-detector`, `mustangostang/spyc`, `geoip2/geoip2`, `maxmind-db/reader`, `maxmind/web-service-common` and their own requirements only); `make composer ARGS='audit'` reports no advisory. Before writing any wrapper, verify the vendor API the design relies on and note deviations in `design.md` decisions 7–9: `rg -n 'class DeviceDetector|function setCache|function setYamlParser|function skipBotDetection|function discardBotInformation|function getDeviceName|function getOs\b|function getClient\b|function isBot' vendor/matomo/device-detector/DeviceDetector.php`, `rg -n 'public static function factory' vendor/matomo/device-detector/ClientHints.php`, `ls vendor/matomo/device-detector/Cache vendor/matomo/device-detector/Yaml`, `rg -n 'function getOsFamily|osFamilies' vendor/matomo/device-detector/Parser/OperatingSystem.php`, `rg -n 'function country|class AddressNotFoundException' vendor/geoip2/geoip2/src/Database/Reader.php vendor/geoip2/geoip2/src/Exception/AddressNotFoundException.php`, `rg -n 'public static function fromString|function all' vendor/symfony/http-foundation/AcceptHeader.php`, `rg -n 'public function boot' vendor/symfony/http-kernel/Kernel.php`. Commit body flags **security-sensitive: dependencies**. Verify: `make composer ARGS='show matomo/device-detector geoip2/geoip2'` prints both with the pinned constraints; `make check` still green.
- [ ] 1.2 `.env`: `COUNTRY_RESOLVERS=header,geolite2`, `GEOIP_COUNTRY_HEADER=CF-IPCountry`, `GEOIP_DATABASE_PATH=%kernel.project_dir%/var/geoip/GeoLite2-Country.mmdb` with comments (operator-downloaded, never committed; header honoured only from `TRUSTED_PROXIES`; an unknown resolver name fails boot); `.env.test`: `COUNTRY_RESOLVERS=fixed`; `config/packages/cache.yaml`: pool `cache.device_detector` on `cache.adapter.filesystem`; `config/services.yaml`: tag `app.country_resolver` with `key` on the header and GeoLite2 resolvers, `App\Redirect\Geo\CountryResolverInterface: '@App\Redirect\Geo\ChainCountryResolver'`, `App\Shared\Boot\StartupChecks` declared `public: true` with the `app.startup_check` tagged iterable, `when@test` registration of `App\Tests\Fixture\FixedMapCountryResolver` with key `fixed`. Verify: `make console ARGS='debug:dotenv'` lists the three variables; `make console ARGS='debug:container App\\Redirect\\Geo\\CountryResolverInterface'` resolves to the chain; `make console ARGS='debug:container --tag=app.country_resolver'` lists `header` and `geolite2`, and with `--env=test` also `fixed`; `make console ARGS='debug:container --tag=app.startup_check'` lists the chain; `make console ARGS='cache:pool:list'` lists `cache.device_detector`.

## 2. Rules document: parser, constraint, API, schema

- [ ] 2.1 `src/Link/Rules/RulesDocumentParser.php` (over the type-preserving tree of design decision 1: `\stdClass` objects, PHP lists, strict `is_int`), `ParseResult.php`, `RulesDocument.php` (constants; `toArray()` canonical form), `Rule.php`, `Variant.php`, `Dimension.php` (enum), `RuleViolation.php`; `src/Link/Api/RulesInput.php` (raw-body decode with objects preserved, `present`/`node`); `src/Link/Validator/RulesDocument.php` (constraint) + `RulesDocumentValidator.php` (`RequestStack` → `RulesInput` → `atPath()` per violation; root violation for a non-object non-null node); `Link::replaceRules()`; `CreateLinkInput`/`UpdateLinkInput` `mixed $rules` placeholders with the constraint; `CreateLinkProcessor` and `UpdateLinkProcessor` reading `RulesInput` and persisting `toArray()` (present → replace or clear); `LinkResource::$rules` with an `ApiProperty` description naming `docs/reference/rules-schema.json`. `tests/Unit/Link/Rules/RulesDocumentParserTest.php` (fixtures decoded with `json_decode($json, false)`): the specification's example document → 4 rules with dimensions device, device (os only), country, language and 2 variants, and `toArray()` re-encodes to a JSON value equal to the input; each violation case of spec routing-rules "Structural violations name their path", "JSON objects where arrays are required, arrays where objects are required" (all nine positions: `rules`, `device`, `os`, `country`, `language`, `variants`, a rule as array, `match` as array, a variant as array), "Variant violations name their path" and "Rule and variant targets obey the target URL policy" → the exact path set; `null` → no document, no violation; `[]`, `{}`, `"x"`, `1` → one violation at the root; stored mode: the canonical array of the example parses to the same model, and distinguishable malformed stored arrays — `version` 2, `rules` as a string, a `match` with `device` and `country`, a weight `"50"` — yield violations (design decisions 1 and 6; a numeric-keyed object is not a stored-mode test case because it is indistinguishable from a list after associative decoding). Verify: `make test` green; failing inputs recorded in the commit body: removing the "exactly one dimension" check fails the `device`+`country` case; removing the weight-sum check fails the 60/50 case; removing the target policy call fails the metadata-IP target case; replacing the `\stdClass` test by "any array" makes the six numeric-keyed-object cases pass validation and fail the test.
- [ ] 2.2 `docs/reference/rules-schema.json` (JSON Schema 2020-12 per design decision 3, `type` on every member) and `tests/Unit/Link/Rules/RulesSchemaParityTest.php` (every enumeration, limit, pattern, `type` and `additionalProperties: false` compared with the `RulesDocument` constants and the parser's type tests; the how-to example document of task 5.1 parses without violations). Verify: `python3 -m json.tool docs/reference/rules-schema.json > /dev/null` exits 0; `make test` green; failing input: changing the schema's `maxItems` for `rules` to 21, or its `type` for `rules` to `object`, fails the parity test (commit body).
- [ ] 2.3 `tests/Api/Link/LinkRulesTest.php` (extends `LinkApiTestCase`): POST with the example document → 201 and `rules` JSON-value-equal to the posted document; `GET /api/v1/links/{id}` and `GET /api/v1/links` carry `rules`; a link without rules → `"rules": null`; every 422 scenario of the spec over HTTP asserted with `violationPaths()`, including the nine JSON-kind cases (six numeric-keyed objects at every list position, three arrays where objects are required) on POST and on PATCH (stored `rules` unchanged after each rejected PATCH, read back through `GET`); `"rules": "x"`, `"rules": []` and `"rules": {}` → `['rules']`; PATCH: replace whole (two rules → variants only), absent → unchanged, `null` → cleared, invalid document → 422 and the stored document unchanged. Verify: `make test` green; `curl` against the dev stack recorded in the commit body: POST with rules → 201 with `rules`, POST with `"rules": {"0": …}` → 422 `application/problem+json` whose `violations[0].propertyPath` is `rules[rules]`.

## 3. Detection, country and language resolution

- [ ] 3.1 `src/Click/Visit.php` gains `acceptLanguage`, `clientHints`, `proxyCountry`, `inputIssues` (design decision 4; `fromHeaders` removed — `rg -n 'fromHeaders' src tests` empty afterwards); `src/Redirect/VisitFactory.php` (classification absent / well-formed / hostile per header with the issue classes; `userAgent` always cut to 1024 bytes for hashing, `referer` to 2048; country header only when `Request::isFromTrustedProxy()`; header name from `GEOIP_COUNTRY_HEADER`); `RedirectController` uses the factory. `tests/Unit/Redirect/VisitFactoryTest.php`: clean request → empty `inputIssues`; UA of 1025 bytes → `user_agent_oversized` and `userAgent` cut to 1024; UA with `\x00` or invalid UTF-8 → `user_agent_malformed`; `Accept-Language` of 257 bytes → `accept_language_oversized` and `acceptLanguage` null; `Accept-Language: de;q=abc` and `Accept-Language: de, ,fr` → `accept_language_malformed`; `Accept-Language: uk-UA;q=0.8, ru;q=0.9` → carried verbatim; a `Sec-CH-UA` of 257 bytes → `client_hints_oversized` and the hint dropped while other hints stay; `Sec-Fetch-Dest` is not a client hint; an untrusted peer with `CF-IPCountry: DE` → `proxyCountry` null and no issue; a request from `127.0.0.1` with the header → `DE`; from `127.0.0.1` with a 17-byte value → `proxy_country_malformed`. Verify: `make test` green; failing inputs recorded in the commit body: removing the `isFromTrustedProxy()` condition fails the untrusted-peer case; removing the grammar check lets `de;q=abc` through and fails its case.
- [ ] 3.2 `src/Redirect/Detection/DeviceDetection.php` + `DetectedClient.php` (design decision 7: PSR-6 bridge on `cache.device_detector`, Symfony YAML parser, client hints, mapping tables). `tests/Unit/Redirect/Detection/DeviceDetectionTest.php` with fixed user-agent strings: Safari/iPhone → `smartphone`/`iOS`; Chrome/Android phone → `smartphone`/`Android`; Safari/iPad → `tablet`/`iOS`; Chrome/Windows → `desktop`/`Windows`; Safari/macOS → `desktop`/`macOS`; Firefox/Linux → `desktop`/`Linux`; Googlebot → `isBot` true; a smart-TV user agent → `other`; `curl/8.5.0` and `SomethingNew/1.0 (unknown)` → all null, `isBot` false; empty string → all null; `browser` non-null for the six browsers and at most 32 characters; the same instance detects twice (cache path exercised). Verify: `make test` green; the test runs under `--filter DeviceDetectionTest` in under 5 s with a warm `var/cache/test` (cold-start note in the how-to).
- [ ] 3.3 `src/Redirect/Language/AcceptLanguage.php` (design decision 9, well-formed input only) + `tests/Unit/Redirect/Language/AcceptLanguageTest.php`: `uk-UA;q=0.8, ru;q=0.9` → `ru`; `en-US,en;q=0.9` → `en`; `de-CH;q=0.7, fr-CH;q=0.9` → `fr`; `zh-Hant-TW` → `zh`; `*` → null; `x` → null; null → null. Verify: `make test` green.
- [ ] 3.4 `src/Redirect/Geo/CountryResolverInterface.php`, `HeaderCountryResolver.php`, `GeoLite2CountryResolver.php` (lazy reader through an injected factory closure; open failure → disabled flag + one `warning` → null; `AddressNotFoundException` → null, no log; any other lookup `\Throwable` propagates — no local catch, design decision 8), `ChainCountryResolver.php` (`COUNTRY_RESOLVERS` order; unknown name → `\InvalidArgumentException` naming it and the known keys; implements `StartupCheckInterface`), `src/Shared/Boot/StartupCheckInterface.php` + `StartupChecks.php`, `src/Kernel.php` `boot()` override calling `StartupChecks::run()` after `parent::boot()`, `tests/Fixture/FixedMapCountryResolver.php` (map of design decision 8). Unit tests `tests/Unit/Redirect/Geo/*Test.php`: header — `de` → `DE`, `XX`/`T1`/`D3`/null → null; GeoLite2 — factory throws (missing file) → null on two calls and exactly one `warning`; reader returns `DE` → `DE`; reader throws `AddressNotFoundException` → null and no record; reader throws `\RuntimeException` → the same exception propagates out of `resolve()` and no record is written by the resolver; chain — `header,geolite2` returns the header's answer first and falls back; `fixed` alone; `bogus` → exception naming `bogus`, `header` and `geolite2`. Integration `tests/Integration/Redirect/Geo/CountryResolversBootTest.php`: with `$_SERVER['COUNTRY_RESOLVERS'] = 'bogus'` set before `self::bootKernel()` (restored in `finally`) the boot throws an exception whose message contains `bogus` — without the test touching the chain; with `fixed` the kernel boots and `static::getContainer()->get(CountryResolverInterface::class)` is the chain. Verify: `make test` green; `docker compose exec -T -e COUNTRY_RESOLVERS=bogus php bin/console about` exits non-zero with the message naming `bogus` (recorded in the commit body) and `make console ARGS='about'` exits 0; failing inputs recorded in the commit body: removing the `boot()` override keeps the console command green and fails the integration test; removing the disabled flag repeats the warning (count assertion fails); widening the lookup catch to `\Throwable` (swallowing the runtime error as unknown) fails the propagation case; removing the name check fails the `bogus` cases.

## 4. Evaluation on redirect and the click facts

- [ ] 4.1 `src/Redirect/VisitorProfile.php`, `VisitorProfiler.php`, `src/Redirect/Rules/RuleEvaluator.php`, `VariantPicker.php`, `Resolution.php`, `ResolvedBy.php` (enum), `UnusableRulesException.php`, `src/Click/ClickFacts.php`, `ClickRecorderInterface::record(Link, Visit, ClickFacts)`, `DbalClickRecorder` writing the seven columns (`browser` cut to 32), `RedirectResolver` with the hostile short-circuit and the guard of design decision 5 (`notice` with `link_id` and `issues` or `exception` only). `tests/Unit/Redirect/Rules/RuleEvaluatorTest.php`: device beats country beats language regardless of document order; document order within a dimension; `device`+`os` requires both; iPad falls through an `os: [Android]` rule; a null dimension skips the rule and evaluation continues to the variants; variants only after no rule matched; no rules and no variants → default with `targetUrl`; null stored rules → default; a stored document with a violation → `UnusableRulesException`. `tests/Unit/Redirect/Rules/VariantPickerTest.php`: same inputs → same variant over 100 calls; 10 000 distinct synthetic visitors for weights 70/30 → each share within ±3 pp; cumulative boundaries (`value` 0 → first variant, 49 → first and 50 → second for 50/50 — asserted through a seam that exposes the bucket function); different link ids are computed independently. `tests/Unit/Redirect/RedirectResolverTest.php` extended: a `Visit` with `inputIssues` → `redirect` to `targetUrl` with UTM, facts `default` with null dimensions, exactly one `notice` with the link id and the issue classes, the profiler and evaluator never called; profiler throws → the same with the exception class; evaluator throws `UnusableRulesException` → the same; no IP/UA/header value in any record; HEAD resolves the rule target and never calls `record()`; a matching rule's target reaches `record()` as `resolvedBy` `device`. Verify: `make test` green; failing inputs recorded in the commit body: evaluating dimensions in document order fails "device beats country"; removing the guard fails the profiler-throws case; removing the hostile short-circuit fails the `inputIssues` case (the profiler mock is called); replacing `% 100` by `% 2` fails the distribution case.
- [ ] 4.2 `tests/Web/Redirect/RoutingRulesTest.php` (real detection, fixed map IPs `198.51.100.7`/`.44`/`.33`, `Accept-Language`): the matrix scenario of spec routing-rules "Fixed matching order" with all four visitors; document order within a dimension; the iOS/Android device scenario with four user agents; default when nothing matches; unknown country skips to the language rule; missing headers with variants → 302 to the variant, null `device_type`/`os`/`country`, `resolved_by` `variant`, no record at `notice` or above; `curl/8.5.0` + `Accept-Language: *` + unmapped IP → default, nulls, no record; the seven user agents of "Common user agents" → `(device_type, os)`, `is_bot`, `browser`; the six `Accept-Language` values of "Quality ordering and subtag" against a link with `ru`/`en`/`fr`/`zh` rules and variants → rule or variant as specified; same visitor 20 × → one `Location` and one `variant`; UTM on a rule target for GET and HEAD; the click rows of spec click-logging "Resolution recorded" and "Plain link". Verify: `make test` green; every scenario of spec `routing-rules` requirements 3–8 and of the modified `click-logging` requirement maps to an assertion (table in the commit body).
- [ ] 4.3 `tests/Web/Redirect/RoutingDegradationTest.php` (`disableReboot`, stubs through the test container): hostile headers (4 KB random `User-Agent`, 8 KB `Accept-Language` of `de,`, 4 KB `Sec-CH-UA`) to a link with a `language` rule for `de` and variants → 302 to `targetUrl` (UTM appended), exactly one click with `resolved_by` `default`, null dimensions and `variant`, exactly one `notice` with the link id and the three issue classes and no header value in any record; `User-Agent: Mozilla/5.0 \x00 garbage` → default, one `notice` with `user_agent_malformed`; a throwing `DeviceDetection` → 302 to `targetUrl`, click `resolved_by` `default` with null `device_type`/`os`/`variant`, exactly one `notice` with the link id and exception class; `CF-IPCountry: DE` from `203.0.113.7` (untrusted) → country rule skipped and `country` null, from `127.0.0.1` with `X-Forwarded-For: 203.0.113.7` → rule matched and `country` `DE` (the `fixed` chain replaced by `header` for this test through the container); the three GeoLite2 scenarios of spec routing-rules "Country resolution" against a link with a `country` rule, a `language` rule for `de` and variants, the chain replaced through the container by a `geolite2`-only chain whose reader factory the test controls: missing file → two requests both 302 to the language target, `country` null, exactly one `warning`, no `notice`; `AddressNotFoundException` → language target, `country` null, no record at `notice` or above; reader `\RuntimeException` → 302 to `targetUrl`, click `resolved_by` `default` with null `country`/`device_type`/`os`/`variant`, exactly one `notice` with the link id and exception class and no IP. Verify: `make test` green; failing inputs recorded in the commit body: removing the trusted-proxy condition (3.1) fails the untrusted case; removing the guard (4.1) fails the throwing-detector case; removing the hostile short-circuit lets the 8 KB `de,` header select the language rule and fails the hostile case; catching the reader error inside the resolver lets the language rule win and fails the reader-error case.
- [ ] 4.4 Existing suites adapted to the new seam and columns: `tests/Unit/Redirect/RedirectResolverTest.php`, `tests/Web/Redirect/RedirectDegradationTest.php`, `tests/Web/Redirect/ClickLoggingTest.php` (the column set is unchanged; `country`/`device_type`/`os`/`browser` now reflect the `Probe/1.0` agent and the unmapped default IP → null, `is_bot` false; the existing 8 KB `User-Agent` scenario still yields 302 and one click — now with `resolved_by` `default` and a `notice`), `tests/Integration/Click/DbalClickRecorderTest.php` and `tests/Fixture/record-click.php` (facts argument; a row written with facts `country DE`, `device_type smartphone`, `resolved_by variant`, `variant B` reads back identically). Verify: `make test` green; `rg -n 'record\(' src tests` shows only three-argument calls.

## 5. Docs

- [ ] 5.1 `docs/how-to/local-development.md`: a "Routing rules" section — a `curl` creating a link with the example document (the same document `RulesSchemaParityTest` reads from this file or a shared fixture), testing a device rule with `curl -sI -A '<iPhone UA>' http://localhost:8082/<slug>` and a language rule with `-H 'Accept-Language: de'`, the three variables, the boot failure on an unknown resolver name (`docker compose exec -T -e COUNTRY_RESOLVERS=bogus php bin/console about`), the GeoLite2 download (MaxMind account → `GeoLite2-Country.mmdb` → `var/geoip/`, picked up at the next process start, one warning while absent), the trusted-proxy condition of the header resolver, the input policy in three lines (absent/unrecognised → skipped; oversized/malformed → default target + `notice`; the click is still recorded), the schema location, the cold-cache note; the Redirect section's `Location` line updated to "resolved destination". Verify: re-read whole after the last edit; every documented command run in its exact form against the dev stack and its output pasted into the commit body; `rg -n 'rules-schema.json' docs src tests` shows the reference, the `ApiProperty` description and the parity test.
- [ ] 5.2 `docs/explanation/requirements.md`: FR-RUL-7 already reconciled in this change's planning commit (hostile = malformed/oversized → default + `notice`; missing or unrecognised → FR-RUL-4 skip); re-read FR-RUL-4/6/7, §7 row 6 and `openspec/ROADMAP.md` row 6 against the delivered scope; edit only if a scope claim changed. Verify: `rg -n 'add-routing-rules|FR-RUL-7' openspec/ROADMAP.md docs/explanation/requirements.md` shows the rows and they match the specs.

## 6. Wrap-up

- [ ] 6.1 `make check` green (cs, stan level 8, all suites); commits per block (`chore(deps):` 1.1, `chore(config):` 1.2, `feat(link):` 2.x, `feat(redirect):` 3.x, `feat(redirect):` 4.1, `test(redirect):` 4.2–4.4, `docs:` 5.x) with the agent trailer; every guard's commit body names its demonstrated failing input; commit bodies and `handoff.md` flag the security-sensitive parts (new dependencies, rule evaluation over untrusted input, hostile-input classification, trusted-proxy country header, rule targets under the URL policy, the boot check). Verify: `git log --oneline main..HEAD`.
- [ ] 6.2 Green Actions run on the exact branch head before Gate 2: the user pushes change/add-routing-rules; the executor queries `https://api.github.com/repos/Azazu/linkboard/actions/runs?branch=change/add-routing-rules` for the head SHA until `status` is `completed` and `conclusion` is `success`; URL and SHA recorded in `handoff.md`.
- [ ] 6.3 `openspec validate add-routing-rules --strict` and `scripts/pregate-verify.sh gate2 add-routing-rules` pass; `scripts/gate-run.sh add-routing-rules 2 full`. Verify: no FAIL line.

## Post-merge acceptance (not a Gate 2 task)

- After the merge: the user pushes `main`; the executor checks the `main` run via the API before proposing the archive; `/opsx:archive` creates spec `routing-rules` and syncs `links`, `redirect`, `click-logging`.
