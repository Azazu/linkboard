## 0. Gate 1 (high tier)

- [ ] 0.1 `scripts/pregate-verify.sh gate1 add-routing-rules` passes; `scripts/gate-run.sh add-routing-rules 1 full`; findings fixed via `/workflow:fix-findings` and confirmed with `scripts/gate-run.sh add-routing-rules 1 confirm <round>`. Verify: the last Gate 1 record in `review.md` reads `approved` or `confirmed`.

## 1. Dependencies and configuration

- [ ] 1.1 `make composer ARGS='require matomo/device-detector:^6.5 geoip2/geoip2:^3.4'`; review the `composer.lock` diff (expected additions: `matomo/device-detector`, `mustangostang/spyc`, `geoip2/geoip2`, `maxmind-db/reader`, `maxmind/web-service-common` and their own requirements only); `make composer ARGS='audit'` reports no advisory. Before writing any wrapper, verify the vendor API the design relies on and note deviations in `design.md` decisions 7–9: `rg -n 'class DeviceDetector|function setCache|function setYamlParser|function skipBotDetection|function discardBotInformation|function getDeviceName|function getOs\b|function getClient\b|function isBot' vendor/matomo/device-detector/DeviceDetector.php`, `rg -n 'public static function factory' vendor/matomo/device-detector/ClientHints.php`, `ls vendor/matomo/device-detector/Cache vendor/matomo/device-detector/Yaml`, `rg -n 'function getOsFamily|osFamilies' vendor/matomo/device-detector/Parser/OperatingSystem.php`, `rg -n 'function country|class AddressNotFoundException' vendor/geoip2/geoip2/src/Database/Reader.php vendor/geoip2/geoip2/src/Exception/AddressNotFoundException.php`, `rg -n 'public static function fromString|function all' vendor/symfony/http-foundation/AcceptHeader.php`. Commit body flags **security-sensitive: dependencies**. Verify: `make composer ARGS='show matomo/device-detector geoip2/geoip2'` prints both with the pinned constraints; `make check` still green.
- [ ] 1.2 `.env`: `COUNTRY_RESOLVERS=header,geolite2`, `GEOIP_COUNTRY_HEADER=CF-IPCountry`, `GEOIP_DATABASE_PATH=%kernel.project_dir%/var/geoip/GeoLite2-Country.mmdb` with comments (operator-downloaded, never committed; header honoured only from `TRUSTED_PROXIES`); `.env.test`: `COUNTRY_RESOLVERS=fixed`; `config/packages/cache.yaml`: pool `cache.device_detector` on `cache.adapter.filesystem`; `config/services.yaml`: tag `app.country_resolver` with `key` on the header and GeoLite2 resolvers, `App\Redirect\Geo\CountryResolverInterface: '@App\Redirect\Geo\ChainCountryResolver'`, `when@test` registration of `App\Tests\Fixture\FixedMapCountryResolver` with key `fixed`. Verify: `make console ARGS='debug:dotenv'` lists the three variables; `make console ARGS='debug:container App\\Redirect\\Geo\\CountryResolverInterface'` resolves to the chain; `make console ARGS='debug:container --tag=app.country_resolver'` lists `header` and `geolite2`, and with `--env=test` also `fixed`; `make console ARGS='cache:pool:list'` lists `cache.device_detector`.

## 2. Rules document: parser, constraint, API, schema

- [ ] 2.1 `src/Link/Rules/RulesDocumentParser.php`, `ParseResult.php`, `RulesDocument.php` (constants of design decision 1), `Rule.php`, `Variant.php`, `Dimension.php` (enum), `RuleViolation.php`; `src/Link/Validator/RulesDocument.php` (constraint) + `RulesDocumentValidator.php` (`atPath()` per violation); `Link::replaceRules()`; `CreateLinkInput`/`UpdateLinkInput` `mixed $rules` with the constraint; `CreateLinkProcessor` (non-null → replace) and `UpdateLinkProcessor` (present → replace or clear); `LinkResource::$rules` with an `ApiProperty` description naming `docs/reference/rules-schema.json`. `tests/Unit/Link/Rules/RulesDocumentParserTest.php`: the specification's example document → 4 rules with dimensions device, device (os only), country, language and 2 variants; each violation case of spec routing-rules "Structural violations name their path", "Variant violations name their path" and "Rule and variant targets obey the target URL policy" → the exact path set; `null` → no document, no violation; `[]`, `"x"`, `1` → one violation at the root. Verify: `make test` green; failing inputs recorded in the commit body: removing the "exactly one dimension" check fails the `device`+`country` case; removing the weight-sum check fails the 60/50 case; removing the target policy call fails the metadata-IP target case.
- [ ] 2.2 `docs/reference/rules-schema.json` (JSON Schema 2020-12 per design decision 3) and `tests/Unit/Link/Rules/RulesSchemaParityTest.php` (every enumeration, limit, pattern and `additionalProperties: false` compared with the `RulesDocument` constants; the how-to example document of task 5.1 parses without violations). Verify: `python3 -m json.tool docs/reference/rules-schema.json > /dev/null` exits 0; `make test` green; failing input: changing the schema's `maxItems` for `rules` to 21 fails the parity test (commit body).
- [ ] 2.3 `tests/Api/Link/LinkRulesTest.php` (extends `LinkApiTestCase`): POST with the example document → 201 and `rules` echoed byte-equal after JSON re-encoding; `GET /api/v1/links/{id}` and `GET /api/v1/links` carry `rules`; a link without rules → `"rules": null`; every 422 scenario of the spec over HTTP asserted with `violationPaths()`; `"rules": "x"` and `"rules": []` → `['rules']`; PATCH: replace whole (two rules → variants only), absent → unchanged, `null` → cleared, invalid document → 422 and the stored document unchanged. Verify: `make test` green; `curl` against the dev stack recorded in the commit body: POST with rules → 201 with `rules`, POST with a 21-rule document → 422 `application/problem+json` whose `violations[0].propertyPath` is `rules[rules]`.

## 3. Detection, country and language resolution

- [ ] 3.1 `src/Click/Visit.php` gains `acceptLanguage`, `clientHints`, `proxyCountry` (design decision 4; `fromHeaders` removed — `rg -n 'fromHeaders' src tests` empty afterwards); `src/Redirect/VisitFactory.php` (bounds UA 1024, Referer 2048, `Accept-Language` 256, each `Sec-CH-UA*` header 256, country header 16 bytes only when `Request::isFromTrustedProxy()`; header name from `GEOIP_COUNTRY_HEADER`); `RedirectController` uses the factory. `tests/Unit/Redirect/VisitFactoryTest.php`: every bound; an untrusted peer with `CF-IPCountry: DE` → `proxyCountry` null; a request from `127.0.0.1` with the header → `DE`; a `Sec-Fetch-Dest` header is not a client hint. Verify: `make test` green; failing input: removing the `isFromTrustedProxy()` condition fails the untrusted-peer case (commit body).
- [ ] 3.2 `src/Redirect/Detection/DeviceDetection.php` + `DetectedClient.php` (design decision 7: PSR-6 bridge on `cache.device_detector`, Symfony YAML parser, client hints, mapping tables). `tests/Unit/Redirect/Detection/DeviceDetectionTest.php` with fixed user-agent strings: Safari/iPhone → `smartphone`/`iOS`; Chrome/Android phone → `smartphone`/`Android`; Safari/iPad → `tablet`/`iOS`; Chrome/Windows → `desktop`/`Windows`; Safari/macOS → `desktop`/`macOS`; Firefox/Linux → `desktop`/`Linux`; Googlebot → `isBot` true; a smart-TV user agent → `other`; empty string → all null, `isBot` false; 1024 bytes of random printable characters → all null; `browser` non-null for the six browsers and at most 32 characters; the same instance detects twice (cache path exercised). Verify: `make test` green; the test runs under `--filter DeviceDetectionTest` in under 5 s with a warm `var/cache/test` (cold-start note in the how-to).
- [ ] 3.3 `src/Redirect/Language/AcceptLanguage.php` (design decision 9) + `tests/Unit/Redirect/Language/AcceptLanguageTest.php`: `uk-UA;q=0.8, ru;q=0.9` → `ru`; `en-US,en;q=0.9` → `en`; `de-CH;q=0.7, fr-CH;q=0.9` → `fr`; `*` → null; `x` → null; `zh-Hant-TW` → `zh`; null/empty → null; an 8 KB header of `de,` repeated → `de` and no exception; invalid UTF-8 → null or a two-letter tag, never an exception. Verify: `make test` green.
- [ ] 3.4 `src/Redirect/Geo/CountryResolverInterface.php`, `HeaderCountryResolver.php`, `GeoLite2CountryResolver.php` (lazy reader through an injected factory closure; disabled flag + one `warning`; `AddressNotFoundException` → null; other `\Throwable` → null + `notice` without IP), `ChainCountryResolver.php` (`COUNTRY_RESOLVERS` order; unknown name → `\InvalidArgumentException` in the constructor), `tests/Fixture/FixedMapCountryResolver.php` (map of design decision 8). Unit tests `tests/Unit/Redirect/Geo/*Test.php`: header — `de` → `DE`, `XX`/`T1`/`D3`/null → null; GeoLite2 — factory throws (missing file) → null on two calls and exactly one `warning`; reader returns `DE` → `DE`; reader throws `AddressNotFoundException` → null and no record; reader throws `\RuntimeException` → null and one `notice` whose context has no `ip` key and whose message does not contain the IP; chain — `header,geolite2` returns the header's answer first and falls back; `fixed` alone; `bogus` → exception. Verify: `make test` green; `make console ARGS='debug:container App\\Redirect\\Geo\\ChainCountryResolver'` (dev) constructs without error; failing inputs recorded in the commit body: removing the disabled flag repeats the warning (count assertion fails); narrowing the lookup catch to `AddressNotFoundException` fails the reader-failure case; removing the name check fails the `bogus` case.

## 4. Evaluation on redirect and the click facts

- [ ] 4.1 `src/Redirect/VisitorProfile.php`, `VisitorProfiler.php`, `src/Redirect/Rules/RuleEvaluator.php`, `VariantPicker.php`, `Resolution.php`, `ResolvedBy.php` (enum), `UnusableRulesException.php`, `src/Click/ClickFacts.php`, `ClickRecorderInterface::record(Link, Visit, ClickFacts)`, `DbalClickRecorder` writing the seven columns (`browser` cut to 32), `RedirectResolver` with the guard of design decision 5 (`notice` with `link_id` and `exception` only). `tests/Unit/Redirect/Rules/RuleEvaluatorTest.php`: device beats country beats language regardless of document order; document order within a dimension; `device`+`os` requires both; iPad falls through an `os: [Android]` rule; a null dimension skips the rule and evaluation continues; variants only after no rule matched; no rules and no variants → default with `targetUrl`; null stored rules → default; a stored document with a violation → `UnusableRulesException`. `tests/Unit/Redirect/Rules/VariantPickerTest.php`: same inputs → same variant over 100 calls; 10 000 distinct synthetic visitors for weights 70/30 → each share within ±3 pp; cumulative boundaries (`value` 0 → first variant, 49 → first and 50 → second for 50/50 — asserted through a seam that exposes the bucket function); different link ids are computed independently. `tests/Unit/Redirect/RedirectResolverTest.php` extended: profiler throws → `redirect` to `targetUrl` with UTM, facts `default` with null dimensions, exactly one `notice` with the link id and exception class and no IP/UA; evaluator throws `UnusableRulesException` → same; HEAD resolves the rule target and never calls `record()`; a matching rule's target reaches `record()` as `resolvedBy` `device`. Verify: `make test` green; failing inputs recorded in the commit body: evaluating dimensions in document order fails "device beats country"; removing the guard fails the profiler-throws case; replacing `% 100` by `% 2` fails the distribution case.
- [ ] 4.2 `tests/Web/Redirect/RoutingRulesTest.php` (real detection, fixed map IPs `198.51.100.7`/`.44`/`.33`, `Accept-Language`): the matrix scenario of spec routing-rules "Fixed matching order" with all four visitors; document order within a dimension; the iOS/Android device scenario with four user agents; default when nothing matches; unknown country skips to the language rule; missing headers → 302 to `targetUrl` with null `device_type`/`os`/`country` and `resolved_by` `default`; the seven user agents of "Common user agents" → `(device_type, os)`, `is_bot`, `browser`; `Accept-Language` variants of "Quality ordering and subtag" → language rule matched or not; same visitor 20 × → one `Location` and one `variant`; UTM on a rule target for GET and HEAD; the click rows of spec click-logging "Resolution recorded" and "Plain link". Verify: `make test` green; every scenario of spec `routing-rules` requirements 3–8 and of the modified `click-logging` requirement maps to an assertion (table in the commit body).
- [ ] 4.3 `tests/Web/Redirect/RoutingDegradationTest.php` (`disableReboot`, stubs through the test container): a throwing `DeviceDetection` → 302 to `targetUrl` (UTM appended), click `resolved_by` `default` with null `device_type`/`os`/`variant`, exactly one `notice` with the link id and exception class, no IP/UA/header value in any record; hostile headers (4 KB random `User-Agent`, 8 KB `Accept-Language`, 4 KB `Sec-CH-UA`) → 302, one click, no record at `notice` or above; garbage `User-Agent` → 302 to `targetUrl`, null device/OS, `is_bot` false; `CF-IPCountry: DE` from `203.0.113.7` (untrusted) → country rule skipped and `country` null, from `127.0.0.1` with `X-Forwarded-For: 203.0.113.7` → rule matched and `country` `DE` (the `fixed` chain is replaced by `header` for this test through the container); a `GeoLite2CountryResolver` built in the test with a missing file → two requests, one `warning`. Verify: `make test` green; failing inputs recorded in the commit body: removing the trusted-proxy condition (3.1) fails the untrusted case; removing the guard (4.1) fails the throwing-detector case; removing the `Accept-Language` bound makes the 8 KB case reach the parser unbounded (assert the bound through `VisitFactoryTest` 3.1).
- [ ] 4.4 Existing suites adapted to the new seam and columns: `tests/Unit/Redirect/RedirectResolverTest.php`, `tests/Web/Redirect/RedirectDegradationTest.php`, `tests/Web/Redirect/ClickLoggingTest.php` (the column set is unchanged; `country`/`device_type`/`os`/`browser` now reflect the `Probe/1.0` agent and the unmapped default IP → null, `is_bot` false), `tests/Integration/Click/DbalClickRecorderTest.php` and `tests/Fixture/record-click.php` (facts argument; a row written with facts `country DE`, `device_type smartphone`, `resolved_by variant`, `variant B` reads back identically). Verify: `make test` green; `rg -n 'record\(' src tests` shows only three-argument calls.

## 5. Docs

- [ ] 5.1 `docs/how-to/local-development.md`: a "Routing rules" section — a `curl` creating a link with the example document (the same document `RulesSchemaParityTest` reads from this file or a shared fixture), testing a device rule with `curl -sI -A '<iPhone UA>' http://localhost:8082/<slug>` and a language rule with `-H 'Accept-Language: de'`, the three variables, the GeoLite2 download (MaxMind account → `GeoLite2-Country.mmdb` → `var/geoip/`, picked up at the next process start, one warning while absent), the trusted-proxy condition of the header resolver, the schema location, the cold-cache note; the Redirect section's `Location` line updated to "resolved destination". Verify: re-read whole after the last edit; every documented command run in its exact form against the dev stack and its output pasted into the commit body; `rg -n 'rules-schema.json' docs src tests` shows the reference, the `ApiProperty` description and the parity test.
- [ ] 5.2 Re-read `openspec/ROADMAP.md` row 6 and `docs/explanation/requirements.md` §7 row 6 against the delivered scope; edit only if a scope claim changed. Verify: `rg -n 'add-routing-rules' openspec/ROADMAP.md docs/explanation/requirements.md` shows the rows and they match.

## 6. Wrap-up

- [ ] 6.1 `make check` green (cs, stan level 8, all suites); commits per block (`chore(deps):` 1.1, `chore(config):` 1.2, `feat(link):` 2.x, `feat(redirect):` 3.x, `feat(redirect):` 4.1, `test(redirect):` 4.2–4.4, `docs:` 5.x) with the agent trailer; every guard's commit body names its demonstrated failing input; commit bodies and `handoff.md` flag the security-sensitive parts (new dependencies, rule evaluation over untrusted input, trusted-proxy country header, rule targets under the URL policy). Verify: `git log --oneline main..HEAD`.
- [ ] 6.2 Green Actions run on the exact branch head before Gate 2: the user pushes change/add-routing-rules; the executor queries `https://api.github.com/repos/Azazu/linkboard/actions/runs?branch=change/add-routing-rules` for the head SHA until `status` is `completed` and `conclusion` is `success`; URL and SHA recorded in `handoff.md`.
- [ ] 6.3 `openspec validate add-routing-rules --strict` and `scripts/pregate-verify.sh gate2 add-routing-rules` pass; `scripts/gate-run.sh add-routing-rules 2 full`. Verify: no FAIL line.

## Post-merge acceptance (not a Gate 2 task)

- After the merge: the user pushes `main`; the executor checks the `main` run via the API before proposing the archive; `/opsx:archive` creates spec `routing-rules` and syncs `links`, `redirect`, `click-logging`.
