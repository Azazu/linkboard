## 0. Gate 1 (high tier)

- [ ] 0.1 `scripts/pregate-verify.sh gate1 add-async-click-logging` passes; `scripts/gate-run.sh add-async-click-logging 1 full`; findings fixed via `/workflow:fix-findings` and confirmed with `scripts/gate-run.sh add-async-click-logging 1 confirm <round>`. Verify: the last Gate 1 record in `review.md` reads `approved` or `confirmed`.

## 1. Counter and message

- [ ] 1.1 `src/Click/Counter/ClickCounterInterface.php` (`increment(Uuid, int $seed, int $max): RecordOutcome`, `forget(Uuid): void`), `src/Click/Counter/RedisClickCounter.php` (design decision 2: lazy `\Redis` from `RedisAdapter::createConnection(REDIS_URL)`, the Lua script as a constant — lift an absent or lower key to the seed, never lower it, compare, increment — one `eval($script, [$key, $seed, $max], 1)` per call, key `<prefix>link:<uuid>:clicks`, `forget` = `DEL`, non-integer reply or `\RedisException` → `\RuntimeException`); `config/services.yaml`: `$keyPrefix: ''` and `when@test` `'test:'`. Before writing, verify the vendor API: `rg -n 'public static function createConnection' vendor/symfony/cache/Traits/RedisTrait.php` and `docker compose exec -T php php -r 'echo new ReflectionMethod("Redis","eval");'`. `tests/Integration/Click/RedisClickCounterTest.php` (real Redis from `REDIS_URL`, keys deleted in `tearDown`): absent key + seed 0 + max 3 → allowed ×3 then exhausted, key holds 3; absent key + seed 2 + max 3 → allowed once then exhausted, key holds 3; existing key 3 + max 5 → allowed (limit change applies); existing key 3 + seed 7 + max 10 → the key is lifted to 7 and the call allows (ends at 8) — the re-enabled-limit case; existing key 7 + seed 3 → the key is not lowered; `forget` deletes the key and the next call re-seeds; `tests/Fixture/count-click.php` + a case that starts ten processes on a fresh key with max 3 → exactly 3 allowed, key 3 (the child-process pattern of `add-redirect-with-sync-logging`); a stub client whose `eval` throws `\RedisException` → `\RuntimeException` propagates. Verify: `make test` green; `docker compose exec -T redis redis-cli --scan --pattern 'test:link:*'` is empty after the suite; failing inputs recorded in the commit body: replacing the script by `INCR` + PHP compare fails the ten-process case (more than 3 allowed under contention); dropping the `SET` seed branch fails the seed-2 case; dropping the `current < seed` lift fails the key-3/seed-7 case.
- [ ] 1.2 `src/Click/Message/ClickRecorded.php` (readonly, scalars + `\DateTimeImmutable`, design decision 3); `config/packages/messenger.yaml`: `routing: App\Click\Message\ClickRecorded: async`, `failed` as `dsn` + `options.auto_setup: false`, and under `when@test` the `async` retry strategy with `delay: 10` (same `max_retries: 3`, `multiplier: 2`; design decision 7); `migrations/Version*.php` via `make migration` → review: table `messenger_messages` with the bridge's columns and indexes, `getDescription()`, reversible `down()`; `make migrate`; `make test-db`. Verify: `make console ARGS='debug:messenger'` lists `ClickRecorded` → `App\Click\Handler\ClickRecordedHandler` (after 2.1) and `make console ARGS='debug:config framework messenger.routing'` shows the route; `make console ARGS='doctrine:migrations:migrate prev --no-interaction'` then `make migrate` both succeed and `make console ARGS="dbal:run-sql \"SELECT column_name FROM information_schema.columns WHERE table_name = 'messenger_messages' ORDER BY ordinal_position\""` lists the bridge's columns (`id`, `body`, `headers`, `queue_name`, `created_at`, `available_at`, `delivered_at`); `rg -n 'auto_setup' config/packages/messenger.yaml` shows `false`; `make console ARGS='debug:config framework messenger'` shows `async` retry `max_retries: 3`, `delay: 1000`, `multiplier: 2` in the dev environment and `delay: 10` with `--env=test`.

## 2. Handler and recorder

- [ ] 2.1 `src/Click/Handler/ClickRecordedHandler.php` (design decision 4: `#[AsMessageHandler]`, DBAL `transactional`, insert first then `UPDATE links SET click_count = click_count + 1`; both constraint exceptions are caught *outside* the transaction: `UniqueConstraintViolationException` → `debug` + return; `ForeignKeyConstraintViolationException` → `info` `{link_id, click_id}` + return normally — never `UnrecoverableMessageHandlingException`, which Messenger would still park; anything else propagates). `tests/Integration/Click/ClickRecordedHandlerTest.php` (kernel + PostgreSQL): one message → one row with the twelve §3.4 columns equal to the message's facts and `click_count` +1; the same message twice → one row, `click_count` +1, no exception, one `debug` record; a message for a deleted link → the handler returns without throwing, no row, one `info` record with `link_id` and `click_id`; an insert that fails for another reason (a click id colliding with a row of *another* link is a duplicate — use instead a `visitor_hash` of 65 characters to violate the column) → the exception propagates and `click_count` is unchanged. Verify: `make test` green; failing inputs recorded in the commit body: swapping the insert and the update lets the deleted-link case increment a vanished row (the FK case then fails); replacing the FK catch by `UnrecoverableMessageHandlingException` makes the Worker test of 3.3 find the message on `failed`.
- [ ] 2.2 `src/Click/Recorder/MessengerClickRecorder.php` (design decision 1: counter only when `maxClicks` is set, counter failure propagates, dispatch failure caught → `error` `{link_id, exception}` → `Allowed`; `VisitorHasher` + `RefererHost` compute the message's `visitorHash`/`refererHost`; `clickId` = UUID v7); `config/services.yaml` binding `ClickRecorderInterface` → `MessengerClickRecorder`; delete `src/Click/Recorder/DbalClickRecorder.php`, `tests/Integration/Click/DbalClickRecorderTest.php`, `tests/Fixture/record-click.php`; `RedirectResolver` docblock. `tests/Unit/Click/MessengerClickRecorderTest.php` (stub counter, spy bus): unlimited link → counter never called, one dispatch whose message carries the facts, the hash and the referer host and none of IP/UA/referer URL; limited link → counter called with `(id, click_count, max)` then dispatch; counter `Exhausted` → `Exhausted`, no dispatch; counter throws → the throwable propagates, no dispatch; bus throws → `Allowed`, one `error` record `{link_id, exception}` without IP/UA. Verify: `make test` green; `rg -n 'DbalClickRecorder|record-click' src tests config docs` empty; `make console ARGS='debug:container App\\Click\\ClickRecorderInterface'` resolves to `MessengerClickRecorder`; failing inputs recorded in the commit body: catching the counter's exception inside the recorder fails the "propagates" case (and, over HTTP, 4.2's 503 case); letting the bus exception propagate fails the `Allowed` case.
- [ ] 2.3 `src/Link/Api/DeleteLinkProcessor.php`: `forget($linkId)` after the flush inside `try/catch (\Throwable)` → `warning` `{link_id, exception}` (design decision 6). `tests/Api/Link/DeleteLinkTest.php` extended: a limited link redirected through once (counter key exists) → `DELETE` 204 and the key is gone; a counter stub that throws → 204, link gone, one `warning` record. Verify: `make test` green; failing input: moving `forget` before the flush and making it throw would abort the deletion — the throwing-stub case fails (commit body).

## 3. Redirect over the queue (Web and integration tests)

- [ ] 3.1 `tests/Web/Redirect/RedirectWebTestCase.php`: `consumeAsync(): int` (a real `Symfony\Component\Messenger\Worker` over the in-memory `messenger.transport.async` with the container's `messenger.default_bus` and `event_dispatcher`, stopped by `StopWorkerOnMessageLimitListener` after the pending count — the `messenger:consume` code path, so retries, parking and acknowledgement are observable; design decision 7), `pendingMessages()`, `failedMessages()` (in-memory `messenger.transport.failed`), `redis()` from `REDIS_URL`, `tearDown` deleting `test:link:*:clicks`; `tests/Fixture/StatementRecorder.php` (DBAL middleware, `when@test`, tag `doctrine.middleware`, static list of executed SQL; design decision 8). Verify the Messenger API first: `rg -n 'public function __construct' -A8 vendor/symfony/messenger/Worker.php`, `ls vendor/symfony/messenger/EventListener/`, `rg -n 'availableAt|DelayStamp' vendor/symfony/messenger/Transport/InMemory/InMemoryTransport.php`, `rg -n 'interface Middleware' vendor/doctrine/dbal/src/Driver/Middleware.php`. Existing tests adapted: `tests/Web/Redirect/ClickLoggingTest.php`, `RedirectMatrixTest.php`, `RoutingRulesTest.php`, `RoutingDegradationTest.php` call `consumeAsync()` before asserting rows or counts; `RedirectDegradationTest.php` replaces the `DbalClickRecorder` stub by a counter stub (limited → 503 + `warning`; unlimited → 302, counter never called) and a throwing transport (302 + `error`, no row); `tests/Unit/Redirect/RedirectResolverTest.php` keeps its recorder stubs (the seam is unchanged). Verify: `make test` green with every previously green scenario intact.
- [ ] 3.2 `tests/Web/Redirect/AsyncClickTest.php`: "One redirect, one click" with `consumeAsync()`; "Before the worker runs" (no row, `clickCount` 0, one pending message); "Non-redirects record nothing" (no message for 404/410); "Click count catches up" through the API before/after consuming; "Message contents" — the pending envelope's serialized body contains the hash and `news.example.org` and neither `203.0.113.7` nor `Probe/1.0` nor the referer URL; "Statement kinds during a redirect" — `StatementRecorder` reset before the request: exactly one `SELECT` on `links`, no `INSERT`/`UPDATE`/`DELETE`, and a counting counter decorator installed through the container reports 0 calls for the unlimited link and 1 for the limited one; "HEAD mirrors GET without recording" — no counter call, no message. Verify: `make test` green; every ADDED/MODIFIED scenario of the three deltas maps to an assertion (table in the commit body); failing input: re-enabling a synchronous write (a test double recorder that inserts) fails the statement-kinds case.
- [ ] 3.3 `tests/Web/Redirect/AsyncClickDegradationTest.php` + `tests/Integration/Click/ClickRetryTest.php`: concurrent first redirects (10 requests through the kernel — sequential in one process is not concurrency; the real race is 1.1's ten-process counter test, and this test asserts the HTTP outcome: 3 × 302, 7 × 410, counter 3, `clickCount` 3 after consuming); seeding from persisted `clickCount` 2 with `maxClicks` 3 (302 then 410, 410); limit change (counter 3, patch `maxClicks` 5 → 302, counter 4); counter throws → limited 503 + `warning` + no pending message, unlimited 302 + one message; transport throws → 302 for both link kinds, no row, `error` record, the limited link's counter kept its increment; delete-then-consume through the Worker: redirect, `DELETE` the link, `consumeAsync()` → no row, the handler ran once, the message is in `getAcknowledged()` and not in `getRejected()`, the in-memory `failed` transport is empty, one `info` record with `link_id` and `click_id`, no failure record; retry then park through the Worker (mandatory — no configuration-only fallback): a message the test dispatches directly with a `visitor_hash` of 65 characters, `consumeAsync()` with a message limit of 4 → the handler ran four times (a counting decorator installed through the container), the `WorkerMessageRetriedEvent`s carried `DelayStamp`s of 10, 20 and 40 ms, the message is on `failed` exactly once with a `RedeliveryStamp` count of 3, no row, `clickCount` unchanged; the same test run with `failure_transport` unset under `when@test` fails (no parked envelope) — the demonstrated failing input; the limit re-enabled after unlimited redirects (counter 3 → no limit → four consumed redirects → `maxClicks` 10 → three 302 with the key lifted to 7 then 10, then 410); dispatch failures then key loss (three 302 with the transport throwing, key deleted, three more 302 and a 410 — the stated overshoot). `tests/Integration/Click/RedisTransportRoundTripTest.php`: `Connection::fromDsn(REDIS_URL, ['stream' => 'messages_test_<random>', 'group' => 'test', 'consumer' => 'phpunit'])`, `add` a serialized `ClickRecorded`, `get`, `ack`, `cleanup`. Verify: `make test` green; failing inputs recorded in the commit body: replacing the FK catch by `UnrecoverableMessageHandlingException` puts the deleted-link message on `failed` (assertion fails); unsetting `failure_transport` in `when@test` fails the retry-then-park case; removing the recorder's dispatch catch fails the transport-down 302 cases; dropping the script's lift fails the re-enabled-limit case.

## 4. Docs

- [ ] 4.1 `docs/how-to/local-development.md`: the Redirect section's baseline note replaced by the async flow (one `SELECT`, one Redis command for limited links, one message; the worker — `make worker` or `docker compose --profile worker up -d`; `make console ARGS='messenger:stats'`; `messenger:failed:show` / `messenger:failed:retry`; `clickCount` eventually consistent; Redis down → 503 for limited links only; transport down → 302 without a record; the counter key `link:{id}:clicks`, its seeding and its deletion with the link); the Daily table rows and the Troubleshooting entry that referred to "until `add-async-click-logging`" updated. Verify: re-read whole after the last edit; every documented command run in its exact form against the dev stack (a redirect with the worker running lands in `clicks`; `messenger:stats` shows the stream; a parked message shown and retried) and pasted into the commit body.
- [ ] 4.2 `docs/explanation/requirements.md`: FR-CLK-1 and FR-CLK-2 reworded — the message carries the finished facts, the hash and the referer host (derived in the request, where routing already needs them) and never the raw IP or user agent; the handler persists and increments; FR-RED-3's guarantee boundary restated — the script lifts the key to the persisted count and never lowers it, and the overshoot is bounded by the accepted redirects not yet persisted at seeding (queued, dispatch-failed, parked, crashed before dispatch), compounding with repeated key loss; FR-CLK-5's discard mechanism restated as an acknowledged return, not an unrecoverable exception; re-read FR-RED-2/4, FR-CLK-3/4, §7 row 7 and `openspec/ROADMAP.md` row 7 against the delivered scope. Verify: `rg -n 'add-async-click-logging|FR-CLK-1|FR-CLK-2' openspec/ROADMAP.md docs/explanation/requirements.md` shows the rows and they match the specs.

## 5. Wrap-up

- [ ] 5.1 `make check` green (cs, stan level 8, all suites); commits per block (`feat(click):` 1.x and 2.1–2.2, `feat(link):` 2.3, `test(redirect):` 3.x, `docs:` 4.x) with the agent trailer; every guard's commit body names its demonstrated failing input; commit bodies and `handoff.md` flag the security-sensitive parts (Messenger retry/failure handling, the counter as the limit authority, the delete-then-consume path, the migration). Verify: `git log --oneline main..HEAD`.
- [ ] 5.2 Green Actions run on the exact branch head before Gate 2: the user pushes change/add-async-click-logging; the executor queries `https://api.github.com/repos/Azazu/linkboard/actions/runs?branch=change/add-async-click-logging` for the head SHA until `status` is `completed` and `conclusion` is `success`; URL and SHA recorded in `handoff.md`.
- [ ] 5.3 `openspec validate add-async-click-logging --strict` and `scripts/pregate-verify.sh gate2 add-async-click-logging` pass; `scripts/gate-run.sh add-async-click-logging 2 full`. Verify: no FAIL line.

## Post-merge acceptance (not a Gate 2 task)

- After the merge: the user pushes `main`; the executor checks the `main` run via the API before proposing the archive; `/opsx:archive` syncs `redirect`, `click-logging`, `links`.
