# Handoff — add-api-keys-and-rate-limiting

**Updated:** 2026-09-12 · claude
**State:** proposing
**Branch:** change/add-api-keys-and-rate-limiting

## Done this session
- Branch `change/add-api-keys-and-rate-limiting` created from `main` (`64ac596`, after the archive of `add-qr-codes`); change scaffolded with `openspec new change`.

## Next step
`/opsx:propose add-api-keys-and-rate-limiting` — roadmap row 10: hashed API keys (plaintext once), access-token authenticator, per-key/per-user API limit with rate-limit headers, trusted-proxy handling; the prod deep health probe becomes available to a valid admin API key. Tier `high` (authentication, API keys, rate limits — AGENTS.md).

## Blockers
None.
