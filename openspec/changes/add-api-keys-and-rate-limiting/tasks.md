# Tasks — add-api-keys-and-rate-limiting

High tier (authentication, API keys, firewall, rate limits, a migration — AGENTS.md, NFR-SEC-7): Gate 1 on the artifacts and Gate 2 on the code. Work on the change branch; `scripts/workflow-verify.sh apply add-api-keys-and-rate-limiting` first. Every task touching authentication or authorization is flagged in its commit body.

## 0. Gate 1 (high tier)

- [ ] 0.1 `scripts/pregate-verify.sh gate1 add-api-keys-and-rate-limiting` passes and `scripts/gate-run.sh add-api-keys-and-rate-limiting 1 full` is run on the completed artifacts (proposal with tier and Non-goals, specs `api-keys` + three modified capabilities, design with the applicability table, tasks); findings via `/workflow:fix-findings`, confirmation with `scripts/gate-run.sh add-api-keys-and-rate-limiting 1 confirm <round>`. Verify: review.md carries a Gate 1 round bound to the requested commit; the merge verifier checks that the last Gate 1 decision reads approved/confirmed.

## 1. Schema and domain

- [ ] 1.1 `src/Auth/Entity/ApiKey.php` (design decision 3: Uuid v7 id, owner, name, keyHash, prefix, expiresAt, revokedAt, lastUsedAt, createdAt; `revoke(now)` idempotent, `isActive(now)`), `src/Auth/ApiKeyRepositoryInterface.php`, `src/Auth/Repository/DoctrineApiKeyRepository.php` (`add`, `findById`, `findByIdAndOwner`, `findActiveByHash(hash, now)`, `listByOwner`, `countByOwner`, `countActiveByOwner(owner, now)`, `lockOwner(owner)` = `SELECT id FROM users WHERE id = :id FOR UPDATE` inside the caller's transaction, `touchLastUsed(id, now, threshold)` as one DBAL `UPDATE … WHERE last_used_at IS NULL OR last_used_at < :threshold`); `make migration` → read and, where needed, hand-correct the generated class into the §3.2 shape (PK, FK `user_id` `ON DELETE CASCADE` + index, `UNIQUE (key_hash)`, index `(user_id, created_at)`, reversible `down()`), then `make migrate` and `make test-db`. `tests/Unit/Auth/ApiKeyTest.php` (`isActive` matrix: fresh, revoked, expired, expiring later; `revoke` twice keeps the first timestamp). `tests/Integration/Auth/DoctrineApiKeyRepositoryTest.php`: `findActiveByHash` returns a fresh key and null for revoked, expired, unknown; `touchLastUsed` sets once and is a no-op within 60 s, updates after; `countActiveByOwner` excludes revoked/expired; deleting the user removes the keys (FK); `doctrine:migrations:migrate prev` then `migrate` round-trips in the test database. Verify: `make console ARGS='doctrine:schema:validate'` reports the mapping and the database in sync; `docker compose exec -T php vendor/bin/phpunit --filter 'ApiKeyTest|DoctrineApiKeyRepositoryTest'` green; failing inputs in the commit body: dropping `revoked_at IS NULL` from `findActiveByHash` fails the revoked case; dropping the threshold predicate from `touchLastUsed` fails the once-per-minute case.
- [ ] 1.2 `src/Auth/ApiKey/ApiKeyGenerator.php` (design decision 2: `lb_` + 40 characters of `[A-Za-z0-9]` from `random_int`, `GeneratedKey{plaintext, hash, prefix}`) and `tests/Unit/Auth/ApiKeyGeneratorTest.php`: 200 keys match `^lb_[A-Za-z0-9]{40}$` and are distinct, `hash` equals `hash('sha256', plaintext)`, `prefix` is the first 8 characters. Verify: the test is green; failing input in the commit body: a 39-character body fails the shape case.

## 2. Authentication with a key

- [ ] 2.1 `src/Auth/ApiKey/ApiKeyHeaderExtractor.php` (`AccessTokenExtractorInterface`, regex `^Bearer (lb_[A-Za-z0-9]{40})$`), `src/Auth/ApiKey/JwtExtractorIgnoringApiKeys.php` (`#[AsDecorator('lexik_jwt_authentication.extractor.chain_extractor')]`, returns false for a token starting with `lb_`), `src/Auth/ApiKey/ApiKeyTokenHandler.php` (design decision 4: hash → `findActiveByHash` with the clock → `BadCredentialsException` or `touchLastUsed` + `UserBadge` with a loader returning the owner and the badge attribute `api_key_id`; `#[\SensitiveParameter]`), `src/Auth/ApiKey/ApiKeyAuthenticationFailureHandler.php` (`AccountStatusException` → 403 `blocked`, else 401 `Invalid API key.` problem details); `config/packages/security.yaml`: `access_token` on the `api` firewall with those three services, `jwt` and `entry_point: jwt` unchanged. Before writing, re-read `AccessTokenAuthenticator::supports/createToken/onAuthenticationFailure` and `AuthenticatorManager::executeAuthenticators` in `vendor/symfony/security-http` and the Lexik extractor service id in `vendor/lexik/jwt-authentication-bundle/Resources/config/token_extractor.php` (design Context). `tests/Unit/Auth/ApiKeyExtractorsTest.php`: header extractor accepts the shape and rejects a JWT-looking token, a 39/41-character body and `bearer` in lowercase; the decorator returns false for `Bearer lb_…` and passes a JWT through to the inner extractor. Verify: `make console ARGS='debug:firewall api'` lists both authenticators; `docker compose exec -T php vendor/bin/phpunit --testsuite Unit --filter ApiKey` green.
- [ ] 2.2 `tests/Api/Auth/ApiKeyAuthTest.php` (`WebTestCase` + JWT helper as in `tests/Api/Link/LinkApiTestCase.php`; keys created through the API of task 3.1 or, until it exists, through the repository): a key on `GET /api/v1/me` → 200 with the owner's `email` and `lastUsedAt` set; a second request within the minute leaves `lastUsedAt` unchanged; revoked key 401, expired key 401, unknown key (valid shape) 401, malformed `Bearer lb_short` 401 — each `application/problem+json`; the owner blocked → 403 `detail` `blocked`, unblocked → 200 again; a JWT still works on `/me`; a stranger's key on a foreign `GET /links/{id}` → 403 (voters apply). Verify: `docker compose exec -T php vendor/bin/phpunit --testsuite Api --filter ApiKeyAuth` green; failing inputs in the commit body: removing the decorator makes the key request 401 (Lexik claims the header); replacing `findActiveByHash` by a lookup ignoring `expires_at` makes the expired case 200; removing the failure handler's `AccountStatusException` branch turns the blocked case into 401.

## 3. Key management API

- [ ] 3.1 `src/Auth/Api/ApiKeys/` — `ApiKeyOutput` (resource `ApiKey`: `GetCollection /api-keys` with `OwnApiKeysProvider`, `Delete /api-keys/{id}` with `OwnApiKeyItemProvider` (by id **and** owner → 404 otherwise) and `RevokeApiKeyProcessor` (idempotent 204), `Post /api-keys` with `CreateApiKeyInput` (`name` trimmed, `NotBlank`, `Length(max: 64)`; `expiresAt` nullable, `GreaterThan('now')`), `CreateApiKeyProcessor` (one transaction: `lockOwner`, count active → `ConflictHttpException` at 10, else generate, persist, commit; return `CreatedApiKeyOutput` with `key` — design decision 5), `status: 201`), `src/Auth/Security/ApiKeyVoter.php` (`API_KEY_VIEW`/`API_KEY_REVOKE`, owner only, no admin override) applied in the revoke processor; `tests/Unit/Auth/ApiKeyVoterTest.php` (owner true, stranger false, admin-stranger false). `tests/Api/Auth/ApiKeysTest.php`: 201 with `key` matching the shape and `prefix` = first 8; the listing carries no `key`; `SELECT key_hash, prefix FROM api_keys` equals sha256 of the plaintext / its prefix and no column contains the plaintext; 422 matrix (`name` empty, 65 characters, `expiresAt` in the past → one violation each on the named field); ten keys then 409 whose `detail` mentions 10, revoke one → 201; users A and B — A lists only A's keys; revoke twice 204/204 with `revokedAt` set, B's key by id 404, an admin deleting A's key 404; `GET /api/docs.json` lists the three operations. `tests/Integration/Auth/ApiKeyCreationConcurrencyTest.php` with `tests/Fixture/create-api-key.php` (the `RateLimiterConcurrencyTest` pattern): a user with 9 committed active keys, 12 processes each creating one key through the processor → exactly one success, eleven conflicts, ten active keys in `api_keys`; rows removed afterwards. Verify: `docker compose exec -T php vendor/bin/phpunit --filter 'ApiKeysTest|ApiKeyVoterTest|ApiKeyCreationConcurrency'` green; failing inputs in the commit body: removing the owner scope from `OwnApiKeyItemProvider` turns the foreign `DELETE` into 204; removing the active count turns the 11th `POST` into 201; removing `lockOwner()` lets the concurrency test end with more than ten keys in repeated runs (recorded, not asserted — the race is real but not deterministic); returning the entity instead of `CreatedApiKeyOutput` leaks no key — the plaintext-once case fails on the missing `key`.

## 4. Per-identity rate limit

- [ ] 4.1 `config/packages/framework.yaml`: limiter `api_identity` (sliding window, `%env(int:RATE_LIMIT_API_PER_KEY)%`, 1 minute, `cache.rate_limiter`, `lock.factory`; `when@test` storage `app.rate_limiter.test_storage`), `.env`: `RATE_LIMIT_API_PER_KEY=600` with a comment; `src/Auth/RateLimit/ApiRateLimitListener.php` (design decision 6: `LoginSuccessEvent` on firewall `api`, main request, path not `^/api/v1/auth/`; key `key:<id>` from the passport's badge attribute or `user:<id>`; `try/catch (\Throwable)` → `warning` + proceed; rejected → `setResponse(429 problem details)` with `Retry-After`, `X-RateLimit-Limit`, `X-RateLimit-Remaining: 0` — the chain stops before access control; accepted → stash the `RateLimit`) and `src/Auth/RateLimit/ApiRateLimitHeaders.php` (`kernel.response`: the two headers on any response of a limited request). Before writing, re-read `AuthenticatorManager::handleAuthenticationSuccess` and the firewall listener order in `SecurityExtension` (design Context). `tests/Api/Auth/ApiRateLimitTest.php`: first key request → `X-RateLimit-Limit: 600`, `X-RateLimit-Remaining: 599`; the same for a JWT of the same user (its own window); exhaustion through a test-only lower limit (the `api_identity` limiter replaced through the test container by a factory with limit 3) → the 4th request 429 `application/problem+json` with `Retry-After` ≥ 1 and `X-RateLimit-Remaining: 0`, and a `POST /links` over the limit creates no link; key B and the JWT still 200 after key A is exhausted; an ordinary user's key and JWT on `GET /api/v1/admin/stats/summary` → 403 with both headers and a decreasing `X-RateLimit-Remaining`, then 429 at exhaustion (with the test-only lower limit); `POST /api/v1/auth/token` and an anonymous `GET /api/v1/me` (401) carry no `X-RateLimit-*`; storage throwing (a factory whose storage throws installed through the test container) → 200 without the headers and a `warning` naming the failure class. `tests/Integration/Auth/RateLimiterStorageTest.php` gains the `api_identity` Redis path like `auth_ip_redis` (design Risks). Verify: `docker compose exec -T php vendor/bin/phpunit --filter 'ApiRateLimit|RateLimiterStorage'` green; failing inputs in the commit body: removing the `try/catch` fails the storage case with 500; removing the `X-RateLimit-Remaining` header fails the header case; keying by user for key-authenticated requests fails the separate-windows case; moving the consumption to a `kernel.request` listener at priority 7 fails the role-denied case (403 without headers, never 429).

## 5. Docs

- [ ] 5.1 `docs/how-to/local-development.md` — in "Accounts and the API": create a key (`curl -X POST …/api-keys`), call the API with `Authorization: Bearer lb_…`, list and revoke, the plaintext-once warning, `X-RateLimit-*` and the 429 with `Retry-After`, `RATE_LIMIT_API_PER_KEY`, fail-open; `docs/explanation/requirements.md` FR-KEY-1…4 refined (409 on the 11th key, headers on limited responses only, fail-open, once-per-minute `lastUsedAt`; FR-KEY-5 unchanged) and §7: row 10 loses the probe clause, a row for `authorize-deep-probe-by-api-key` follows it; `.env` comment for the new variable. Verify: every documented command run in its exact form on the dev stack (create, use, list, revoke, a 401 after revoke, the two headers visible) with the key never pasted into any file, ticket or chat — only its `prefix`; both files re-read whole after the last edit; `rg -n 'api-keys' docs openspec/changes/add-api-keys-and-rate-limiting` shows how-to, brief, proposal, specs and design agreeing.

## 6. Wrap-up

- [ ] 6.1 `make check` green (cs, stan level 8, all suites); commits per block (`feat(auth):` 1.x/2.x/3.x/4.x, `docs:` 5.1) with the agent trailer; every guard's commit body names its demonstrated failing input; the authentication, authorization and rate-limit parts flagged in the commit bodies and in `handoff.md`. Verify: `git log --oneline main..HEAD`; `openspec validate add-api-keys-and-rate-limiting --strict` passes.
- [ ] 6.2 Green Actions run on the exact branch head before Gate 2: the user pushes the change branch; the executor queries `https://api.github.com/repos/Azazu/linkboard/actions/runs?branch=change/add-api-keys-and-rate-limiting` until the run for `git rev-parse HEAD` is `completed` / `success`; URL and SHA recorded in `handoff.md`. Verify: the run's `head_sha` equals the branch head.
- [ ] 6.3 `scripts/pregate-verify.sh gate2 add-api-keys-and-rate-limiting` passes and `scripts/gate-run.sh add-api-keys-and-rate-limiting 2 full` is run; findings via `/workflow:fix-findings`, confirmation with `scripts/gate-run.sh add-api-keys-and-rate-limiting 2 confirm <round>`. Verify: review.md carries a Gate 2 round bound to the requested commit; the merge verifier checks that the last Gate 2 decision reads approved/confirmed and is fresh.

## Post-merge acceptance (not a Gate 2 task)

- After the merge: the user pushes `main`; the executor checks the `main` run via the API before proposing the archive; `/opsx:archive` syncs `api-keys` (new) and the three modified requirements (`authentication`, `user-accounts`, `health-check` — reference wording only) and removes row 10 from `openspec/ROADMAP.md` (the follow-up row `authorize-deep-probe-by-api-key` stays).
