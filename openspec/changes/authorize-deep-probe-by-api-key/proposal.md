# Proposal — authorize-deep-probe-by-api-key

**Risk-Tier:** high

Tier rationale: an authorization boundary on a production endpoint that today is unconditionally refused, a credential check that runs outside the firewall, and a monitoring surface whose failure modes (database down, stalled, Redis down) decide what an unauthenticated caller can learn — AGENTS.md and NFR-SEC-7 put this at `high`. Gate 1 on the artifacts and Gate 2 on the code, a demonstrated failing input for every guard, the applicability table in `design.md`, a green branch run before Gate 2. No new package and no new extension, but one promotion: the lookup's deadline is enforced by running it in a child process that the application kills when the deadline passes, so `symfony/process` moves from `require-dev` (where a linter happened to pull it in) to `require` — a production install must ship it (Gate 2 round 1, finding 1) — the only mechanism in PHP that bounds a PostgreSQL client *including its cleanup*: pdo_pgsql has no read deadline, and ext-pgsql's cancellation and close block on the same lost response they were meant to escape.

## Why

`GET /health?deep=1` is refused with 404 in `prod` because no credential fit for a monitor existed. `add-api-keys-and-rate-limiting` (archived 2026-09-12) shipped the keys and left this authorization to a change of its own after two Gate 1 confirmations showed the hard part: a key lookup that must be *bounded* (a monitor cannot wait on a hung database), *fail-closed* (an unverifiable key opens nothing), and still useful — a deep probe that answers 404 whenever the database is down cannot tell the monitor the one thing it exists to tell. This change designs exactly that.

## What Changes

1. **The `prod` deep probe runs for a valid admin API key** (health-check spec): `GET /health?deep=1` with `Authorization: Bearer lb_…` runs the probe when the key resolves — hash lookup, not revoked, not expired — to an unblocked `ROLE_ADMIN` user. Everything else — no header, a JWT, a session, an unknown, revoked or expired key, a non-admin's or a blocked admin's key — keeps today's 404 problem details (`Cache-Control: no-store`), identical for every reason. Sessions and JWTs never authorize the probe. The check never records a use of the key (`last_used_at` untouched) and never logs the key.
2. **A deadline the operating system enforces**: the database lookup runs in a small standalone PHP script (no kernel) started as a child process with a 2.5-second timeout — the key hash on its stdin, never on a command line — and the parent kills it at the deadline, so a connection that never completes, a lookup that stalls, a response the network loses, or a client stuck in its own teardown all end when the process ends; a server-side `statement_timeout` is only a belt. The Redis phase has a reserved 1-second allowance for its fixed sequence of four operations, each with its own 0.25-second **deadline** — measured from the moment the operation starts, so a server that dribbles its reply out byte by byte cannot outlast it the way it outlasts a per-read timeout (Gate 2 round 1, finding 2). The command after the lookup is therefore always funded. A refusal for a verification that could not complete leaves within 5 seconds of the request in every such mode, is the same 404 as for a bad key, and logs a `warning` naming the failure class. Host-name resolution stays outside the budget, as for the probe's own checks.
3. **Authorization survives a database outage — the case the probe exists for**: a verified answer is remembered in Redis for 300 seconds counted from the verification, together with the key's expiry; a denied answer replaces a per-key random denial token and drops the memory; a verification is stored only if the token is still the one read before the request's lookup began — a random token never repeats, so this holds across the token's own expiry — and a verification that raced a revocation is therefore refused by construction, in the order Redis serialises the requests, with no clock or snapshot comparison. When the database phase is unavailable (unreachable, refused, timed out, response lost — not "no such active admin key"), a remembered, unexpired verification authorizes the probe, which then runs and reports its own checks — `database: fail` for a refused database, `ok` for one that still answers `SELECT 1`. A denied answer observed while Redis was reachable takes effect on the next request and cannot be undone by a later outage. The admitted case: when Redis was unreachable at the moment of the denial, the denial is not recorded and the earlier verification may authorize the probe during a database outage for the remainder of its 300 seconds — stated as the bound, logged as a `warning`. Redis down as well → nothing remembered, fail-closed 404.
4. **Tests that reproduce every failure mode end to end** through the `prod` kernel, with the admin and keys committed in the `app` database: the refusal matrix; the admin key → probe body; a closed port → 404 fast; a connection that never completes (black-hole socket) → 404 at the connect allowance; a stalled lookup (`LOCK TABLE api_keys IN ACCESS EXCLUSIVE MODE` held by the test) → 404 at the lookup allowance; a **completed handshake followed by a lost response** (a TCP proxy that swallows the server's reply) → 404 within 5 s, the child killed and gone — the case no in-process client passes; **delayed connection plus stalled statement** through the same proxy → within 5 s; Redis that accepts and never answers, and a Redis whose every operation is slow (a fake RESP server with a password and a per-response delay) → bounded; a remembered monitor during a refused database → 503 `database: fail`, and during a locked table → the probe runs and reports `ok`; a revoked key is refused at once and stays refused through a later outage; the verification-vs-revocation interleaving reproduced deterministically against the real database and Redis, with the ordering established by the mechanism, not supplied by the test. CI migrates the `app` database (`make migrate EXEC=`).
5. **Docs**: the how-to's health paragraph (the monitor's admin key, alert on any non-200, what a 404 means while the database is down, the 300-second memory), the brief's health rows (§4 table, the deep-probe sentence) and §7 row 10a refined; `.env` unchanged — the budget and the TTL are constants of the design, not deployment knobs.

## Capabilities

### New Capabilities

_None._

### Modified Capabilities

- `health-check`: "Deep dependency probe" — the `prod` refusal gets its exception (a valid admin API key), the bounded total deadline, the fail-closed rule with the identical 404, the Redis memory that lets the probe report a database outage to a recently verified monitor, and the no-`last_used_at`/no-logging rules.

## Non-goals

- Any change to `/health` liveness (still no authentication, no dependency touched), to `/health` outside `prod` (still open), or to the OpenAPI document (`/health` stays outside the API contour).
- Authorizing the probe by a JWT, a session, a non-admin key, or a dedicated "monitor secret" in the environment (a secret in env is what API keys replaced).
- Per-key or per-IP rate limiting of `/health` (a monitor polls; the probe is cheap and read-only; the existing `redirect_ip`/`auth_ip` limits do not apply and none is added).
- Caching the probe's *result*, distinguishing "key refused" from "database down" in the HTTP response (the 404 stays identical; the distinction lives in the `warning`), or a status code other than 404 for refusals.
- Changing the probe's own dependency checks or timeouts, the API firewall, the API-key resource, or the `api_identity` limiter.

## Impact

- New: `bin/probe-key-lookup` (the standalone lookup script), `src/Shared/Health/KeyLookupProcess.php` (runs it with a kill deadline, parses `V`/`D`/`U`) with `KeyLookupInterface`, `LookupOutcome` and `LookupStatus`, `src/Shared/Health/BoundedRedisConnection.php` (a small RESP client over a raw socket whose every operation is bounded by a wall-clock deadline, used by the memory and by the probe's own Redis check) with `RedisOperationFailed`, `src/Shared/Health/ProbeMemory.php` (the token-ordered Redis memory) with `ProbeMemoryInterface` and `MemoryUnavailable`, `src/Shared/Health/ProbeAuthorizer.php`; tests under `tests/Unit/Shared/Health/`, `tests/Integration/Shared/Health/` and `tests/Api/HealthDeepProdTest.php`; fixtures `tests/Fixture/black-hole-socket.php`, `tcp-proxy.php` (delay and lost-response modes), `fake-redis-server.php`, `full-backlog-listener.php`, `prod-health-request.php`; test support `tests/Support/` (the prod-kernel request and its log records, committed rows, the fixture processes, the running-process list, the spy logger, the interleaving wrapper).
- Modified: `composer.json` / `composer.lock` (`symfony/process` promoted to `require`), `src/Shared/Health/HealthController.php` (the `prod` branch asks the authorizer), `src/Shared/Health/HealthProbe.php` (its Redis check uses the shared bounded connection — same `PING`, same 2-second bound, now a deadline instead of a per-read timeout; its PDO check is unchanged), `src/Auth/ApiKey/ApiKeyHeaderExtractor.php` (the header match exposed as a static `keyFromHeader()` the request-less authorizer can use; the regex and the authenticator path unchanged), `config/packages/monolog.yaml` (a `health` channel with an always-on `warning` stream in `prod` — the main handler is `fingers_crossed` and would drop a warning whose response is a 404), `.github/workflows` (`APP_ENV=dev make migrate EXEC=`), `docs/how-to/local-development.md`, `docs/explanation/requirements.md` (health rows, §7 row 10a), `openspec/ROADMAP.md` (row 10a removed at archive time).
- Unchanged: the firewall, `ApiKeyTokenHandler` and the API-key resource (the authorizer reuses the extractor's regex and `ApiKeyGenerator::hash()` only), the liveness probe, the probe's own dependency checks (their lost-response gap is recorded in the design's Risks as a follow-up).

## User decisions

- **2026-09-12 — third confirmation authorised after two failed ones** (Gate 1 round 1, findings 1–2): the user chose to apply the two remaining fixes — a complete Redis operation budget (four operations, 0.25 s each, the post-lookup command always funded) and a random, non-recurring denial token with an absolute verification-age bound — and to run a third confirmation, rather than dropping the Redis memory (a probe blind to database outages) or splitting it out. Recorded per AGENTS.md ("after two failed confirmations … ask the user to arbitrate").
- **2026-09-12 — fourth confirmation authorised after the third** (Gate 1 round 1, finding 1): confirmation 3 accepted the Redis budget itself and rejected only the fixture mechanics (command index off by one because a TCP connect is no RESP command; a delayed `accept` stalls the first read, not the connect; no assertion pinned the operation that timed out). The user chose to correct the fixtures — command index 3, a full-backlog listener for a stalled connect, `MemoryUnavailable` naming its operation plus a fixture command log so tests assert the phase — and to run a fourth confirmation, rather than waiving the finding on a security change.
