# Tasks — authorize-deep-probe-by-api-key

High tier (an authorization boundary on a production endpoint outside the firewall — AGENTS.md, NFR-SEC-7): Gate 1 on the artifacts and Gate 2 on the code. Work on the change branch; `scripts/workflow-verify.sh apply authorize-deep-probe-by-api-key` first. Every task touching authorization is flagged in its commit body.

## 0. Gate 1 (high tier)

- [ ] 0.1 `scripts/pregate-verify.sh gate1 authorize-deep-probe-by-api-key` passes and `scripts/gate-run.sh authorize-deep-probe-by-api-key 1 full` is run on the completed artifacts (proposal with tier and Non-goals, the health-check delta, design with the applicability table, tasks); findings via `/workflow:fix-findings`, confirmation with `scripts/gate-run.sh authorize-deep-probe-by-api-key 1 confirm <round>`. Verify: review.md carries a Gate 1 round bound to the requested commit; the merge verifier checks that the last Gate 1 decision reads approved/confirmed.

## 1. The extension, the deadline and the two clients

- [ ] 1.1 `.docker/php/Dockerfile`: `pgsql` added to `install-php-extensions`; `.github/workflows/*.yml`: `pgsql` added to the `setup-php` extensions and `make migrate EXEC=` before `make test-db EXEC=` (design decision 8). Verify: `docker compose build php` then `docker compose exec -T php php -m | grep -x pgsql` prints `pgsql` and `php -r 'var_dump(function_exists("pg_connect_poll"));'` prints `bool(true)`; `rg -n 'pgsql' .docker/php/Dockerfile .github/workflows` shows both; the branch run of task 5.2 passes with `HealthDeepProdTest` executed.
- [ ] 1.2 `src/Shared/Health/Deadline.php` (design decision 2: `start(float $seconds)` on `hrtime`, `remaining(): float` ≥ 0, `expired(): bool`), `src/Shared/Health/AsyncPostgresQuery.php` (design decision 3: `selectOne(string $url, string $sql, array $params, Deadline $connect, Deadline $query): array|null` over `pg_connect(…, PGSQL_CONNECT_ASYNC | PGSQL_CONNECT_FORCE_NEW)` + `pg_connect_poll` on `stream_select(pg_socket)`, `pg_send_query_params`, `stream_select` + `pg_consume_input` + `pg_connection_busy`, `pg_get_result`; a missed deadline → `pg_cancel_query` + `pg_close` + `DependencyUnavailable` exception; the DSN built from `DATABASE_URL` with `options='-c statement_timeout=<ms>'`), `src/Shared/Health/BoundedRedisCommands.php` (`open(string $url, float $perOperationTimeout): \Redis` — the probe's connect/AUTH factored out, `OPT_READ_TIMEOUT` set), `src/Shared/Health/HealthProbe.php` `checkRedis()` delegates to the factory (2.0 s), the PDO check unchanged. `tests/Unit/Shared/Health/DeadlineTest.php`. `tests/Integration/Shared/Health/AsyncPostgresQueryTest.php` (real PostgreSQL + the fixtures of task 3.1): a `SELECT 1` answers within milliseconds; a closed port → unavailable at once; the black-hole socket → unavailable at the connect allowance (1.0 s ± 0.3); `LOCK TABLE api_keys` held by the test + a lookup → unavailable at the query allowance; the proxy with `--stall-after-responses=1` (handshake completes, the query's response is swallowed) → unavailable at the query allowance — the case the synchronous client cannot pass; `BoundedRedisCommands::open` against the black hole → the `AUTH`/`PING` read times out at the per-operation timeout. Verify: `docker compose exec -T php vendor/bin/phpunit --filter 'DeadlineTest|AsyncPostgresQueryTest|HealthProbeTest'` green; failing inputs in the commit body: the query wait's `stream_select` timeout replaced by a blocking wait → the lost-response case hangs past its bound and fails; the connect allowance not applied → the black-hole case runs to libpq's own timeout.

## 2. The memory and the authorizer

- [ ] 2.1 `src/Shared/Health/ProbeMemory.php` (design decision 4: key `probe-auth:<sha256(key hash)>`, value `{"s":"v"|"d","at":<µs>}`, `remember(hash, at)` / `forget(hash, at)` through one Lua CAS script (`EVAL`: write only if absent or stored `at` < new `at`; `PEXPIRE` 300 s), `consult(hash, now): bool` = `GET` → `s = v` and `now − at ≤ 300 s`; a Redis failure → `MemoryUnavailable`), `src/Shared/Health/ProbeAuthorizer.php` (design decisions 1–2, 5: regex → hash → database phase 2.5 s with connect ≤ 1.0 s → verified/denied/unavailable → remember/forget/consult under the reserved 1.0-second Redis allowance with `1.0/3` per operation; `warning` `{exception}` for either phase, never the key), `src/Shared/Health/HealthController.php`: in `prod`, run the probe when `authorize()` is true, else the existing 404 body. `tests/Unit/Shared/Health/ProbeAuthorizerTest.php` with fakes for the two phases: verified → true + remember; denied → false + forget; unavailable + fresh `v` → true; unavailable + `v` older than 300 s → false; unavailable + `d` → false; unavailable + nothing → false; unavailable + memory failing → false; a non-key header → false with no I/O; a database phase that used its whole allowance still consults the memory. `tests/Integration/Shared/Health/ProbeMemoryTest.php` (real Redis): `remember` then `forget` with a later `at` → `d`; `forget(at=t₂)` then `remember(at=t₁<t₂)` → still `d` (the CAS refuses the older write); `remember` with a newer `at` after `d` → `v`; TTL ≤ 300 s; the black-hole Redis → `MemoryUnavailable` within the per-operation timeout. `tests/Integration/Shared/Health/ProbeAuthorizerTest.php` (real PostgreSQL and Redis, spy logger, rows through `ApiKeyFactory`/`UserFactory`): admin key → true and `v` with `at` ≈ now; non-admin, blocked, revoked, expired, unknown → false and `d`; closed port → false within 3 s with the "database unavailable" warning; `LOCK TABLE` → false within 3 s; a remembered `v` + closed port → true; a `d` + closed port → false; Redis unreachable during a denied answer → false, the "memory unavailable" warning, and the earlier `v` still present (the admitted case, asserted as such). Verify: `docker compose exec -T php vendor/bin/phpunit --filter 'ProbeMemory|ProbeAuthorizer'` green; failing inputs in the commit body: the `roles` check removed → the non-admin case is true; the `is_blocked` check removed → the blocked case is true; the Lua CAS replaced by a plain `SET` → the older-write case ends in `v`; the `at` freshness check removed → a 301-second-old `v` authorizes; the reserved Redis allowance replaced by "skip when the database phase used the budget" → the remembered-monitor case is false.

## 3. Production end-to-end tests

- [ ] 3.1 `tests/Fixture/black-hole-socket.php` (binds 127.0.0.1:0, prints the port, accepts, never writes) and `tests/Fixture/tcp-proxy.php` (binds 127.0.0.1:0, prints the port; `--upstream=host:port`, `--delay=<s>` before connecting upstream, `--stall-after-responses=<n>` forwards upstream→client for the first n upstream messages and swallows the rest while still forwarding client→upstream; `stream_select` loop), both run with `Process` and stopped in `finally` (design decision 7). `tests/Api/HealthDeepProdTest.php` (prod kernel as `HealthTest::bootProdKernel`; admin user and keys inserted into the `app` database through a bounded client, committed, removed in `finally`; `DATABASE_URL`/`REDIS_URL` set in `$_SERVER`/`$_ENV` for the kernel under test and restored): refusal matrix — no header, admin JWT, non-admin key, revoked admin key, expired admin key, blocked admin's key → identical 404 bodies, `no-store`, `last_used_at` unchanged; admin key → probe body (200/503 with `checks`); closed port → 404 under 1 s; black-hole connect → 404 in 1–2 s; `LOCK TABLE` → 404 in 2.5–4 s; proxy `--delay=0.7` + `LOCK TABLE` → 404 under 5 s; proxy `--stall-after-responses=1` (lost response) → 404 under 5 s; Redis black hole + closed database port → 404 under 5 s; admin key verified against the real database, then the closed port → 503 with `checks.database` `fail`; the same, then `LOCK TABLE` instead → the probe runs (its own report: 200 expected, `database` `ok`); the key revoked while the database is up → 404, then the closed port → still 404 (the `d` marker). Verify: `docker compose exec -T php vendor/bin/phpunit --filter HealthDeepProd` green; failing inputs in the commit body: the query wait's deadline removed → the lost-response case exceeds 5 s; `remember` removed → the remembered-monitor case answers 404; the 404 body built differently on a lookup failure → the identical-body assertion fails.

## 4. Docs

- [ ] 4.1 `docs/how-to/local-development.md` — the Health paragraph: in `prod` the deep probe needs `Authorization: Bearer <admin API key>`; a monitor alerts on any non-200; a 404 with a valid key means the key could not be verified — the database was unreachable and no verification from the last 5 minutes was remembered; a monitor that polled before the outage sees the probe's own report instead; the monitor's timeout should allow 10 s; IP literals or a local resolver for `DATABASE_URL`/`REDIS_URL` where the resolver may stall; the `pgsql` extension requirement; `docs/explanation/requirements.md`: the §4 table row for `/health` and the deep-probe sentence in §2.10 refined (admin key, budget, memory), §7 row 10a kept in sync with the delivered scope. Verify: the documented probe request run on the dev stack in its exact form (dev is open, so the request succeeds; the `prod` behaviour is the test suite's) and a curl against the prod kernel is not attempted outside tests; both files re-read whole after the last edit; `rg -n 'deep=1' docs openspec/changes/authorize-deep-probe-by-api-key` shows how-to, brief, proposal, spec and design agreeing.

## 5. Wrap-up

- [ ] 5.1 `make check` green (cs, stan level 8, all suites); commits per block (`build:` 1.1, `feat(health):` 1.2 and 2.1, `test(health):` 3.1, `docs:` 4.1) with the agent trailer; every guard's commit body names its demonstrated failing input; the authorization parts flagged in the commit bodies and in `handoff.md`. Verify: `git log --oneline main..HEAD`; `openspec validate authorize-deep-probe-by-api-key --strict` passes.
- [ ] 5.2 Green Actions run on the exact branch head before Gate 2: the user pushes the change branch; the executor queries `https://api.github.com/repos/Azazu/linkboard/actions/runs?branch=change/authorize-deep-probe-by-api-key` until the run for `git rev-parse HEAD` is `completed` / `success`; URL and SHA recorded in `handoff.md`. Verify: the run's `head_sha` equals the branch head.
- [ ] 5.3 `scripts/pregate-verify.sh gate2 authorize-deep-probe-by-api-key` passes and `scripts/gate-run.sh authorize-deep-probe-by-api-key 2 full` is run; findings via `/workflow:fix-findings`, confirmation with `scripts/gate-run.sh authorize-deep-probe-by-api-key 2 confirm <round>`. Verify: review.md carries a Gate 2 round bound to the requested commit; the merge verifier checks that the last Gate 2 decision reads approved/confirmed and is fresh.

## Post-merge acceptance (not a Gate 2 task)

- After the merge: the user pushes `main`; the executor checks the `main` run via the API before proposing the archive; `/opsx:archive` syncs the modified `health-check` requirement and removes row 10a from `openspec/ROADMAP.md`.
