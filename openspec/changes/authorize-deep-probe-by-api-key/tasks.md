# Tasks — authorize-deep-probe-by-api-key

High tier (an authorization boundary on a production endpoint outside the firewall — AGENTS.md, NFR-SEC-7): Gate 1 on the artifacts and Gate 2 on the code. Work on the change branch; `scripts/workflow-verify.sh apply authorize-deep-probe-by-api-key` first. Every task touching authorization is flagged in its commit body.

## 0. Gate 1 (high tier)

- [x] 0.1 `scripts/pregate-verify.sh gate1 authorize-deep-probe-by-api-key` passes and `scripts/gate-run.sh authorize-deep-probe-by-api-key 1 full` is run on the completed artifacts (proposal with tier and Non-goals, the health-check delta, design with the applicability table, tasks); findings via `/workflow:fix-findings`, confirmation with `scripts/gate-run.sh authorize-deep-probe-by-api-key 1 confirm <round>`. Verify: review.md carries a Gate 1 round bound to the requested commit; the merge verifier checks that the last Gate 1 decision reads approved/confirmed.

## 1. The lookup process and the Redis client

- [x] 1.1 `bin/probe-key-lookup` (design decision 3: a standalone PHP script — plain `PDO` from the `DATABASE_URL` environment variable with `ATTR_TIMEOUT` 2 and `statement_timeout` from `--statement-timeout-ms`, the key hash read from stdin, one `SELECT … JOIN users … WHERE key_hash = :hash`, prints exactly one line `V <expires_at|->`, `D` or `U <class>`; executable bit set) and `src/Shared/Health/KeyLookupProcess.php` (`lookup(string $hash, float $allowanceSeconds): LookupOutcome` — `Symfony\Component\Process\Process` with `setTimeout($allowance)`, `setInput($hash)`, `DATABASE_URL` in the child's env; a clean line → the outcome; `ProcessTimedOutException`, a non-zero exit or unparsable output → `Unavailable` with the reason class). `src/Shared/Health/BoundedRedisConnection.php` (design decision 3a: a RESP client over a non-blocking socket — `connect(url, timeout)`, `command(operation, ...words)`, `close()` — whose every operation has one absolute deadline, with status/integer/bulk replies and a 64 KiB ceiling); `src/Shared/Health/HealthProbe.php` `checkRedis()` uses it (the same `PING`, the same 2.0 s), the PDO check unchanged. `tests/Unit/Shared/Health/KeyLookupProcessTest.php` (output parsing: `V 2030-01-01T00:00:00+00:00`, `V -`, `D`, `U PDOException`, garbage, empty → the right outcome). `tests/Integration/Shared/Health/KeyLookupProcessTest.php` (real PostgreSQL + the fixtures of task 3.1; rows through `ApiKeyFactory`/`UserFactory` committed for the child through the test's connection — the child is a separate process, so the test uses committed rows outside the suite transaction as the concurrency test does): admin key → `V` with its expiry; non-admin, blocked, revoked, expired, unknown → `D`; a closed port → `U` at once; black-hole connect → `U` within 3 s; `LOCK TABLE api_keys` held by the test → `U` at 2.5 s ± 0.3 and no `probe-key-lookup` process left (`pgrep -f`); proxy `--stall-after-responses=1` (handshake done, response lost) → `U` at 2.5 s ± 0.3, child gone — the case no in-process client passes; the command line of the running child (during the `LOCK TABLE` case, `ps -o args`) does not contain the hash; `BoundedRedisCommands::open` against the black hole → the `PING` read times out at the per-operation timeout. Verify: `docker compose exec -T php vendor/bin/phpunit --filter 'KeyLookupProcess|HealthProbeTest'` green; failing inputs in the commit body: `setTimeout` removed → the lost-response case hangs past its bound (the test's own guard fails it); the hash passed as an argument → the command-line assertion fails.

- [x] 1.2 `composer.json` / `composer.lock`: `symfony/process` promoted from `require-dev` to `require` — production code runs it, and `composer install --no-dev` did not install it (Gate 2 round 1, finding 1). `tests/Unit/ProductionDependenciesTest.php` reads the lock and refuses any `require-dev` namespace in `src/`, `bin/` or `public/` — the suite itself always has the development packages, so only the manifest can be asked. Verify: the test; and the prod request driven on a clean `composer install --no-dev` tree (admin key → the probe report, unknown key → the identical 404); failing input: `symfony/process` back in `require-dev` → the test names the file and the package, and the same clean tree answers 500.

## 2. The memory and the authorizer

- [x] 2.1 `src/Shared/Health/ProbeMemory.php` (design decision 4: keys `probe-auth:gen:<sha256(hash)>` and `probe-auth:v:<sha256(hash)>`; `token(hash): string` (`GET`, absent → `""`); `remember(hash, string $token, ?\DateTimeImmutable $expiresAt, \DateTimeImmutable $verifiedAt)` through one Lua `EVAL` that stores `{gen, exp, verified_at}` with a 300-second TTL only if the current token equals `$token` (absent matches only `""`); `deny(hash)` = one Lua `EVAL` that sets a fresh `random_bytes(16)` hex token (TTL 300 s) and deletes `v`; `consult(hash, now): bool` = `GET` v → present, `verified_at` within 300 s of `now`, and `exp` null or > now; a Redis failure → `MemoryUnavailable` naming the operation (`connect`, `auth`, `token`, `remember`, `deny`, `consult`), logged at `warning` with `{operation, exception class}`; one connection per request — connect, `AUTH`/`PING`, `token()`, one post-lookup command — each with a 0.25-second deadline over the whole operation), `src/Shared/Health/ProbeAuthorizer.php` (design decisions 1–2, 5: regex → hash → connect + `token()` → `KeyLookupProcess::lookup(hash, 2.5)` → verified: `remember(token read before, exp, now)`; denied: `deny()`; unavailable: `consult()` — four Redis operations at 0.25 s each, the post-lookup one always funded; a Redis failure before the lookup leaves the request without memory but still verified for itself; `warning` `{exception}` for either phase, never the key), `src/Shared/Health/HealthController.php`: in `prod`, run the probe when `authorize()` is true, else the existing 404 body. `tests/Unit/Shared/Health/ProbeAuthorizerTest.php` with fakes for the process and the memory: verified → true + `remember(token read before)`; denied → false + `deny`; unavailable + fresh unexpired `v` → true; unavailable + `v` older than 300 s by `verified_at` → false; unavailable + expired key in `v` → false; unavailable + no `v` → false; unavailable + memory failing → false; a non-key header → false with no I/O; a timed-out lookup still consults. `tests/Integration/Shared/Health/ProbeMemoryTest.php` (real Redis): `remember` stores only when the token is current (absent matches only an absent read); two `deny` calls yield two different tokens and delete `v`; TTLs ≤ 300 s; **the interleaving established by the mechanism**, both sides being the real `ProbeAuthorizer` (`InterleavingLookup` wraps A's lookup so the revocation and B's `authorize()` — denied → fresh token, `v` deleted — land between A's database answer and A's memory write; A's token read already preceded its statement): A is honoured for its own request and its verification is refused, `v` absent — no timestamps supplied by the test; **the reuse case**: the same wrapping with a denial's token X read first and the token key deleted inside the window (standing for TTL expiry) → B's denial creates Y ≠ X and A's write is refused; **the age bound**: a `v` stored with `verified_at` 301 s in the past is not consulted although its TTL remains; the fake Redis with a password and `--command-log`: `--response-delay=0.2` on every command → `AUTH`, the token `GET` and the post-lookup command all answered (asserted from the command log), Redis time ≥ 0.6 s, the phase within 1.0 s; `--slow-from-command=3 --response-delay=0.5` (three RESP commands per request — `AUTH` = 1, token `GET` = 2, post-lookup = 3; connect is not a command) → the log shows `AUTH` and the token `GET` answered and the post-lookup command received, `MemoryUnavailable` with `operation === 'consult'` (closed database port) raised after ≥ 0.25 s and < 0.5 s; `tests/Fixture/full-backlog-listener.php` as the Redis address → `MemoryUnavailable` with `operation === 'connect'` after ≥ 0.25 s and < 0.5 s, empty command log, and `authorize()` still runs the database phase (a verified key → true, nothing stored). `tests/Integration/Shared/Health/ProbeAuthorizerTest.php` (real PostgreSQL and Redis, spy logger): admin key → true and `v` present; non-admin, blocked, revoked, expired, unknown → false, token replaced, `v` absent; closed port → false within 3 s with the "database unavailable" warning; `LOCK TABLE` → false within 3 s; a remembered `v` + closed port → true; after a denial + closed port → false; Redis unreachable during a denied answer → false, the "memory unavailable" warning, and the earlier `v` still present (the admitted case, asserted as such). Verify: `docker compose exec -T php vendor/bin/phpunit --filter 'ProbeMemory|ProbeAuthorizer'` green; failing inputs in the commit body: the `roles` check removed → the non-admin case is true; the `is_blocked` check removed → the blocked case is true; the token read moved after the lookup → the established-interleaving case stores the stale `v`; the CAS replaced by a plain `SET` → the same; the random token replaced by an incrementing counter → the reuse case stores the stale `v`; `DEL` in the deny script removed → the "after a denial + closed port" case is true; the `exp` check at consult removed → an expired remembered key authorizes; the `verified_at` bound removed → the 301-second-old `v` authorizes; the per-command Redis timeout removed → the slow post-lookup test observes the full 0.5 s and the full-backlog test does not return within 0.5 s; the reserved Redis allowance replaced by "skip when the database phase used its allowance" → the remembered-monitor case is false.

## 3. Production end-to-end tests

- [x] 3.1 `tests/Fixture/black-hole-socket.php` (binds 127.0.0.1:0, prints the port, accepts, never writes), `tests/Fixture/tcp-proxy.php` (binds 127.0.0.1:0, prints the port; `--upstream=host:port`, `--delay=<s>` before connecting upstream, `--stall-after-responses=<n>` forwards upstream→client for the first n upstream messages and swallows the rest while still forwarding client→upstream; `stream_select` loop) and `tests/Fixture/fake-redis-server.php` (binds 127.0.0.1:0, prints the port; answers `AUTH` `+OK`, `PING` `+PONG`, `GET` `$-1`, `EVAL` `+OK`, each after `--response-delay=<s>` and, when asked, in `--fragment-bytes=<n>` pieces `--fragment-delay=<s>` apart, both only from the `--slow-from-command=<n>`-th RESP command when given; appends `received`/`answered` lines with the command name and a timestamp to `--command-log=<file>`; `--get-value=<string>` answers `GET` with that bulk string, a slow Redis that does hold a remembered verification), `tests/Fixture/full-backlog-listener.php` (binds 127.0.0.1:0, `listen(0)`, connects to itself once and never accepts, prints the port — the next connect's SYN is dropped and the client's connect stalls), all run with `Process` and stopped in `finally` (design decision 7). `tests/Api/HealthDeepProdTest.php` (the prod kernel in a process of its own — `tests/Fixture/prod-health-request.php` driven by `tests/Support/ProdHealthRequest.php`, which passes the DSNs in that process's environment and reads the prod log records from its stderr; admin user and keys inserted into the `app` database through a bounded PDO, committed, removed in `tearDown`): refusal matrix — no header, admin JWT, non-admin key, revoked admin key, expired admin key, blocked admin's key → identical 404 bodies, `no-store`, `last_used_at` unchanged; admin key → probe body (200/503 with `checks`); closed port → 404 under 1 s; black-hole connect → 404 under 3.5 s; `LOCK TABLE` → 404 in 2.5–4 s; proxy `--delay=0.7` + `LOCK TABLE` → 404 under 5 s; proxy `--stall-after-responses=1` (lost response) → 404 under 5 s and no `probe-key-lookup` process left; closed database port + fake Redis with a password in `REDIS_URL` and `--slow-from-command=3 --response-delay=0.5 --command-log` → 404 under 5 s, the command log shows `AUTH` and the token `GET` answered and the consultation received, and the `warning` record names `operation: consult`; the same with `--response-delay=0.2` on every command → the memory answers (404 without a remembered key; with `--get-value` holding a verification, the probe body — the log shows the consultation answered, then the probe's own `AUTH`/`PING`); `REDIS_URL` at the full-backlog listener + the admin key + a live database → the probe body, the `warning` names `operation: connect`, nothing stored in the real Redis; admin key verified against the real database, then the closed port → 503 with `checks.database` `fail`; the same, then `LOCK TABLE` instead → the probe runs (its own report: 200 expected); the key revoked while the database is up → 404, then the closed port → still 404. Verify: `docker compose exec -T php vendor/bin/phpunit --filter HealthDeepProd` green; failing inputs in the commit body: the child's `setTimeout` removed → the lost-response case exceeds 5 s; `remember` removed → the remembered-monitor case answers 404; the 404 body built differently on a lookup failure → the identical-body assertion fails.
- [x] 3.2 `.github/workflows/*.yml`: `make migrate EXEC=` before `make test-db EXEC=` (design decision 8), so the `app` database the prod-kernel tests read carries `api_keys`. Verify: task 5.2's green run shows `HealthDeepProdTest` executed (not skipped) in the PHPUnit output; `rg -n 'make migrate EXEC=' .github/workflows` shows the step.

## 4. Docs

- [x] 4.1 `docs/how-to/local-development.md` — the Health paragraph: in `prod` the deep probe needs `Authorization: Bearer <admin API key>`; a monitor alerts on any non-200; a 404 with a valid key means the key could not be verified — the database was unreachable and no verification from the last 5 minutes was remembered; a monitor that polled before the outage sees the probe's own report instead; the monitor's timeout should allow 10 s; IP literals or a local resolver for `DATABASE_URL`/`REDIS_URL` where the resolver may stall; `proc_open` must be allowed (the lookup runs in a child process); `docs/explanation/requirements.md`: the §4 table row for `/health` and the deep-probe sentence in §2.10 refined (admin key, budget, memory), §7 row 10a kept in sync with the delivered scope. Verify: the documented probe request run on the dev stack in its exact form (dev is open, so the request succeeds; the `prod` behaviour is the test suite's) and a curl against the prod kernel is not attempted outside tests; both files re-read whole after the last edit; `rg -n 'deep=1' docs openspec/changes/authorize-deep-probe-by-api-key` shows how-to, brief, proposal, spec and design agreeing.

## 5. Wrap-up

- [x] 5.1 `make check` green (cs, stan level 8, all suites); commits per block (`feat(health):` 1.1 and 2.1, `test(health):` 3.1, `ci:` 3.2, `docs:` 4.1) with the agent trailer; every guard's commit body names its demonstrated failing input; the authorization parts flagged in the commit bodies and in `handoff.md`. Verify: `git log --oneline main..HEAD`; `openspec validate authorize-deep-probe-by-api-key --strict` passes.
- [x] 5.2 Green Actions run on the exact branch head before Gate 2: the user pushes the change branch; the executor queries `https://api.github.com/repos/Azazu/linkboard/actions/runs?branch=change/authorize-deep-probe-by-api-key` until the run for `git rev-parse HEAD` is `completed` / `success`; URL and SHA recorded in `handoff.md`. Verify: the run's `head_sha` equals the branch head.
- [x] 5.3 `scripts/pregate-verify.sh gate2 authorize-deep-probe-by-api-key` passes and `scripts/gate-run.sh authorize-deep-probe-by-api-key 2 full` is run; findings via `/workflow:fix-findings`, confirmation with `scripts/gate-run.sh authorize-deep-probe-by-api-key 2 confirm <round>`. Verify: review.md carries a Gate 2 round bound to the requested commit; the merge verifier checks that the last Gate 2 decision reads approved/confirmed and is fresh.

## Post-merge acceptance (not a Gate 2 task)

- After the merge: the user pushes `main`; the executor checks the `main` run via the API before proposing the archive; `/opsx:archive` syncs the modified `health-check` requirement and removes row 10a from `openspec/ROADMAP.md`.
