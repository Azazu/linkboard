# Tasks — add-analytics-read-model

Medium tier: no Gate 1; Gate 2 on the code (AGENTS.md). Work on `change/add-analytics-read-model`; `scripts/workflow-verify.sh apply add-analytics-read-model` first.

## 1. Read model

- [x] 1.1 `src/Analytics/Report/Period.php` (UTC `from`/`to`, `defaults(ClockInterface)` = start of the next UTC day and 30 days before, `of(?string, ?string, ClockInterface)` with RFC 3339 parsing and the rules — order → violation on `from`, > 366 days → violation on `to` — `previous()`), `src/Analytics/Report/Granularity.php` (enum `hour|day`, interval literal, 14-day bound for `hour` → violation on `granularity`), `src/Analytics/Report/ReportRequest.php` (link id or null, period, granularity, limit, includeBots, `cacheKey(string $report)` = `<report>.<sha1 of the canonical parameters>`). `tests/Unit/Analytics/PeriodTest.php`, `GranularityTest.php`, `ReportRequestTest.php`: defaults alignment with a fixed clock (2026-09-11T14:05:00Z → `2026-08-13T00:00:00Z`…`2026-09-12T00:00:00Z`), `Z` and `+02:00` spellings parse to UTC, order/366/14 violations name the specified parameter, `previous()` length, key determinism and distinctness per parameter. Verify: `docker compose exec -T php vendor/bin/phpunit --testsuite Unit --filter Analytics` green; failing inputs in the commit body: dropping the 366-day rule fails the length case; dropping the `hour` bound fails the granularity case. Done: `src/Analytics/Report/Period.php`, `src/Analytics/Report/Granularity.php`, `src/Analytics/Report/ReportRequest.php`, `src/Analytics/Report/InvalidReportParameter.php` (the parameter-naming exception the API turns into a violation), `tests/Unit/Analytics/PeriodTest.php`, `tests/Unit/Analytics/GranularityTest.php`, `tests/Unit/Analytics/ReportRequestTest.php` (12 tests); both failing inputs demonstrated (no 366-day rule → the length case fails; no `hour` bound → the granularity cases fail).
- [x] 1.2 `tests/Fixture/ClickRows.php` (DBAL inserts with the handler's twelve columns, explicit facts, defaults for the rest); `src/Analytics/Dto/*` (one readonly DTO per report with typed rows, `generatedAt`, echoed parameters); `src/Analytics/Query/LinkSummaryQuery.php`, `TimeseriesQuery.php` (nullable link for the global variant), `BreakdownQuery.php` (countries, devices, referrers, variants over one template with `sum() over ()` share and `rank()`), `GlobalStatsQuery.php` — design decision 2: bound parameters, the `AND NOT is_bot` fragment as a PHP branch between two literals, `AT TIME ZONE 'UTC'` bucketing, `generate_series` left join, `FILTER` clauses for the summary. `tests/Integration/Analytics/LinkSummaryQueryTest.php`, `TimeseriesQueryTest.php`, `BreakdownQueryTest.php`, `GlobalStatsQueryTest.php` asserting the numbers of every spec scenario: summary (4/2/100.0/today/all-time/first/last, null delta, empty link), timeseries (`[0,3,0,0,1,0,0]` and cumulative `[0,3,3,3,4,4,4]`; `2026-03-29T00:30:00+02:00` in the `2026-03-28` bucket; 24 hourly buckets), countries (DE 5/50.0/1, US 3/30.0/2, null 2/20.0/3; `limit`), devices (two breakdowns), referrers (`direct` 5 first), variants (A 6/4, B 4/4, total 10, rule-resolved rows ignored, empty → total 0), rank ties (two keys with equal counts share rank 1), bots toggle (10 vs 8), global (3 users, 3 links, 2 active, 12 clicks; top links with slug and owner; timeseries over links). Verify: `docker compose exec -T php vendor/bin/phpunit --testsuite Integration --filter Analytics` green; failing inputs in the commit body: removing the bot fragment fails the toggle; removing the left join fails gap filling; `row_number()` fails the tie case; dropping `AT TIME ZONE 'UTC'` with the session zone set to `Europe/Berlin` fails the DST case. Done: `tests/Fixture/ClickRows.php`, `src/Analytics/Dto/` (SummaryFigures, TimeBucket, Grouped, CountryRow, RefererRow, DeviceTypeRow, OsRow, Devices, VariantRow, TopLinkRow, GlobalTotals), `src/Analytics/Query/Sql.php` (shared fragments and parameter types), `src/Analytics/Query/LinkSummaryQuery.php`, `src/Analytics/Query/TimeseriesQuery.php`, `src/Analytics/Query/BreakdownQuery.php`, `src/Analytics/Query/GlobalStatsQuery.php`, `tests/Integration/Analytics/AnalyticsQueryTestCase.php`, `tests/Integration/Analytics/LinkSummaryQueryTest.php`, `tests/Integration/Analytics/TimeseriesQueryTest.php`, `tests/Integration/Analytics/BreakdownQueryTest.php`, `tests/Integration/Analytics/GlobalStatsQueryTest.php` (16 tests, 139 assertions); all four failing inputs demonstrated — the DST case needed a click at 23:30Z (inside the two-hour window where Berlin and UTC days differ) to catch the missing `AT TIME ZONE`.
- [x] 1.3 `tests/Unit/Analytics/ArchitectureTest.php`: scans every file under `src/Analytics/` and fails on `App\Click\Entity`, `App\Link\Entity` or `Doctrine\ORM`. Verify: the test is green; adding `use App\Click\Entity\Click;` to a query class makes it fail (recorded in the commit body, then reverted). Done: `tests/Unit/Analytics/ArchitectureTest.php`; a `use App\Click\Entity\Click;` in `GlobalStatsQuery` fails it with the file and the namespace named.

## 2. API and cache

- [x] 2.1 `src/Analytics/Api/LinkSummaryReport.php`, `LinkTimeseriesReport.php`, `LinkCountriesReport.php`, `LinkDevicesReport.php`, `LinkReferrersReport.php`, `LinkVariantsReport.php` (the DTOs of 1.2 carrying `#[ApiResource]` with one `Get` at `/links/{id}/stats/<report>`, `security: is_granted("ROLE_USER")`, declared `QueryParameter`s with constraints — design decision 3; before choosing the `from`/`to` constraint verify it accepts both `2026-09-01T00:00:00Z` and `2026-09-01T00:00:00+00:00`: `docker compose exec -T php php -r 'var_dump(\DateTimeImmutable::createFromFormat(\DateTimeInterface::RFC3339, "2026-09-01T00:00:00Z"));'`), `src/Analytics/Api/LinkReportProvider.php` (404 for a malformed or unknown id first, `LinkResource` through `PublicUrl`, `Security::isGranted(LinkVoter::VIEW, …)` → `AccessDeniedException`, the `Period` cross-checks → `ValidationException` with the specified property path, the report through `ReportCache` of 2.2 — until 2.2 lands, computed directly). `tests/Api/Analytics/LinkReportsTest.php` (`WebTestCase` + JWT as in `tests/Api/Link/LinkApiTestCase.php`): the six reports × owner 200 / admin 200 / stranger 403 problem+json / anonymous 401; `not-a-uuid` and a random uuid → 404; inactive link → 200 with counts; defaults echo (`from`/`to` day-aligned, `includeBots` false, `generatedAt` RFC 3339); the 422 matrix — `from=yesterday`, `granularity=week`, `limit=0`, `limit=51`, `includeBots=maybe`, `from > to`, > 366 days, `hour` over 31 days — each with exactly one violation whose `propertyPath` is `from`/`granularity`/`limit`/`limit`/`includeBots`/`from`/`to`/`granularity`; `GET /api/docs.json` lists the six paths under `/api/v1/links/{id}/stats/` with their parameters. Verify: `docker compose exec -T php vendor/bin/phpunit --testsuite Api --filter LinkReports` green; `make console ARGS='debug:router'` shows the six routes; failing inputs in the commit body: dropping the provider's `isGranted` fails the stranger case; declaring `limit` without `Range` fails the `limit=51` case. Done: `src/Analytics/Api/LinkSummaryReport.php`, `src/Analytics/Api/LinkTimeseriesReport.php`, `src/Analytics/Api/LinkCountriesReport.php`, `src/Analytics/Api/LinkDevicesReport.php`, `src/Analytics/Api/LinkReferrersReport.php`, `src/Analytics/Api/LinkVariantsReport.php` (each DTO is its own `#[ApiResource]` with one `Get`; `linkId` is the identifier behind `{id}`), `src/Analytics/Api/Parameter/` (`FromParameter`, `ToParameter`, `IncludeBotsParameter`, `GranularityParameter`, `LimitParameter` — `QueryParameter` subclasses so the constraints live in one place; attribute arguments cannot call methods), `src/Analytics/Api/ReportRequestFactory.php` (query → `ReportRequest`; cross-checks rendered as `ValidationException` on the parameter; one clock reading), `src/Analytics/Api/LinkReportProvider.php`, `tests/Api/Analytics/AnalyticsApiTestCase.php`, `tests/Api/Analytics/LinkReportsTest.php` (8 tests: matrix, 404s, inactive link, defaults/echo, 422 matrix incl. `limit=abc` and a date-only `to`, inconsistent parameters, numbers and the bots toggle over HTTP, OpenAPI paths and parameters). `\DateTimeImmutable::createFromFormat(RFC3339)` accepts both `Z` and `+00:00` (verified). `debug:router` lists the six routes. Failing inputs demonstrated: dropping the provider's `isGranted` fails the stranger case; dropping the order rule in `Period` fails the inconsistent-parameters case over HTTP. Note: single-value rules are enforced twice on purpose — the declared constraint (OpenAPI + 422 message) and the domain check in `Period`/`ReportRequest`/the factory — so removing only the `Range` constraint keeps `limit=51` at 422 through the domain check; the Range's own observable effect is the documented `minimum`/`maximum`.
- [x] 2.2 `config/packages/cache.yaml`: `framework.cache.prefix_seed: 'linkboard/%kernel.environment%'` and `pools.cache.reports: { adapter: cache.adapter.redis_tag_aware, provider: '%env(REDIS_URL)%', default_lifetime: 300 }`; `src/Analytics/Cache/ReportCache.php` (design decision 7: `remember(key, tags, compute)` with `try/catch (\Throwable)` → `warning` `{exception}` + direct compute; `forgetLink(Uuid)`, `forgetGlobal()` with `try/catch` → `warning` `{link_id, exception}`); `LinkReportProvider` reads through `remember()` with tag `link-<uuid>`; `src/Link/Api/UpdateLinkProcessor.php` calls `forgetLink()` after the flush, `src/Link/Api/DeleteLinkProcessor.php` calls `forgetLink()` and `forgetGlobal()` after the flush (design decision 8). Before writing, verify the adapter's contract: `rg -n 'noeviction' vendor/symfony/cache/Adapter/RedisTagAwareAdapter.php` and `rg -n 'Failed to fetch key' vendor/symfony/cache/Traits/AbstractAdapterTrait.php`. `tests/Unit/Analytics/ReportCacheTest.php` (a pool stub whose `get()` throws → compute called once, one `warning`; `invalidateTags` throws → `warning` with the link id, no exception). `tests/Api/Analytics/ReportCacheTest.php` (reuses `tests/Fixture/StatementRecorder.php`): second identical request → no statement on `clicks`, identical body including `generatedAt`; `includeBots=true` → recomputed; `PATCH {"isActive":false}` between two summaries → the second counts a click recorded in between, a rejected 422 patch leaves the cached body; `DELETE` → the summary answers 404 and an admin `top-links` no longer names the link (2.3); cache store unavailable — `new RedisTagAwareAdapter(RedisAdapter::createConnection('redis://127.0.0.1:1?lazy=1'))` installed into `ReportCache` through the test container before the first request → 200 with database numbers and one `warning`; the same stub during a `PATCH` → 200 and a `warning` naming the link id. `tearDown` clears the `cache.reports` pool. Verify: `docker compose exec -T php vendor/bin/phpunit --testsuite Api --filter Analytics` green; `docker compose exec -T redis redis-cli --scan --pattern '*linkboard*test*' | head` is empty after the suite; `make console ARGS='cache:pool:list'` lists `cache.reports`; failing inputs in the commit body: removing `forgetLink` from the update processor fails the invalidation case; removing the `try/catch` in `remember()` fails the unavailable-store case (500). Done: `config/packages/cache.yaml` (`prefix_seed` per environment, pool `cache.reports`), `src/Analytics/Cache/ReportCache.php` (`remember`, `forgetLink`, `forgetGlobal`; a `false` from `invalidateTags` is logged too — the Symfony adapter logs the exception itself and returns `false` rather than throwing), `src/Link/Api/UpdateLinkProcessor.php`, `src/Link/Api/DeleteLinkProcessor.php`, `tests/Unit/Analytics/ReportCacheTest.php` (3 tests), `tests/Api/Analytics/ReportCacheTest.php` (6 tests: hit without a `clicks` statement, distinct entries, patch invalidates / rejected patch does not, delete → 404 and `global` refreshed, store unavailable → 200 with the adapter's `Failed to fetch key` warning carrying the exception, store unavailable during a patch → 200 with our `Report cache not invalidated` warning naming the link id). `cache:pool:list` shows `cache.reports`; the adapter's contract verified (`noeviction`/`volatile-*` check, `Failed to fetch key` fail-open). Failing inputs demonstrated: no `forgetLink` in the update processor fails the invalidation case; no `catch` in `remember()` fails the unit case with the throwing pool — the HTTP unavailable-store case stays green without it because the Symfony adapter itself fails open (fetch → miss, save → logged `false`), which is why both layers exist.
- [x] 2.3 `src/Analytics/Api/AdminSummaryReport.php`, `AdminTimeseriesReport.php`, `AdminTopLinksReport.php` (`/admin/stats/summary|timeseries|top-links`, `security: is_granted("ROLE_ADMIN")`, the same parameters, tag `global`) and `src/Analytics/Api/AdminStatsProvider.php`. `tests/Api/Analytics/AdminStatsTest.php`: summary (`totalUsers` 3, `totalLinks` 3, `activeLinks` 2, `totalClicks` 12, `clicksToday`), `top-links?limit=2` (rank 1 and 2 with `slug`, `ownerId`, `clicks`, `uniqueVisitors`), timeseries buckets summing to every link's clicks with zeros on empty days, `includeBots`, regular user 403 problem+json on each, anonymous 401, cache hit through `StatementRecorder`, `global` invalidated by a link deletion. Verify: `docker compose exec -T php vendor/bin/phpunit --testsuite Api --filter AdminStats` green; `GET /api/docs.json` lists the three admin paths; failing input in the commit body: removing `forgetGlobal` from the delete processor fails the top-links-after-delete case. Done: `src/Analytics/Api/AdminSummaryReport.php`, `src/Analytics/Api/AdminTimeseriesReport.php`, `src/Analytics/Api/AdminTopLinksReport.php`, `src/Analytics/Api/AdminStatsProvider.php`, `tests/Api/Analytics/AdminStatsTest.php` (3 tests: summary/top-links/timeseries numbers incl. `includeBots` and a cache hit, user 403 + anonymous 401 on each, OpenAPI paths). `debug:router` lists the three routes. Failing input demonstrated: no `forgetGlobal` in the delete processor → the deleted link stays in `top-links` (case fails). `make check` green on the block: 553 tests / 7210 assertions.

## 3. Demo seed

- [x] 3.1 `src/Shared/Demo/DemoDataset.php` (ten slugs, targets, documents — device, country, language, variants, combinations — limits/expiry/UTM for some; per-link click weights; the SQL `CASE` distributions) and `src/Shared/Demo/DemoSeedCommand.php` (`app:demo:seed`, `--clicks`, `--days`, `--reset`; the `prod` guard on `%kernel.environment%`; the duplicate guard on the two demo e-mails; one `Connection::transactional()` — design decision 10; passwords from `random_bytes`, hashed by `UserPasswordHasherInterface`, printed once; documents through `RulesDocumentParser`; one `INSERT … SELECT … FROM generate_series` per link; `click_count` set from the rows; counters forgotten and cache tags invalidated on `--reset`). `tests/Unit/Shared/DemoSeedCommandTest.php` (constructed with environment `prod` → exit 1, no repository call), `tests/Integration/Shared/DemoSeedCommandTest.php` (`CommandTester`): `--clicks=500 --days=5` → 2 users (one `ROLE_ADMIN`), 10 links with non-null `rules` belonging to the demo user, exactly 500 rows within the last 5 days, `click_count` = rows per link, ≥ 1 bot, ≥ 1 null country, ≥ 1 `variant` with `resolved_by` `variant`, ≥ 1 null `referer_host`, distinct hashes < 500, no raw IP-looking or UA-looking value in any column; the printed password logs the demo user in (`POST /api/v1/auth/token` → 200); second run → exit 1, counts unchanged; `--reset --clicks=300 --days=3` → 300 rows, ten new link ids, old ids gone. Verify: `docker compose exec -T php vendor/bin/phpunit --filter DemoSeed` green; on the dev stack `make console ARGS='app:demo:seed'` prints two accounts and passwords, `make console ARGS='app:demo:seed'` again exits 1, `make console ARGS='app:demo:seed --reset'` reseeds; `make console ARGS="dbal:run-sql 'SELECT count(*) FROM clicks'"` shows 50000; failing inputs in the commit body: removing the duplicate guard makes the second run create a third account (the unchanged-counts case fails); removing the environment guard fails the unit case. Done: `src/Shared/Demo/DemoDataset.php` (ten links: device/OS rules, country rules, language rules, A/B variants of two and three names, two with `maxClicks`, one expired, two with UTM; weights summing to 100), `src/Shared/Demo/DemoSeedCommand.php` (`--clicks`, `--days`, `--reset`; guards; one `Connection::transactional()` around the entity writes and one `INSERT … SELECT … FROM generate_series` per link with `random()`-driven CASE distributions, `resolved_by`/`variant` consistent with each document; `click_count` from the rows; counters and cache tags dropped afterwards; passwords `bin2hex(random_bytes(12))`, printed once), `tests/Unit/Shared/DemoSeedCommandTest.php` (prod guard before any repository call), `tests/Integration/Shared/DemoSeedCommandTest.php` (500/5 → every invariant of the spec's small-seed scenario, `click_count` = rows per link, the printed password obtains a token; second run exit 1 and unchanged; `--reset --clicks=300 --days=3` → 300 rows, ten new ids, old ids gone, a new password). Dev stack: `app:demo:seed` printed the two accounts (passwords not recorded anywhere), the second run was refused with the `--reset` hint, `--reset` reseeded, `dbal:run-sql` counted 50 000 new clicks over 60 days. Failing inputs demonstrated: without the duplicate guard the second run silently reseeds (exit 0, the unchanged-counts case fails); without the environment guard the unit case fails.

## 4. Docs

- [x] 4.1 `docs/how-to/local-development.md`: an **Analytics** section (the six report endpoints and the three admin ones, parameters and defaults — the day-aligned default period, 366/14-day rules, `limit`, `includeBots` — curl examples, the 422 shape, `generatedAt` and the 300 s staleness, invalidation on `PATCH`/`DELETE`, Redis unavailable → computed report + `warning`, the eviction-policy requirement of the tag-aware adapter, `app:demo:seed` with the printed credentials and `--reset`), the Daily table row for the seed; `docs/reference/commands.md`: the `app:demo:seed` row. Verify: both files re-read whole after the last edit; every documented command run in its exact form against the dev stack (seed, token with the printed password, three report curls, a `PATCH` followed by a recomputed report) and the outputs pasted into the commit body; `rg -n 'demo:seed' docs` shows both files. Done: `docs/how-to/local-development.md` (Analytics section: report table, parameters and defaults, curl examples, the seed, cache and degradation incl. the eviction-policy requirement and `prefix_seed`, admin statistics, pointer to the design appendix; Daily row), `docs/reference/commands.md` (`app:demo:seed` row); both re-read whole after the last edit. Dev stack, exact forms: `make console ARGS='app:demo:seed --reset'` → 10 links / 50 000 clicks with two printed accounts; tokens for both accounts with the printed passwords; summary (`clicksInPeriod` 4191 vs 4276, `deltaPercent` -2.0, `clicksToday` 53), timeseries (7 day buckets, cumulative 133 → 1037), countries (`limit=3&includeBots=true`: DE 30.5 %, US 20.0 %, GB 11.4 %), variants (A 50.7 % / B 49.3 %), admin `top-links?limit=3` (app-download, spring-sale, eu-landing) and `summary`; hourly buckets over 31 days → 422 on `granularity`; a second summary returned the same `generatedAt` (cache hit), a `PATCH {"isActive":false}` made the next summary recompute (new `generatedAt`).
- [x] 4.2 `docs/explanation/requirements.md`: FR-ANL-2 refined (the default period is the current UTC day plus the 29 before it, half-open; `summary` semantics — all-time totals, period vs previous, `deltaPercent` null on an empty previous period; unknown country and `direct` groups; variants report over variant-resolved clicks; `generatedAt`), FR-ANL-3 (`global` invalidated on link deletion; cache failure → computed report + `warning`; `prefix_seed` per environment), FR-ANL-4 (`activeLinks`, `top-links` fields); §7 row 8 and `openspec/ROADMAP.md` row 8 re-read against the delivered scope (unchanged unless the scope moved). Verify: `rg -n 'FR-ANL-' docs/explanation/requirements.md openspec/changes/add-analytics-read-model/specs/analytics/spec.md` — every refined statement matches the spec; the section re-read whole. Done: FR-ANL-2 (half-open UTC period, day-aligned default, 422 per parameter, `generatedAt`; `summary` all-time vs period semantics; `FILTER` instead of `lag()`; unknown-country group; variants over variant-resolved clicks; rank ties), FR-ANL-3 (per-environment namespaces, `global` on deletion, computed-without-cache degradation, refused invalidation never fails the write), FR-ANL-4 (`activeLinks`, `top-links` fields); §7 row 8 and `openspec/ROADMAP.md` row 8 re-read — unchanged, the scope did not move. §2.6 re-read whole.

## 5. Measurement and wrap-up

- [ ] 5.1 Design decision 9: `make console ARGS='app:demo:seed --reset --clicks=1000000 --days=60'`; for each link report SQL (default period, bots excluded and included) `make console ARGS="dbal:run-sql 'EXPLAIN (ANALYZE, BUFFERS) …'"`, plus the three global statements; a scratch script (`docker compose exec -T php php /app/var/bench.php`, written to `var/` — gitignored — not committed) running every statement 20 times through the DBAL and printing the 95th percentile; the plans (index used, rows, timing) and the p95 table pasted into the Appendix of `design.md`; the `(occurred_at)` index decision recorded there (added in this change only if the admin timeseries misses 300 ms — then a reviewed migration and a test); then `make console ARGS='app:demo:seed --reset'` back to 50 000. Verify: the Appendix names, per statement, the index the plan used and the p95; every per-link plan uses `idx_clicks_link_occurred_human` or `idx_clicks_link_occurred`; `rg -n 'Appendix' -A 40 openspec/changes/add-analytics-read-model/design.md` shows the table.
- [ ] 5.2 `make check` green (cs, stan level 8, all suites); commits per block (`feat(analytics):` 1.x and 2.x, `feat(link):` the invalidation in the processors, `feat(demo):` 3.1, `docs:` 4.x, `docs(add-analytics-read-model):` 5.1) with the agent trailer; every guard's commit body names its demonstrated failing input; the seed's commit body and `handoff.md` flag the security-relevant part (accounts created with generated passwords through the configured hasher; the environment guard). Verify: `git log --oneline main..HEAD`; `openspec validate add-analytics-read-model --strict` passes.
- [ ] 5.3 Green Actions run on the exact branch head before Gate 2: the user pushes `change/add-analytics-read-model`; the executor queries `https://api.github.com/repos/Azazu/linkboard/actions/runs?branch=change/add-analytics-read-model` for the head SHA until `status` is `completed` and `conclusion` is `success`; URL and SHA recorded in `handoff.md`. Verify: the run's `head_sha` equals `git rev-parse HEAD`.
- [ ] 5.4 `scripts/pregate-verify.sh gate2 add-analytics-read-model` passes; `scripts/gate-run.sh add-analytics-read-model 2 full`; findings handled via `/workflow:fix-findings` and confirmed with `scripts/gate-run.sh add-analytics-read-model 2 confirm <round>`. Verify: the last Gate 2 record in `review.md` reads `approved` or `confirmed`.

## Post-merge acceptance (not a Gate 2 task)

- After the merge: the user pushes `main`; the executor checks the `main` run via the API before proposing the archive; `/opsx:archive` syncs `analytics` (new), `demo-data` (new) and `links` (two modified requirements), and removes row 8 from `openspec/ROADMAP.md`.
