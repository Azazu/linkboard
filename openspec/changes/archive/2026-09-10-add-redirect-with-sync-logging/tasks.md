## 0. Gate 1 (high tier)

- [x] 0.1 `scripts/pregate-verify.sh gate1 add-redirect-with-sync-logging` passes; `scripts/gate-run.sh add-redirect-with-sync-logging 1 full`; findings fixed via `/workflow:fix-findings` and confirmed. Verify: the last Gate 1 record in `review.md` reads `approved` or `confirmed`.

## 1. Click write model: entity, migration, recorder

- [x] 1.1 `src/Click/Entity/Click.php` (read-only mapping, every §3.4 column, index `(link_id, occurred_at)` — ascending, a btree serves DESC by backward scan and Doctrine does not model sort order — partial index `WHERE NOT is_bot`, FK `ON DELETE CASCADE`), `src/Click/Visit.php` (readonly DTO: client IP, truncated UA, truncated Referer, occurredAt, isHead), `src/Click/RecordOutcome.php` (enum `Allowed|Exhausted`), `src/Click/ClickRecorderInterface.php`, `src/Click/Recorder/DbalClickRecorder.php` (one transaction: conditional `UPDATE links … WHERE click_count < max_clicks`, then `INSERT INTO clicks`), `src/Click/VisitorHasher.php` (`VISITOR_HASH_SALT`, empty refused), `src/Click/RefererHost.php` (moved here from the planned `src/Redirect/` — `referer_host` is a click column and Click must not depend on Redirect), binding in `config/services.yaml`. Verify: `make console ARGS='doctrine:schema:validate --skip-sync'` reports the mapping OK; `make console ARGS='debug:container App\\Click\\ClickRecorderInterface'` resolves to the DBAL recorder.
- [x] 1.2 `migrations/Version20260909123249.php` via `make migration` → review the generated class: table `clicks` per design decision 11, description, reversible `down()`; `make migrate`; `make test-db`. Verify: `make console ARGS='doctrine:migrations:migrate prev --no-interaction'` then `make migrate` both succeed; `make console ARGS='dbal:run-sql "\d clicks"'` (or the `pg_indexes` query) shows the FK with `ON DELETE CASCADE`, the composite index and the partial index.
- [x] 1.3 `.env`: `RATE_LIMIT_REDIRECT_PER_IP=60`, `VISITOR_HASH_SALT=<local default>` with a comment that production sets it in `.env.local`/CI; `.env.test`: fixed `VISITOR_HASH_SALT`. Verify: `make console ARGS='debug:dotenv'` lists both; `git diff` contains no real secret.
- [x] 1.4 `tests/Integration/Click/DbalClickRecorderTest.php` (+ `tests/Fixture/record-click.php` child-process script, `tests/Unit/Click/RefererHostTest.php`): (a) unlimited link → `allowed`, one `clicks` row with the expected columns, `click_count` 1; (b) link with `max_clicks` 1 and `click_count` 1 → `exhausted`, no row, count unchanged; (c) the insert and the increment are one transaction — with the INSERT made to fail (e.g. a duplicate click id supplied by the test) the `click_count` is unchanged; (d) 10 concurrent recordings on a fresh link with `max_clicks` 3 over separate connections → exactly 3 `allowed`, `click_count` 3 (runs outside the dama transaction: rows committed on an independent `DriverManager` connection, ten child processes, cleanup in `finally` — recorded in the test docblock). `tests/Unit/Click/VisitorHasherTest.php`: same IP+UA → same hash; different UA → different; different salt → different; 64 lowercase hex chars. Verify: `make test` green; removing the `WHERE click_count < max_clicks` clause makes 1.4(b) and 1.4(d) fail (failing input recorded in the commit body).

## 2. Redirect: resolver, UTM, controller, pages, rate limit

- [x] 2.1 `src/Redirect/UtmAppender.php` (design decision 6: raw pair processing, no `parse_str`), `src/Click/RefererHost.php` (decision 7, delivered in 1.1: same-origin via `APP_PUBLIC_URL`, column bounds → null), `src/Redirect/RedirectDecision.php` + `src/Redirect/RedirectStatus.php`, `src/Redirect/RedirectResolver.php` (decision 2 order: lookup — failure → `unavailable` per decision 13 — → 404 → 410 fast path → HEAD → record → 302/410/503 with the write-failure policy of decision 5 and its log records). Unit tests `tests/Unit/Redirect/UtmAppenderTest.php` (query preserved, UTM key replaced in every spelling incl. `utm%5Fsource`, repeated keys `tag=a&tag=b` kept, dotted key `a.b=1` kept, bracket notation `x%5By%5D=2` kept, encoded values kept verbatim, fragment kept, RFC 3986 encoding of a space and `&` in UTM values, no UTM → identical string, empty query, target with port), `tests/Unit/Click/RefererHostTest.php` (delivered in 1.4: lower-cased host, absent → null, garbage → null, own public host → null, 300-char host → null, invalid UTF-8 → null, control character → null, 255-byte host kept), `tests/Unit/Redirect/RedirectResolverTest.php` with an in-memory repository and a stub recorder (repository throws → `unavailable` and an `error` record without the slug value; unknown → notFound; inactive+expired → notFound; expired → gone; count ≥ max → gone without calling the recorder; HEAD → redirect without calling the recorder; recorder `exhausted` → gone; recorder throws → redirect for an unlimited link and `unavailable` for a limited one, with the logger asserting the record contains the link id and no IP/UA). Verify: `make test` green; replacing the pair-wise algorithm by `parse_str`/`http_build_query` fails the repeated-key and dotted-key cases (failing input recorded in the commit body).
- [x] 2.2 `src/Shared/Api/ProblemDetails.php` (decision 10) and `JwtProblemDetailsSubscriber` + `AuthRateLimitSubscriber` delegating to it. Verify: `make test` green (the existing 401/429 problem-details tests pass unchanged); `rg -n 'function problem\(' src/` shows a single definition.
- [x] 2.3 `config/packages/security.yaml`: `api` firewall pattern `^/api(/|$)`, access_control `^/health$` (decision 1; **security-sensitive**, named in the commit body); `config/packages/framework.yaml`: `redirect_ip` limiter (decision 9) and its `when@test` in-memory storage; `src/Redirect/RedirectRateLimit.php` + `src/Redirect/RateLimitVerdict.php` (fail-open wrapper: `allowed | limited(retryAfter)`; a limiter failure is logged at `warning` without the IP and reported as allowed); `src/Redirect/RedirectController.php` (route with requirements, methods GET/HEAD, `priority: -100`; rate-limit check first; header bounds; `Visit`; decision → response; `Cache-Control: no-store` on every response, `Referrer-Policy: no-referrer-when-downgrade` on the 302; problem details vs Twig by `Accept`); `templates/redirect/not_found.html.twig`, `gone.html.twig`, `rate_limited.html.twig`, `unavailable.html.twig` (`Retry-After: 5`). Verify: `make console ARGS='debug:router'` lists `redirect` last with `GET|HEAD` and the slug requirement, and `/health`, `/login`, /api/v1 still resolve to their own controllers; `make console ARGS='debug:config security firewalls.api.pattern'` prints `^/api(/|$)` (`debug:firewall` does not display patterns in this Symfony version); `make console ARGS='debug:container --tag=rate_limiter'` (or `debug:container limiter.redirect_ip`) shows the limiter; curl against the dev stack recorded in the commit body: 302 with headers, `-I` (HEAD) 302 without a new click, 404 HTML, 404 with `-H 'Accept: application/json'` → problem+json, 410 for an expired link, 61st request → 429 with `Retry-After`.

## 3. HTTP tests (Web suite)

- [x] 3.1 `tests/Web/Redirect/RedirectMatrixTest.php` (+ `tests/Web/Redirect/RedirectWebTestCase.php`, `tests/Factory/LinkFactory.php` states `withUtm`/`expiring`/`limited`): active link 302 with `Location` = target + UTM and no `Set-Cookie`; slug `api-promo` → 302 with no credentials and with `Authorization: Bearer not-a-token`, while `GET /api/v1/me` with the same bearer is 401 (firewall boundary; reverting the pattern to `^/api` fails this case — failing input recorded in the commit body); case-sensitive slug (only `Promo` exists → `/promo` 404); unknown 404; inactive 404; inactive + expired 404; expired 410; `maxClicks` 3 with 3 clicks 410; `maxClicks` 3 with 2 clicks 302 and then `clickCount` 3 via the API; headers (`Cache-Control: no-store` on 302/404/410, `Referrer-Policy` on 302); `Accept: application/json` → problem details 404/410 with `status` and `title`; `Accept: text/html` → HTML body; `HEAD` → same status and headers as `GET`, empty body, no click recorded; never 301 (`assertNotSame(301, …)` on every redirect). Verify: `make test` green; every scenario of spec `redirect` requirements 1–4 maps to an assertion (table in the commit body).
- [x] 3.2 `tests/Web/Redirect/ClickLoggingTest.php`: one redirect → one `clicks` row (columns exactly the twelve of §3.4; none contains the IP or the UA string) and `clickCount` 1 through `GET /api/v1/links/{id}`; 404/410 requests record nothing and keep `clickCount`; visitor hash stable for the same IP+UA and different for another UA; `Referer` → lower-cased host, absent → null, own public host → null; 8 KB UA and 4 KB Referer → 302 and one row; hostile referer hosts (300-char host, invalid UTF-8) on an unlimited link and on a link with `maxClicks` 5 → every response 302, one row each, `referer_host` null (removing the column-bound check in `RefererHost` makes the INSERT fail and this case fail — failing input recorded in the commit body); `DELETE /api/v1/links/{id}` on a link with 2 clicks → 0 rows for that link (cascade). Verify: `make test` green; every scenario of spec `click-logging` maps to an assertion (table in the commit body).
- [x] 3.3 `tests/Web/Redirect/RedirectDegradationTest.php` (`disableReboot`, stubs installed through the test container): (a) recorder throws → unlimited link 302 with no click row and an `error` log record with the link id and no IP/UA (Monolog test handler), limited link → 503 with `Retry-After: 5`; (b) `LinkRepositoryInterface` throws → 503 with `Retry-After: 5` for any slug and an `error` record without the slug value; (c) limiter failures, three stubs for `limiter.redirect_ip`: (c1) the factory throws on `create()`; (c2) a real `SlidingWindowLimiter` with a lock store that throws on `save()` (lock acquisition fails inside `consume()`); (c3) a real `SlidingWindowLimiter` with a storage that throws on `fetch()`/`save()` (storage fails inside `consume()`) — each → 302 (fail-open) and a `warning` record naming the failure class without the IP. `tests/Web/Redirect/RedirectRateLimitTest.php`: 61 requests from one IP → 60 matrix answers then 429 with `Retry-After` (HTML, and problem details with `Accept: application/json`); requests with different `X-Forwarded-For` values from an untrusted peer share one bucket. Verify: `make test` green; with the try/catch around record() removed (a) fails; with the lookup catch removed (b) fails; with the limiter wrapper's catch removed (c1)–(c3) fail; with only `consume()` moved outside the catch (c1) stays green while (c2) and (c3) fail — the reason the catch surrounds the whole expression; with the limiter check removed the rate-limit test fails (all failing inputs recorded in the commit body).

## 4. Docs

- [x] 4.1 `docs/how-to/local-development.md` (also the two stale "asynchronous" claims in Daily and Troubleshooting corrected): a "Redirect" section (curl -I of a short link, the 404/410/302 matrix in two lines, `RATE_LIMIT_REDIRECT_PER_IP`, `VISITOR_HASH_SALT` — set in `.env.local`, rotation note, never committed), and a one-paragraph baseline note: clicks are written synchronously on the hot path until `add-async-click-logging`, contrary to FR-RED-2, on purpose. Verify: re-read whole after the last edit; every documented command run in its exact form against the dev stack.
- [x] 4.2 Re-read `openspec/ROADMAP.md` row 5 and `docs/explanation/requirements.md` §7 row 5 against the delivered scope; edit only if a scope claim changed. Verify: `rg -n 'add-redirect-with-sync-logging' openspec/ROADMAP.md docs/explanation/requirements.md` shows the rows and they match.

## 5. Wrap-up

- [x] 5.1 `make check` green (cs, stan level 8, all suites); commits per block (`feat(click):` 1.x, `feat(redirect):` 2.x, `test(redirect):` 3.x, `docs:` 4.x) with the agent trailer; every commit body of a guard names its demonstrated failing input. Verify: `git log --oneline main..HEAD`.
- [x] 5.2 Green Actions run on the exact branch head before Gate 2: the user pushes the branch change/add-redirect-with-sync-logging; the executor polls the run list for the head SHA and the jobs endpoint until `detect`, `workflow` and `php` are `success`; URL and SHA recorded in `handoff.md`.
- [x] 5.3 `openspec validate add-redirect-with-sync-logging --strict` and `scripts/pregate-verify.sh gate2 add-redirect-with-sync-logging` pass; `scripts/gate-run.sh add-redirect-with-sync-logging 2 full`. Verify: no FAIL line.

## Post-merge acceptance (not a Gate 2 task)

- After the merge: the user pushes `main`; the executor checks the `main` run via the API before proposing the archive; `/opsx:archive` syncs specs `redirect` and `click-logging`.
