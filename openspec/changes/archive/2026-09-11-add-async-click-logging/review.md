# Review — add-async-click-logging

## Round 1 · Gate 1
**Reviewer:** codex
**Date:** 2026-09-10
**Reviewed-Commit:** 06256b3034d1f275a1db340bc0668090cbfc7c5f
**Verdict:** changes-requested

### Findings
| # | Severity | Location | Finding | Status |
|---|----------|----------|---------|--------|
| 1 | major | design.md decision 4; tasks.md 2.1, 3.3 | `UnrecoverableMessageHandlingException` prevents retries, but does not prevent parking: the installed `SendFailedMessageForRetryListener` declines retry and `SendFailedMessageToFailureTransportListener::onMessageFailed()` then sends the envelope to `failed`. Thus every deleted-link message violates the promised first-attempt acknowledgement/discard and produces failure logging. Catch the FK failure outside the rolled-back transaction, log the specified info record and return normally (or specify another verified discard mechanism). Require a real Worker/listener-path test proving acknowledgement, no retry and no failed message; the proposed direct bus `consumeAsync()` helper cannot verify that lifecycle and currently throws for this case. Reconcile the applicability table and handler assertions. | fixed |
| 2 | major | design.md decision 2 and Applicability / Empty/zero/null inputs; specs/redirect/spec.md / Click limit is exact under concurrency | Re-enabling a limit reuses a stale existing key, contrary to the claim that unlimited-to-limited is seeded on the next redirect. Example: counter and persisted count are 3; clear `maxClicks`, serve and persist 4 unlimited redirects (count 7, key still 3), then set `maxClicks` to 10. With the worker paused, the script permits 7 more redirects instead of 3, without any key loss. The current update processor only changes the SQL field, and the plan adds no transition mechanism. Specify how retained counters account for unlimited-period clicks, including queued clicks and concurrent limit changes, and add transition implementation and verification tasks. Any weaker guarantee must be explicit and reconciled with the requirement. | fixed |
| 3 | major | proposal.md item 2; design.md decision 2 and Risks; specs/redirect/spec.md / Click limit is exact under concurrency | The claimed overshoot bound of only the queue backlog at seeding time excludes redirects whose dispatch failed. For max 3, allow three redirects while the counter works but dispatch throws: key 3, persisted count 0, backlog 0. Lose the counter key and the next redirect seeds 0 and allows three additional redirects, exceeding the limit with no backlog. Crashes before dispatch and messages parked after permanent failure also leave increments outside the stated backlog. Define the actual bound in terms of unpersisted accepted increments (and repeated loss), or provide a mechanism that enforces the stronger claim; add a failure-plus-key-loss scenario and reconcile FR-RED-3 and the planned operational documentation. | fixed |
| 4 | major | tasks.md 3.3 / retry then park fallback | The optional fallback to testing `MultiplierRetryStrategy` and reading configuration does not verify the promised retry-to-failed lifecycle or demonstrate a failing input when failure routing is removed. Such a test can pass with missing Worker event subscribers or broken parking. Make a Worker test with the configured retry/failure listeners mandatory (in-memory transports can exercise the event flow without waiting for real delays), asserting four handler attempts, retry stamps/delays, one failed envelope and unchanged click data; require it to fail when routing to the failed transport is disabled. | fixed |
| 5 | minor | tasks.md 1.2 / migration verification | `make console ARGS='dbal:run-sql "\d messenger_messages"'` cannot inspect the table: `\d` is a psql meta-command, while the installed DBAL RunSqlCommand passes its argument to the database as SQL. Replace it with an executable catalog query or an explicit psql invocation before recording this task as verified. | fixed |

### Validation
Branch and HEAD match the requested review target; the worktree was clean before this record. Read the proposal, design, tasks, handoff, all three delta specs, repository rules/configuration, relevant current application code and installed Messenger/DBAL source. `scripts/pregate-verify.sh gate1 add-async-click-logging` passed, including strict OpenSpec validation. The high risk tier is appropriate. No implementation or git write commands were performed.

## Confirmation 1 · Gate 1 · Round 1
**Reviewer:** codex
**Date:** 2026-09-10
**Reviewed-Commit:** 1388466f44b204991e42b55cfd4e147cd57719c5
**Verdict:** changes-requested

### Findings
| # | Resolution |
|---|------------|
| 1 | confirmed — Decision 4 and task 2.1 catch the FK violation outside the rolled-back transaction and return normally. Decision 7 and task 3.3 now require a real Worker with the configured dispatcher, asserting first-attempt acknowledgement, no rejection/retry/parking and the info record; replacing the return with an unrecoverable exception must fail that test. The applicability table and delta agree. |
| 2 | changes-requested — The lift fixes the original fully-persisted sequential example, but the requested concurrent-transition semantics and verification remain missing (decision 2; redirect delta; tasks 1.1, 3.3). Example: ten requests load an unlimited link with count 0 and pause before recording; PATCH sets maxClicks to 3; three new requests seed the counter at 0 and consume its allowance; the ten old requests resume and all bypass the counter using their loaded null limit. There were no unpersisted accepted redirects at seeding, yet thirteen redirects are accepted without key loss. Similarly, in-flight requests retain an old higher maximum after a lowering. The script receives request snapshots, not the current SQL limit. Specify synchronization or explicitly scope the guarantee to the loaded snapshot and account for in-flight requests across transitions; reconcile the bound and add controlled-interleaving verification. Also add a re-enable scenario with unlimited-period messages still queued: the only new transition test consumes all four messages first. |
| 3 | confirmed — For the named dispatch-failure/key-loss defect, the delta and design now include all unpersisted accepted redirects (queued, dispatch-failed, parked and crashes before dispatch) and repeated loss. The concrete three-failed-dispatches/key-loss scenario is specified and required by task 3.3; task 4.2 reconciles FR-RED-3. Concurrent limit-transition accounting remains the separate unresolved finding 2 above. |
| 4 | changes-requested — The mandatory Worker test and failure-routing mutation resolve the configuration-only fallback, but the new exact-delay assertions are incompatible with the configured listener path (decision 7; tasks 1.2, 3.3). Installed FrameworkBundle Configuration defaults retry jitter to 0.1, and MultiplierRetryStrategy applies it with random_int; the plan changes only delay to 10, so DelayStamps can be 9–11, 18–22 and 36–44 ms, making assertions of exactly 10/20/40 flaky. Explicitly configure jitter 0 for exact delays, or specify and test the jitter ranges and reconcile the production backoff contract. Clarify stamp assertions too: SendFailedMessageToFailureTransportListener appends RedeliveryStamp(0) when parking, so retry count 3 must be checked in the retry events/history, not as the failed envelope's current retry count. Keep the four-attempt, one-parked-envelope and disabled-failure-routing assertions mandatory. |

### Validation
Reviewed only the requested commit diff and collateral relevant to Round 1 findings, including the current redirect/update paths and installed Messenger listener, retry-strategy and configuration source. Branch and HEAD match the requested target; all source-round findings were dispositioned and the worktree was clean. `scripts/pregate-verify.sh gate1 add-async-click-logging` passed, including strict OpenSpec validation. This confirms planning artifacts, not an implemented runtime. No unrelated findings were introduced; only `review.md` was modified and no git write commands were run.

## Confirmation 2 · Gate 1 · Round 1
**Reviewer:** codex
**Date:** 2026-09-10
**Reviewed-Commit:** f9241820fa4533a0674b17164ecea0d902b16541
**Verdict:** changes-requested

### Findings
| # | Resolution |
|---|------------|
| 1 | confirmed — Decision 4 and task 2.1 catch the FK violation outside the rolled-back transaction and return normally. Tasks 3.1/3.3 require the configured Worker event path, first-attempt acknowledgement, no retry/rejection/parking, the info record, and a failing test when the return is replaced by an unrecoverable exception. The deleted-link delta and applicability table agree. |
| 2 | changes-requested — Snapshot semantics and the queued-unlimited-period scenario now address the missing guarantee boundary, but task 2.2 still does not verify the specified concurrent transition: it executes all ten old-snapshot record() calls before PATCH, so it tests already-queued clicks rather than requests resuming after the new limit has been consumed. Retain the ten snapshots without recording, commit PATCH, run the three new-snapshot calls, then invoke the ten retained snapshots, as the delta scenario requires; explicitly consume all thirteen messages (handling only the named ten produces count 10, not 13). Reconcile the post-persistence assertions too: the unchanged RedirectResolver::isExhausted() returns 410 before calling the recorder at count 13/max 3 or count 14/max 10, so the delta scenarios and task 3.3 cannot assert a Redis lift on that next HTTP redirect. Assert the early 410 with the key unchanged and test the lift separately through the counter seam or after raising the maximum. Finally, carry the in-flight allowance into task 4.2's FR-RED-3 rewrite; it still specifies only unpersisted redirects at seeding. These are the remaining verification and reconciliation gaps of the same transition finding. |
| 3 | confirmed — The named failure-plus-key-loss case is specified and covered by task 3.3. The seeding bound includes queued, dispatch-failed, parked and pre-dispatch-crash increments and repeated key loss; task 4.2 requires replacing the old backlog-only FR-RED-3 claim. The remaining concurrent-transition reconciliation is tracked under finding 2. |
| 4 | confirmed — Decision 7, task 1.2 and the click-logging delta explicitly configure jitter 0, making the exact delay assertions compatible with the installed retry strategy. Task 3.3 checks counts 1/2/3 on WorkerMessageRetriedEvent envelopes and distinguishes the failed envelope's final parking stamp (0), matching the installed listeners. Four handler attempts, one failed envelope, unchanged click data and the disabled-failure-routing failing input remain mandatory. |

### Validation
Reviewed the diff from `06256b3034d1f275a1db340bc0668090cbfc7c5f` to `f9241820fa4533a0674b17164ecea0d902b16541` and collateral reachable from the named findings, including the redirect exhaustion fast path and installed Messenger retry/failure listeners and retry strategy. Branch and HEAD match the requested target; the worktree was initially clean and all source-round findings were dispositioned. `scripts/pregate-verify.sh gate1 add-async-click-logging` passed, including strict OpenSpec validation. This is a review of planning artifacts, not runtime implementation. Only `review.md` was modified; no git write commands were run and no unrelated findings were introduced.

Finding 2 has now failed two confirmations. Per AGENTS.md, stop the confirmation loop: split or reduce the change, or ask the user to arbitrate before another confirmation of that finding.

## Confirmation 3 · Gate 1 · Round 1
**Reviewer:** codex
**Date:** 2026-09-10
**Reviewed-Commit:** 43c8b486f1a5cd67733fc83c6825434de601ea45
**Verdict:** confirmed

### Findings
| # | Resolution |
|---|------------|
| 1 | confirmed — Decision 4 and task 2.1 catch the foreign-key violation outside the rolled-back transaction and return normally. Tasks 3.1/3.3 require the configured Worker event path to prove first-attempt acknowledgement, no rejection/retry/parking and the info record, including a failing input that replaces the return with an unrecoverable exception. The deleted-link delta and applicability table retain this contract. |
| 2 | confirmed — Task 2.2 now retains ten old unlimited snapshots without recording, commits the limit change, consumes the allowance with three fresh-snapshot calls, then records the ten retained snapshots and handles all thirteen messages. It also covers snapshots retaining a higher maximum after lowering. The queued-unlimited-period scenario remains covered. The redirect delta and task 3.3 now assert the persisted-count fast path's 410 with the Redis key unchanged; they verify the lift after raising the limit, while task 2.2 verifies it separately through the recorder seam. These assertions match the current RedirectResolver. Task 4.2 explicitly carries snapshot semantics and the in-flight allowance into FR-RED-3. The named transition verification and reconciliation gaps are resolved. |
| 3 | confirmed — The design and redirect delta account for all unpersisted accepted redirects at seeding, including queued, dispatch-failed, parked and pre-dispatch-crash cases, plus repeated key loss and the separately stated in-flight allowance. Task 3.3 retains the concrete three-failed-dispatches/key-loss scenario; tasks 4.1/4.2 cover operational documentation and replacement of the backlog-only FR-RED-3 claim. |
| 4 | confirmed — The configured Worker lifecycle test remains mandatory: four handler attempts, three retry events with counts 1/2/3 and exact 10/20/40 ms delays, one parked envelope and unchanged click data. Jitter is explicitly zero in every environment; retry-event assertions distinguish the failed envelope's final parking stamp of zero, matching the installed listeners and retry strategy. Disabling failure routing must fail the test. |

### Validation
Reviewed the diff from `06256b3034d1f275a1db340bc0668090cbfc7c5f` to `43c8b486f1a5cd67733fc83c6825434de601ea45` and collateral reachable from the named findings, including the redirect exhaustion fast path and installed Messenger retry/failure source. This third confirmation follows the user's explicit request after the prior stop; the handoff also records that authorization. Branch and HEAD match the requested target, the worktree was initially clean, and every source-round finding was dispositioned. `scripts/pregate-verify.sh gate1 add-async-click-logging` passed, including strict OpenSpec validation. This confirms planning artifacts, not runtime implementation. No unrelated findings were introduced; only `review.md` was modified and no git write commands were run.


## Round 1 · Gate 2
**Reviewer:** codex
**Date:** 2026-09-10
**Reviewed-Commit:** 5ab5f65c4ab37cdf76f3407cb5ae16bc62f89fcf
**Verdict:** changes-requested

### Findings
| # | Severity | Location | Finding | Status |
|---|----------|----------|---------|--------|
| 1 | major | migrations/Version20260910142101.php:27; design.md / Migration Plan; tasks.md 1.2 | The unconditional CREATE TABLE breaks upgrades of an existing database where the previous Doctrine failed transport already auto-created `messenger_messages`. This is a reachable baseline state even without routed messages: the old configuration defaults to `auto_setup: true`, and the installed transport's `getMessageCount()` / `findAll()` create a missing table through `executeQuery()` and `setup()` (for example when inspecting the failed queue). Task 1.2 and the implementation commit explicitly report encountering this state and manually dropping the table before generating the migration. That local workaround is absent from the delivered upgrade path; `make migrate` against such a database will fail with an existing-relation error. Make the migration safely adopt or reconcile the existing transport table while preserving its rows, and verify both a fresh installation and an upgrade from the auto-created schema with a retained message. Define the corresponding rollback behavior; do not require deleting the queue as an upgrade prerequisite. | fixed |
| 2 | major | src/Click/Recorder/MessengerClickRecorder.php:50; src/Click/Counter/RedisClickCounter.php:30; specs/redirect/spec.md:32; design.md decision 2 / Risks | Reseeding uses the count from the request's earlier SELECT, so the stated bound of accepted clicks not yet persisted at seeding time is too small even without a limit change. Controlled interleaving: max 3, counter 3, SQL count 0, three accepted messages queued; three additional requests load count 0 and pause before recording; the worker commits all three messages (SQL count 3, no unpersisted accepted clicks remain); the Redis key is lost; the paused requests resume with seed 0. The script permits all three, giving six accepted redirects although the stated allowance at reseeding is zero. The resolver's fast path already used the same stale snapshot and cannot prevent this. Account explicitly for clicks persisted after the seed snapshot was read (or change the mechanism to uphold the stronger bound), add this interleaving test, and reconcile the guarantee in the delta, proposal, design and requirements/how-to. Existing tests cover an already-stale key and unpersisted dispatch failures, but not a seed snapshot that becomes stale before key loss. | fixed |

### Validation
Reviewed the branch diff against `main`, the change artifacts and three delta specs, related redirect/link paths, test fixtures and coverage, and installed Messenger/Doctrine transport source. The high risk tier is appropriate and Gate 1's last record is confirmed. Branch and HEAD match the requested target; the worktree was initially clean. `git diff --check main...HEAD` passed. `scripts/pregate-verify.sh gate2 add-async-click-logging` passed strict OpenSpec validation and all artifact checks, but its `make check` invocation could not reach Docker: permission denied on `/var/run/docker.sock`, before the style checker or tests ran. Runtime tests and the migration reproducer were therefore not independently executed in this review. The artifacts report 501 tests / 6613 assertions and a green Actions run on a083523; the diff from that commit to the reviewed HEAD contains only tasks.md and handoff.md. These are recorded executor evidence, not an independently verified green run here. Findings above are derived from the checked-in code and installed transport implementation. Only review.md was modified; no git write commands were run.


## Confirmation 1 · Gate 2 · Round 1
**Reviewer:** codex
**Date:** 2026-09-10
**Reviewed-Commit:** e8b25534a169cc6fa0d2c815246327d3902b7b9f
**Verdict:** changes-requested

### Findings
| # | Resolution |
|---|------------|
| 1 | changes-requested — The original upgrade failure is addressed: the migration adopts the bridge-created table, preserves its rows and renames the existing index; the new integration test covers fresh installation and adoption with a retained row. However, the new preservation-on-rollback contract is unsafe under concurrent parking (`migrations/Version20260910142101.php`, `down()`). The `DO` block checks `count(*)` before acquiring the exclusive lock required by `DROP TABLE`. A transport transaction can have inserted a message without committing when the count sees zero; the DROP then waits for that transaction to commit and drops the table including the newly committed message. The initial PHP count does not prevent this either. Serialize the emptiness decision with writers (acquire the appropriate table lock before checking, within the same transaction), or keep the transport table on rollback, and add a controlled two-connection regression proving a concurrently parked message survives. Reconcile `specs/click-logging/spec.md` / "Migration is reversible", which still unconditionally requires the table to be gone after reverting, with the chosen preservation policy. These are collateral effects of the requested migration/rollback fix. |
| 2 | changes-requested — The new `LimitTransitionTest::testASeedSnapshotThatBecomesStaleBeforeKeyLossReseedsFromTheSnapshot` reproduces the named interleaving correctly, and the revised bound in the redirect delta, proposal, design risks, FR-RED-3 and how-to includes messages persisted after the snapshot read. Reconciliation remains incomplete: design decision 2 still states "The counter is never below the persisted count" and that still-queued unlimited-period redirects "are the only ones the lift can miss"; both contradict the stale-snapshot behavior now explicitly tested (after the first resumed call the key is 1 while SQL is 3, and already-persisted clicks are missed). Task 4.2 also still states the bound at seeding rather than at the snapshot read. Qualify the lift guarantee against the count in the loaded snapshot and replace the surviving queue-only/seeding-time claims across the current artifacts and counter documentation. The requested test and principal bound correction are present; the remaining objection is the same guarantee-reconciliation gap, not a new unrelated finding. |

### Validation
Reviewed only the diff from `5ab5f65c4ab37cdf76f3407cb5ae16bc62f89fcf` to `e8b25534a169cc6fa0d2c815246327d3902b7b9f` and collateral reachable from the two Gate 2 Round 1 findings, including the installed Doctrine transport insertion/schema path, migration tests, recorder/counter and snapshot reload paths, and related specification claims. Branch and HEAD match the requested target; the worktree was initially clean and both source-round findings were dispositioned as fixed. `git diff --check` for the requested range passed. `scripts/pregate-verify.sh gate2 add-async-click-logging` passed strict OpenSpec validation and artifact checks, but `make check` failed before running the style checker or tests because access to `/var/run/docker.sock` was denied. Runtime tests and the concurrent rollback interleaving were not independently executed; the rollback finding is derived from the SQL lock/check order and the transport's transactional INSERT path. The handoff reports 504 tests / 6674 assertions, which is executor evidence only. Only `review.md` was modified; no git write commands were run and no unrelated findings were introduced.

## Confirmation 2 · Gate 2 · Round 1
**Reviewer:** codex
**Date:** 2026-09-11
**Reviewed-Commit:** 94ce1385bdcc99c3f547ff5439967aa5c507008d
**Verdict:** confirmed

### Findings
| # | Resolution |
|---|------------|
| 1 | confirmed — The migration adopts the previous transport's auto-created table without deleting its rows, renames the bridge index, and creates the table/index when absent. `MessengerMessagesMigrationTest` covers fresh installation, adoption of the installed bridge's schema with a parked row, rollback and idempotent re-application. Rollback now unconditionally retains the table and performs no DDL or emptiness check, removing the concurrent-parking deletion race by construction; no writer interleaving can cause this down() to drop a message. The click-logging requirement and rollback scenario, design migration plan and task record agree with this preservation policy. |
| 2 | confirmed — `LimitTransitionTest::testASeedSnapshotThatBecomesStaleBeforeKeyLossReseedsFromTheSnapshot` retains count-zero snapshots, persists the three accepted messages, loses the key, and resumes the snapshots, demonstrating six accepted redirects for max 3 and exhaustion with a fresh snapshot. The redirect delta, proposal, design and risks, FR-RED-3 and how-to account for clicks persisted after the seed snapshot was read. Design decision 2 now qualifies the lift against the calling request's loaded count and removes the queue-only claim; task 4.2 and the counter documentation use the same snapshot-read boundary. The named guarantee-reconciliation gap is resolved. |

### Validation
Reviewed only the diff from `5ab5f65c4ab37cdf76f3407cb5ae16bc62f89fcf` to `94ce1385bdcc99c3f547ff5439967aa5c507008d` and collateral reachable from the two Gate 2 Round 1 findings, including the previous confirmation, installed Doctrine transport schema, migration tests, recorder/counter and snapshot reload paths, and related claims across current artifacts. Branch and HEAD match the requested target; the worktree was initially clean and both source-round findings were dispositioned as fixed. `git diff --check` for the requested range passed. `scripts/pregate-verify.sh gate2 add-async-click-logging` passed strict OpenSpec validation and artifact checks, but its `make check` failed before the style checker or tests ran because access to `/var/run/docker.sock` was denied. Runtime tests were not independently executed; this confirmation rests on source inspection and the checked-in regression scenarios, and does not certify a green check run or waive the merge prerequisites. Only `review.md` was modified; no git write commands were run and no unrelated findings were introduced.
