# Tasks — add-web-ui

High tier (a second authenticated surface, the ownership boundary on new pages, and the response hardening of NFR-SEC-5 — AGENTS.md): Gate 1 on the artifacts and Gate 2 on the code, with a demonstrated failing input for every new guard. Work on the change branch; `scripts/workflow-verify.sh apply add-web-ui` first. Every task touching authorization or the policy is flagged in its commit body.

## 0. Gate 1 (high tier)

- [x] 0.1 `scripts/pregate-verify.sh gate1 add-web-ui` passes and `scripts/gate-run.sh add-web-ui 1 full` is run on the completed artifacts (proposal with tier and Non-goals, the `web-ui` delta, design with the applicability table, tasks); findings via `/workflow:fix-findings`, confirmation with `scripts/gate-run.sh add-web-ui 1 confirm <round>`. Verify: review.md carries a Gate 1 round bound to the requested commit; the merge verifier checks that the last Gate 1 decision reads approved/confirmed.

## 1. Dependencies and the asset pipeline

- [x] 1.1 Add the four production packages in their exact form — `make composer ARGS='require symfony/asset-mapper symfony/stimulus-bundle symfony/ux-turbo symfony/ux-chartjs'` — and review what Flex writes: `config/bundles.php`, `config/packages/asset_mapper.yaml`, `importmap.php`, `assets/`. If **symfony/ux-chartjs** cannot resolve against Symfony 8.1, drop it and keep the chart as a local Stimulus controller over the same import-mapped Chart.js (design decision 5's fallback), recording the resolver's message in the commit body. Vendor the front-end files with `make console ARGS='importmap:require @hotwired/turbo @hotwired/stimulus chart.js @picocss/pico'` (the exact packages the entry point imports) and commit `assets/vendor/`. Verify: `make console ARGS='debug:config framework asset_mapper'` shows the pipeline; `make console ARGS='importmap:audit'` reports no vulnerability; `tests/Unit/ProductionDependenciesTest.php` stays green (it fails if a package used by `src/` sits in require-dev); `rg -n 'cdn|http' importmap.php` shows no remote URL at runtime.
- [x] 1.2 `assets/app.js` (starts Stimulus and Turbo), `assets/styles/app.css` (imports Pico and adds the few project rules), `assets/controllers/clipboard_controller.js`, `assets/controllers/rules_editor_controller.js`; the import map's polyfill switched off in `config/packages/asset_mapper.yaml` (design decision 5). Verify: `make console ARGS='asset-map:compile'` succeeds and `rg -n 'polyfill' config/packages/asset_mapper.yaml` shows it disabled; a later page test asserts the page references only same-origin assets.

## 2. The shell and response hardening

- [ ] 2.1 `templates/base.html.twig` rebuilt (design decision 11): `lang`, a viewport meta, the navigation (dashboard, links, API keys, log out — or log in and register for a guest), a flash region with `role="alert"`, a footer linking `/api/docs` (FR-WEB-4), `{{ importmap('app', {nonce: csp_nonce()}) }}`, and no `cdn.jsdelivr.net` block; `templates/form/pico_layout.html.twig` (a form theme whose errors carry `role="alert"`) registered in `config/packages/twig.yaml`; `templates/security/login.html.twig`, `register.html.twig` and `templates/home/index.html.twig` moved into the shell. Verify: `make console ARGS='lint:twig templates'`; the existing `tests/Web/Security/LoginTest.php` and `RegistrationTest.php` pass unchanged (the selectors they assert still exist).
- [x] 2.2 `src/Web/Http/CspNonceListener.php` (on `kernel.request`, 16 bytes from `random_bytes`, base64, onto the request attributes), `src/Web/Http/CspNonce.php` (reads it for the Twig function), `src/Web/Http/CspTwigExtension.php` (`csp_nonce()`), `src/Web/Http/SecurityHeadersSubscriber.php` (on `kernel.response`: `X-Content-Type-Options` on every response; `X-Frame-Options: DENY` and `Referrer-Policy: strict-origin-when-cross-origin` on every HTML response; the policy of design decision 6 on every HTML response except those under `^/api(/|$)` — the segment boundary, so `/api-keys` keeps its policy), and the session cookie flags in `config/packages/framework.yaml` (`cookie_httponly`, `cookie_samesite: lax`, `cookie_secure: auto`). `tests/Web/Http/SecurityHeadersTest.php`: `/dashboard`, `/links`, `/api-keys` and the page that shows a newly created key each carry the four headers; the policy names `'self'` and no other origin and forbids framing and a foreign base URI; two requests carry different nonces and each response's inline script carries its own; `/api/v1/links` carries `nosniff`; `/api/docs` carries `nosniff`, `X-Frame-Options` and a referrer policy and no content security policy (design decision 6's stated gap, and the only one). `tests/Web/Http/SessionCookieTest.php`: the session cookie is `HttpOnly` and `SameSite=Lax`. Verify: `docker compose exec -T php vendor/bin/phpunit --filter 'SecurityHeaders|SessionCookie'` green; failing inputs in the commit body: the policy removed → the header test fails; the nonce made a constant → the two-response test fails; `X-Frame-Options` dropped → its assertion fails; the exclusion widened from `^/api(/|$)` to `^/api` → the `/api-keys` assertion fails.

## 3. One authority for link writes

- [x] 3.1 `src/Link/UseCase/NewLink.php`, `src/Link/UseCase/Change.php` and `src/Link/UseCase/LinkChanges.php` (design decision 2: per field, *leave it* or *set it to this*, null being a legal new value), `src/Link/UseCase/SlugTaken.php`, `src/Link/UseCase/CreateLink.php` (the slug loop, `MAX_ATTEMPTS`, manager reset, `SlugTaken` for a client slug), `src/Link/UseCase/UpdateLink.php` (apply, flush, `ReportCache::forgetLink`, admin audit), `src/Link/UseCase/DeleteLink.php` (remove, flush, counter forget, `forgetLink` + `forgetGlobal`, admin audit). `tests/Unit/Link/UseCase/LinkChangesTest.php`: *leave* leaves, *set to null* clears, *set to a value* sets. Verify: `docker compose exec -T php vendor/bin/phpunit --filter LinkChanges` green.
- [x] 3.2 `src/Link/Api/CreateLinkProcessor.php`, `UpdateLinkProcessor.php` and `DeleteLinkProcessor.php` reduced to adapters over the use cases: they keep the raw-body concerns (`RulesInput`, the presence map), translate `SlugTaken` into the 422 `ValidationException` the API returns today, and render `LinkResource`. Verify: `docker compose exec -T php vendor/bin/phpunit tests/Api/Link` green **without editing a single test** — the refactor's whole claim is that the API's behaviour did not change; `git diff --stat tests/Api` shows no change in that directory.

## 4. The owner-facing pages

- [ ] 4.1 `src/Analytics/Query/OwnerDashboardQuery.php` and `src/Analytics/Dto/OwnerTotals.php` (design decision 4: `totals()` and `daily()`, joined to `links` on `owner_id`, bots excluded through `Sql::bots()`, days zero-filled by `generate_series`). `tests/Integration/Analytics/OwnerDashboardQueryTest.php` against real PostgreSQL: two owners' clicks stay apart, a bot click is excluded unless asked for, an empty day is a zero, the period boundaries are half-open. Verify: `docker compose exec -T php vendor/bin/phpunit --filter OwnerDashboardQuery` green; the `EXPLAIN` of both statements recorded in the commit body, and an index migration added only if the plan asks for one.
- [ ] 4.2 `src/Web/Dashboard/DashboardController.php` and `templates/dashboard/index.html.twig`: the owner's totals, the clicks-per-day chart (the chart bundle's builder, or the fallback controller of task 1.1), and the ten most recent links. `config/packages/security.yaml`: `access_control` refuses `^/(dashboard|links|api-keys)(/|$)` to anonymous users — the segment boundary of design decision 3. Verify: `tests/Web/Dashboard/DashboardTest.php` — the owner's figures count only their links, a second owner's clicks are absent, a guest is redirected to `/login`; `tests/Web/Link/PrefixSlugTest.php` — a guest requesting `/dashboard-sale`, `/links-promo` and `/api-keys-promo`, each an existing link's slug, gets that link's redirect, no login redirect and no session cookie, and the same paths for an unknown slug give the redirect capability's 404 page; failing inputs in the commit body: the `owner_id` predicate dropped from the query → the two-owner case counts both; the `(/|$)` dropped from the pattern → the prefix-slug test lands on `/login`.
- [ ] 4.3 `src/Web/Link/LinkListController.php` and `templates/link/index.html.twig`: `LinkListQuery` from the query string (active state, slug fragment, order), pagination of 30 with page links, empty state. Verify: `tests/Web/Link/LinkListTest.php` — filters and ordering change the rows, another owner's link never appears, page two holds the rest, a guest is redirected; failing input: the `findPageForOwner` call replaced by `findPage` → the stranger's link appears.
- [ ] 4.4 `src/Web/Link/LinkCreateController.php`, `src/Web/Link/LinkType.php`, `src/Web/Link/LinkFormData.php` (the form DTO of design decision 8, carrying the API's constraints plus `Slug` and `TargetUrl`), `templates/link/new.html.twig`. Verify: `tests/Web/Link/LinkCreateTest.php` — a valid submission creates the link and answers 303 to its page; an invalid target, a taken custom slug, a bad UTM member and a missing CSRF token each answer 422 (or refuse) and create nothing; the created link is what `GET /api/v1/links/{id}` returns for its owner; failing input: `SlugTaken` swallowed → the taken-slug case creates a second link or 500s.
- [ ] 4.5 `src/Web/Link/LinkShowController.php`, `src/Web/Link/LinkQrController.php` (`QrCodeRenderer` + `QrFormat` + the API's `private, max-age=86400`) and `templates/link/show.html.twig` (details, the short URL with the clipboard controller, the QR image, the delete confirmation form). Verify: `tests/Web/Link/LinkShowTest.php` — the owner sees the link, an administrator sees another user's, a stranger and an unknown id both get an identical 404 **while `GET /api/v1/links/{id}` for that same stranger stays the 403 the `links` capability requires** (design decision 3), the QR route answers `image/svg+xml` and `image/png`; failing input: the voter call removed → the stranger case returns 200.
- [ ] 4.6 `src/Web/Link/LinkEditController.php`, `src/Web/Link/RulesType.php`, `templates/link/edit.html.twig` (target, expiry, click limit, UTM, active state, the structured rules editor and the raw-JSON fallback), and `src/Web/Link/RuleViolationMapper.php` (design decision 8: `RuleViolation::$path` → a form error path). `tests/Unit/Link/RuleViolationMapperTest.php` for the mapping. Verify: `tests/Web/Link/LinkEditTest.php` — a change of target and an emptied expiry do exactly that and leave slug, click count and creation time alone; an invalid rules document answers 422, keeps the stored rules and shows a message per violation path; a valid document is stored in canonical form; a stranger gets 404; failing inputs: the parser's violations ignored → the invalid document is stored; the voter call removed → the stranger edits another user's link.
- [ ] 4.7 `src/Web/Link/LinkDeleteController.php`: a confirmed, CSRF-protected POST calling `DeleteLink`. Verify: `tests/Web/Link/LinkDeleteTest.php` — the link is gone from the list and its page is 404 afterwards, a GET to the same path deletes nothing, a stranger gets 404; failing input: the method restriction removed → the GET deletes.
- [ ] 4.8 `src/Web/ApiKey/{ApiKeyListController,ApiKeyCreateController,ApiKeyRevokeController}.php`, `src/Web/ApiKey/ApiKeyFormData.php` and `templates/api_key/index.html.twig`: create through `CreateApiKeyProcessor::create()`, show the plaintext once through a flash rendered inside an element marked `data-turbo-temporary`, on a page carrying `<meta name="turbo-cache-control" content="no-cache">`, answered with `Cache-Control: no-store`, and wired to a Stimulus controller that empties that element when the browser restores the document from history (design decision 9a), list the owner's keys (revoked and expired included, newest first), revoke through a confirmed POST. An owner-explicit `revoke(User $owner, Uuid $id): void` added beside the HTTP method of `src/Auth/Api/ApiKeys/RevokeApiKeyProcessor.php`, as the create processor already has. Verify: `tests/Web/ApiKey/ApiKeyPageTest.php` — the plaintext appears once and never again; the page that showed it carries `data-turbo-temporary` on that element, the cache-control meta tag, `Cache-Control: no-store` and the controller that clears it on a restored document; the cap answers with a message rather than a stack trace; another user's key is neither listed nor revocable (404) and still authenticates; a guest is redirected; failing inputs: the owner scope dropped from the list query → another user's key is listed; `data-turbo-temporary` or the meta tag removed → the markup assertions fail (the browser-level demonstration is task 4.9).

- [ ] 4.9 Browser acceptance of the secret's handling — the part no `WebTestCase` can reach (design decision 9a). `tests/Acceptance/api-key-back-navigation.mjs` drives the running dev stack in headless Chromium (`/usr/bin/chromium-browser` through `puppeteer-core`, installed in a scratch directory outside the repository so no Node toolchain enters it — verified working on 2026-09-13 with Chromium 152): sign in, create a key, then (a) navigate away and back within the application and (b) navigate away as a full document load and press Back, reporting for each whether the plaintext is in the restored DOM. `docs/how-to/local-development.md` carries the exact command and the expected output. Each case uses a freshly created key, keeps the `Cache-Control: no-store` header, and reports whether the browser restored the document or refetched it. Verify: the run's output recorded in the commit body and in `handoff.md`, and two negative runs, each matched to the restoration it governs (confirmation 2 of round 1): **(a) Turbo restoration with both `data-turbo-temporary` and the `turbo-cache-control` meta removed** — removing only the first proves nothing, since a `no-cache` page is fetched over the network on a restoration visit; **(b) a full-document restoration with only the `pageshow` clearing removed**, the run recording that a persisted restoration actually occurred (`event.persisted`, or no network request for that URL on the way back) — if the browser refetched instead, the case is recorded as **not exercised**, never as a pass. Both negative runs must show the plaintext coming back; that is the failing input a markup assertion cannot be. This is a local acceptance step, deliberately outside `make check`: CI has no browser, and the task says so rather than pretending the suite covers it.

## 5. Docs

- [ ] 5.1 `docs/how-to/local-development.md`: a "Using the web UI" section (the pages, the demo login from `app:demo:seed`, what needs JavaScript and what does not, the policy and the one documentation-page exclusion, the browser acceptance run of task 4.9 with its exact command and expected output, how to regenerate the vendored assets); `docs/explanation/requirements.md`: the FR-WEB rows and §7 row 11 brought in line with what shipped. Verify: every documented command run in its exact form; both files re-read whole after the last edit; `rg -n 'importmap:require|asset-map:compile' docs` shows the commands that exist.

## 6. Wrap-up

- [ ] 6.1 `make check` green (cs, stan level 8, all suites); commits per block (`feat(web):`, `refactor(link):`, `feat(analytics):`, `test(web):`, `docs:`) with the agent trailer; every guard's commit body names its demonstrated failing input; the authorization and policy parts flagged in the commit bodies and in `handoff.md`. Verify: `git log --oneline main..HEAD`; `openspec validate add-web-ui --strict` passes.
- [ ] 6.2 Green Actions run on the exact branch head before Gate 2: the user pushes the change branch; the executor queries `https://api.github.com/repos/Azazu/linkboard/actions/runs?branch=change/add-web-ui` until the run for `git rev-parse HEAD` is `completed` / `success`; URL and SHA recorded in `handoff.md`. Verify: the run's `head_sha` equals the branch head.
- [ ] 6.3 `scripts/pregate-verify.sh gate2 add-web-ui` passes and `scripts/gate-run.sh add-web-ui 2 full` is run; findings via `/workflow:fix-findings`, confirmation with `scripts/gate-run.sh add-web-ui 2 confirm <round>`. Verify: review.md carries a Gate 2 round bound to the requested commit; the merge verifier checks that the last Gate 2 decision reads approved/confirmed and is fresh.

## Post-merge acceptance (not a Gate 2 task)

- After the merge: the user pushes `main`; the executor checks the `main` run via the API before proposing the archive; `/opsx:archive` syncs the new `web-ui` capability and removes row 11 from `openspec/ROADMAP.md`.
