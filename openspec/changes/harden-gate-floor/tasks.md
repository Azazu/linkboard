# Tasks — harden-gate-floor

Tier `high`: this change edits `phpstan.dist.neon` and
`.github/workflows/ci.yml`, the floor and the verifier every later change is
judged by, which `AGENTS.md` names as a trigger. Gate 1 on the artifacts before
any implementation, Gate 2 on the diff, and a demonstrated failing input for
every new check.

`docker-compose.yml`, `.env` and the migrations themselves stay untouched: the
round trip exists to test the migrations as they are, and a change that edited
them to make its own check pass would prove nothing.

## 1. Gate 1

- [x] 1.1 Request Gate 1 on the artifacts (`scripts/gate-run.sh harden-gate-floor 1 full`) and disposition every finding before section 2 starts. Verify: the last Gate 1 record in `review.md` reads `confirmed` with no finding row left `open` — round 1 raised three major and one minor, Confirmation 1 returned finding 2, and Confirmation 2 (`6cf163d`) confirmed all of them.

## 2. The typed row reader, and `src` at level 9

- [x] 2.1 `src/Shared/Db/Row.php`: `int()`, `float()`, `string()`, `nullableString()` and `nullableFloat()` read one column of an `array<string, mixed>` row, accept the shapes PDO returns for PostgreSQL (int or decimal string for an integer, float/int/numeric string for a number, string for text, `null` only where the signature allows it) and throw `App\Shared\Db\UnexpectedColumnValue` naming the column and the type received for anything else (design decision 2). Verify, executed: `tests/Unit/Shared/Db/RowTest.php` — 21 tests, 58 assertions — covers each accepted shape and 16 rejected ones, including `int` of `null` (the cast that used to return `0`), `int` of `'1.5'`, `string` of an `int` (no silent formatting), and a missing column through every reader; the message assertions pin the column name, and one case pins that a present-but-null column is a different message from a missing one.
- [x] 2.2 The five classes of `src/Analytics/Query/` read every row value through the reader instead of casting; `BreakdownQuery` gained a `total()` helper so that "no rows" is the one place a zero is the answer rather than a cast over a missing column. Verify, executed: `vendor/bin/phpstan analyse --level=9 src/Analytics` reports `No errors`, and `tests/Api/Analytics tests/Integration/Analytics` pass unchanged (66 tests, 663 assertions with the reader's own tests) — the SQL is untouched, so an unchanged number means the reading kept its meaning.
- [x] 2.3 The remaining `src` findings, each a narrowing at the boundary rather than a cast: `src/Shared/Db/OneResult.php` narrows what `getOneOrNullResult()` returns for the two Doctrine repositories (a runtime check, so it holds in production too, not a docblock assertion); the three API Platform sites narrow `$context['request']` with `instanceof Request`; `ListQueryFactory` refuses an `order` whose key is not a field name or whose value is not a word instead of stringifying it; `LinkPages`' three form parameters are typed `FormInterface<LinkFormData>` instead of `<mixed>`; `RulesDocumentMapper` builds a `list<string>|null` and falls back to raw mode; `DemoSeedCommand` throws on a link id that is not a string. Verify, executed: `vendor/bin/phpstan analyse --level=9 src` reports `No errors`; `tests/Unit/Shared/Db/OneResultTest.php` and `tests/Unit/Web/Link/RulesDocumentMapperTest.php` cover the two new behaviours, `tests/Api/Link/ListLinksTest.php` gains `order[0]=desc` and `order[createdAt][]=desc` as 400s, and the whole suite passes (878 tests, 11 182 assertions).
- [x] 2.4 Nothing was silenced. Verify, executed: `git diff main -- src/` contains no added `@phpstan-ignore`, `@psalm` or `@phpstan-var` line, and `phpstan.dist.neon` still carries exactly the one `ignoreErrors` entry it had before this change (the Symfony 8 reflection hook on `src/Kernel.php`).

## 3. The typed JSON accessor, and `tests` at level 9

- [x] 3.1 `tests/Support/Json.php`: `decode()`/`decodeList()` for a body and `string()`, `int()`, `float()`, `bool()`, `nullableString()`, `map()`, `items()` and `objects()` for one field, each asserting through `PHPUnit\Framework\Assert` so a missing or wrong-typed field fails naming the field (design decision 3). Verify, executed: `tests/Unit/Support/JsonTest.php` — 13 tests, 142 assertions — checks every reader's return type and nine failure modes, each asserting the message names the field (`"nope" is present`, `"tags" is an object, not a list`), plus a body that is a list, a body that is a bare string and a response with no body at all. No `assert()` and no `@phpstan-var`: the two readers that cannot be expressed as a single PHPUnit assertion narrow with `Assert::fail()`, which returns `never`.
- [x] 3.2 `tests/Api` converted (157 findings, 21 files). The document-walking tests needed path readers — `mapAt()`, `listAt()`, `objectsAt()`, `hasAt()` — because `$operation['responses']['429'] ?? null` is a chain of `mixed`, and two more support readers came out of it: `Json::column()` for the string column these tests compare against a list, and `tests/Support/Env.php` for `$_SERVER['REDIS_URL'] ?? $_ENV['REDIS_URL']`, where the cast hid the one case worth knowing about — the variable not being set at all. `Row::toInt()`/`toFloat()`/`toString()` read `fetchOne()`, which is `mixed` for the same reason a row is. Verify, executed: `vendor/bin/phpstan analyse --level=9 tests/Api` reports `No errors` and the suite passes — 236 tests, 13 436 assertions.
- [x] 3.3 `tests/Integration` converted (70 findings). The YAML wiring test reads `config/packages/monolog.yaml` through the same path readers — a parsed YAML document is the same `array<array-key, mixed>` a decoded JSON one is — and `Row::toStrings()` reads a whole `fetchFirstColumn()`. Verify, executed: `vendor/bin/phpstan analyse --level=9 tests/Integration` reports `No errors` and the suite passes (121 tests, 937 assertions). Note on the commits: the `fetchOne()` and environment-variable sweeps of task 3.2 already touched files in this group and in `tests/Web`, so the group commits are not disjoint by file — each is still one reviewable step with `make check` green.
- [x] 3.4 `tests/Unit` converted (49 findings, all in `tests/Unit/Link/Rules/RulesSchemaParityTest.php`, which walks the published JSON schema). Verify, executed: `vendor/bin/phpstan analyse --level=9 tests/Unit` reports `No errors` and the suite passes (368 tests, 5 462 assertions).
- [x] 3.5 `tests/Web` and `tests/Support` converted (18 findings). `ProdHealthRequest` — the prod-kernel request fixture — now reads its subprocess's JSON payload field by field instead of casting five of them, and `SpyLogger` records a non-string level as its type rather than stringifying it. Verify, executed: `vendor/bin/phpstan analyse --level=9 tests` reports `No errors` for the whole tree, and the suite passes.
- [x] 3.6 The conversion changed no assertion's meaning, and the numbers are explained rather than expected to match (Gate 1 round 1, finding 4). Verify, executed and recorded: **before** the conversion (commit `c1b82d4`, the accessor and its own test) `make test` reported **891 tests, 11 324 assertions**; **after** it reports **895 tests, 21 768 assertions** (Unit 368/5 462, Integration 121/937, Api 236/13 686, Web 170/1 683). The four added tests are three cases of `tests/Unit/Support/JsonTest.php` (the path readers, an absent step, a JSON object with numeric member names) and one of `tests/Unit/Shared/Db/RowTest.php` (the value readers); the assertion delta is the accessor asserting as it reads across **169 call sites**, most of them inside loops over the OpenAPI document — which is why `tests/Api` carries almost all of it. The original assertions were checked directly rather than inferred from the totals: `git diff main -- tests/` removes 152 assertion lines, and each is either the same assertion reading through an accessor (same expected value, same message) or a type guard the accessor now performs — 5 `assertIsString($token)`, 6 `assertIsArray($decoded)`, 3 `assertIsArray($problem['violations'])`, 2 `assertIsArray($paths)`.

## 4. The level

- [x] 4.1 `phpstan.dist.neon` moves to `level: 9` over `src` and `tests`, with no baseline file and no new `ignoreErrors` entry (design decision 1), and the reason for the number is a comment beside it. Verify, executed: `make stan` reports `No errors` at the new level; `phpstan-baseline.neon` does not exist; the config's only `ignoreErrors` entry is the one it had before this change (the Symfony 8 reflection hook on `src/Kernel.php`).

## 5. The migration round trip

- [x] 5.1 `scripts/migrations-roundtrip.sh`: the ten steps of design decision 4 against a scratch database named for the configured one plus `_roundtrip_` and eight random hex characters, POSIX `sh`, printing the name before creating it. Verify, executed: `sh -n scripts/migrations-roundtrip.sh` passes (CI's `workflow` job runs that over every script), and the run against the real database is recorded in task 5.4.
- [x] 5.2 Ownership comes from an exclusive create and from asking which database the migrations reached, not from a check or from string surgery (Gate 1 round 1, finding 2 and its confirmation; Gate 2 round 1, finding 1). The scratch database is taken with `CREATE DATABASE`, which fails atomically on a name that already exists — measured, the second one exits `7` with `already exists`; a failed create aborts before any migration and any drop, and the `trap` drops exactly the database whose create succeeded in this process. The scratch URL is built with any `dbname` query parameter **dropped**, because DBAL merges the query over the path — measured on the installed `DsnParser`: `…/app_roundtrip_abc?dbname=app` resolves to `dbname=app` — and before a single migration runs the connection is asked `SELECT current_database()`, which must answer with this run's own database. Verify: demonstrated in tasks 7.5, 7.6, 7.7 and 7.10.
- [x] 5.3 The fingerprint SQL lives in `scripts/schema-fingerprint.sql` — one sorted listing of every column with its **complete** type (`format_type(atttypid, atttypmod)` from `pg_attribute`, so the modifiers are in it), its nullability and its default, every index definition and every constraint definition, written against `current_schema()` so it can be pointed at any schema — and the shell script reads that file rather than carrying its own copy. Verify, executed: `rg -n 'pg_attribute|information_schema' scripts/` shows the query in exactly one file, `tests/Integration/Db/SchemaFingerprintTest.php` reads the same file through `file_get_contents`, and the listing prints `character(64)` and `timestamp(0) with time zone` rather than `character` and `timestamp with time zone`. The console prints it through `dbal:run-sql --force-fetch` with `COLUMNS=4000`, because the table formatter would otherwise wrap a long definition across two lines and corrupt the listing.
- [x] 5.4 `make migrations-roundtrip` runs it through the usual `EXEC` indirection (the container locally, natively in CI). Verify, executed and recorded: against the real database the target reports `the connection resolves to <scratch>`, `up: 77 schema objects`, `down: only the declared survivors remain (doctrine_migration_versions messenger_messages)` and `the round trip reproduced the schema exactly`, and drops its scratch database — `SELECT datname FROM pg_database WHERE datname LIKE '%roundtrip%'` is empty afterwards. Two runs in a row both pass on different names (`linkboard_roundtrip_09046a8b`, `linkboard_roundtrip_8731297b`). A leftover from an interrupted run (`linkboard_roundtrip_deadbeef`, created by hand as a killed run would leave it) changes nothing: the next run passes on its own name and the leftover is still there afterwards, untouched, for a human to remove.
- [x] 5.5 The tables that survive a full `down` are declared, not assumed empty: `doctrine_migration_versions` and `messenger_messages`, the latter with the reason its migration gives (design decision 4). Verify: the script names both in `SURVIVORS` with the reason as a comment and fails on any other survivor — demonstrated in task 7.3.

## 6. CI

- [x] 6.1 A `migrations` job in `.github/workflows/ci.yml` with its own `postgres` service (and a `redis` one, because the console boots the kernel), running `make migrations-roundtrip EXEC=`, separate from the `php` job whose database the health tests depend on (design decision 5). Verify: the workflow parses (`yaml.safe_load`), and the job appears as its own check on the branch run, green, with the run id and SHA recorded in `handoff.md` by task 9.2.
- [x] 6.2 The job does not disturb the existing ones. Verify, executed: `git diff .github/workflows/ci.yml` removes no line — the change is one added job — so `workflow`, `detect` and `php` are byte for byte what they were; the branch run showing all four green is task 9.2's evidence.

## 7. The demonstrated failing inputs

Two layers, because one cannot do the other's job (design decisions 4a and 6):
the stub suite proves the script's control flow, and a real-PostgreSQL test
proves the fingerprint SQL sees what the listing promises.

- [x] 7.1 `scripts/migrations_roundtrip_test.sh` in the shape `scripts/gate_run_test.sh` established: a throwaway repository, a fixture `bin/console` on the script's path — which records the `DATABASE_URL` of every invocation and answers `current_database()` the way DBAL resolves it, query over path — one case per rule, no database. Verify, executed: the suite reports `56 passed, 0 failed`, CI's `workflow` job already runs every `scripts/*_test.sh`, and the suite is **not vacuous**: six mutations of the real script were executed and each turns it red — dropping the `created` guard from the cleanup (28/2 at the time), removing the unexpected-survivors check (27/3), removing the `diff` comparison (27/3), keeping the `dbname` parameter (53/3), removing the effective-database check (50/6) and ignoring the console's status again (50/6); restored, 56/0.
- [x] 7.2 A second listing that differs fails the round trip and the message names the differing line. Verify, executed: the fixture returns an extra index on its second listing; the suite asserts a non-zero exit, that the output carries `differs from the schema before it` and that the diff names `idx_widgets_label`. This case proves the comparison, **not** the SQL — task 7.8 is what proves the SQL.
- [x] 7.3 An unexpected surviving table fails the round trip. Verify, executed: the fixture leaves `leftovers` standing; the suite asserts the failure says `a full down left tables the declared set does not name` and names `leftovers`.
- [x] 7.4 A failing `down` fails the round trip rather than being skipped, and the scratch database is still dropped. Verify, executed: the fixture exits non-zero on `doctrine:migrations:migrate first`; the suite asserts a non-zero exit and that `DROP DATABASE IF EXISTS` was issued for the database this run created.
- [x] 7.5 A create collision aborts the run, whatever is in the existing database. Verify, executed: the fixture makes `CREATE DATABASE` fail with `already exists` and exit `7` — once for an existing **empty** database and once for one with tables — and the suite asserts, for both, a non-zero exit, the message `the name is taken, and this run owns nothing`, **no** `doctrine:migrations:migrate` in the console log and **no** `DROP DATABASE` in it.
- [x] 7.6 Two overlapping runs drop only their own database. Verify, executed: the suite starts one run whose fixture blocks inside the migration step on a `hold` file, runs a second to completion against the same configured database, and asserts from the console log that the two runs created different names, that the second dropped only its own, that releasing the first lets it finish and drop only its own, and that neither log carries a drop of the other's database.
- [x] 7.7 The guard against operating on the configured database. Verify, executed: a `DATABASE_URL` whose path is empty makes the resolved name meaningless, and the suite asserts the script refuses with `refusing to operate on the configured database` and that `CREATE DATABASE` never reaches the console. The failed-create path issuing no drop is task 7.5's second assertion.
- [x] 7.8 `tests/Integration/Db/SchemaFingerprintTest.php` against real PostgreSQL (Gate 1 round 1, finding 3): in a throwaway schema with `search_path` pointed at it, the fingerprint changes when an index is added (and returns exactly to the previous listing when it is dropped), when a column's nullability, default, type, **length** or **precision and scale** changes, and when a constraint is added and dropped — each case asserting the differing line names the object — plus a case asserting the type's modifiers are in the line (`character varying(32)` → `character varying(64)`, `numeric(10,2)` → `numeric(12,4)`) rather than merely that something changed, and one asserting the listing contains all three kinds. Verify, executed: 5 tests, 49 assertions green; each extraction removed from `scripts/schema-fingerprint.sql` in turn turns a case red — the index block, the constraint block, the column block — and so does replacing `format_type` with `information_schema.columns.data_type`, which fails `a column's precision and scale is part of the schema`; restored, green again.
- [x] 7.9 The limits of the listing are stated where the promise is. Verify: the header comment of `scripts/schema-fingerprint.sql` and the docblock of the test name what the fingerprint does not cover — sequences, triggers, functions, comments, grants and row contents — so nobody reads a green round trip as "the schema is identical in every respect".
- [x] 7.10 A `DATABASE_URL` whose query string selects the database does not send a migration to it (Gate 2 round 1, finding 1). Verify, executed: the fixture console resolves `current_database()` the way DBAL does, and the suite runs the script with `?dbname=fixture&serverVersion=16` — the run passes, no logged `doctrine:migrations:migrate` carries a `dbname=` parameter, and every one of them carries the scratch name. A second case forces the connection to resolve to `fixture` whatever the URL says: the script aborts with `not to the database this run created`, issues no migration at all and still drops the database it created.
- [x] 7.11 A listing query that fails, fails the run (Gate 2 round 1, finding 2). Verify, executed: the fixture makes one query exit `7` at each of the four stages in turn — `current_database`, the first fingerprint, the table list and the second fingerprint — and the suite asserts, for each, a non-zero exit, the SQLSTATE in the output, **no** `the round trip reproduced the schema exactly`, and the owned database dropped. A failing migration is covered the same way, asserting the message names which invocation failed.

## 8. The documents say what the floor is

- [x] 8.1 `docs/explanation/requirements.md`: NFR-QA-1's analysis level (now naming level 9 over `src` and `tests` with no baseline file, and what raised it), the tooling table, the summary table, and the reversibility claim of section 3 — which now names what proves it rather than asserting it. Verify, executed: `rg -n 'level 8' README.md docs/ openspec/ AGENTS.md` returns only this change's own artifacts describing the work and the archived changes.
- [x] 8.2 `openspec/config.yaml`: the analysis level, and the `Stage: scaffold; application code arrives through OpenSpec changes` line the user folded into this change on 2026-09-16 — the text every agent, Codex at the gates included, receives as project context, and which contradicted the README this series had just rewritten. It now says the service is built and points at the roadmap for what is left. Verify, executed: the file re-read whole after the edit; `rg -n 'scaffold' openspec/config.yaml` returns nothing.
- [x] 8.3 `docs/how-to/local-development.md`'s floor table and `docs/reference/commands.md` carry the new level and the new target, and the reference gains a row for each new script. Verify, executed: both files re-read whole after the last edit; `make migrations-roundtrip` and `sh scripts/migrations_roundtrip_test.sh` run in the exact form the documents print them (`the round trip reproduced the schema exactly`; `30 passed, 0 failed`).
- [x] 8.4 `docs/explanation/requirements.md`'s stage-plan row for 13a is reconciled with what was actually done — the scope now says `src` **and** `tests` with the user's decision dated, and the exit criterion names what proves a wrong `down` fails: the fixture suite for the script and `SchemaFingerprintTest` for the query. Verify: the row read against this change's tasks after the last edit; the roadmap row itself is removed at archive time, which is a lifecycle step below, not a task — a task that could only be checked after the archive commit can never be checked before Gate 2, which is the deadlock `scripts/pregate-verify.sh` and `scripts/workflow-verify.sh` create by design (Gate 1 round 1, finding 1).

## 9. Wrap-up

- [x] 9.1 `make check` green inside the container with no environment override, at level 9 — **900 tests, 21 817 assertions** after the Gate 2 round 1 fixes; `openspec validate harden-gate-floor --strict` passes; commits per logical block with the agent trailer; `handoff.md` updated with the counts before and after and the recorded round-trip output.
- [x] 9.2 Green Actions run on the exact branch head: the user pushes; the executor queries `https://api.github.com/repos/Azazu/linkboard/actions/runs?branch=change/harden-gate-floor` until the run for `git rev-parse HEAD` is `completed` / `success`; URL and SHA recorded in `handoff.md`. Verify, executed: run **35112644807** on `f8567ed` before Gate 2 and run **35122939983** on `4176d3f` after the round 1 fixes, both `completed`/`success` with all four jobs green — `detect`, `workflow` (which runs `scripts/migrations_roundtrip_test.sh` among every `scripts/*_test.sh`), `php` (`make check` at level 9 natively) and `migrations` (the round trip against a real PostgreSQL, natively).

## After every task above is complete — the gate, not a task

Gate 2 is requested once section 9 is done, and it is deliberately not a
checkbox: `scripts/gate-run.sh` runs `scripts/pregate-verify.sh` first, and that
floor rejects any unchecked task — so a task that included "run Gate 2 and
disposition its findings" could never be both truthful and satisfied
(`harden-quality-and-docs`, Gate 1 round 1, finding 1). The lifecycle step is:

1. `scripts/gate-run.sh harden-gate-floor 2 full`.
2. Fix every finding, update its Status in `review.md`, and re-review with
   `scripts/gate-run.sh harden-gate-floor 2 confirm <round>`.
3. The gate has passed when the last Gate 2 record reads `approved` or
   `confirmed` with no finding row left `open`; `scripts/workflow-verify.sh
   merge harden-gate-floor` is what checks that before the merge.
4. After the user merges: `scripts/workflow-verify.sh archive harden-gate-floor`,
   then the archive commit on `main`, which is where row 13a leaves
   `openspec/ROADMAP.md`. That removal is deliberately not a task either, for
   the same reason: it happens after every task must already be checked.
