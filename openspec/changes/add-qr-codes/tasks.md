# Tasks — add-qr-codes

High tier (new dependency + authorization boundary, AGENTS.md): Gate 1 on the artifacts and Gate 2 on the code. Work on the change branch; `scripts/workflow-verify.sh apply add-qr-codes` first.

## 0. Gate 1 (high tier)

- [x] 0.1 `scripts/pregate-verify.sh gate1 add-qr-codes` passes and `scripts/gate-run.sh add-qr-codes 1 full` is run on the completed artifacts (proposal with tier and Non-goals, specs `qr-codes` + `api-docs` delta, design with the applicability table, tasks); findings via `/workflow:fix-findings`, confirmation with `scripts/gate-run.sh add-qr-codes 1 confirm <round>`. Verify: review.md carries a Gate 1 round bound to the requested commit; the merge verifier checks that the last Gate 1 decision reads approved/confirmed. Done: round 1 recorded at `af515f7` (changes-requested: blocker — custom controller bypasses the pipeline; minors — enum `from`, 401 before 404), fixed in `2db3fd4`, Confirmation 1 `b3605f2` confirmed (Reviewed-Commit `f99b7cb`).

## 1. Dependency and renderer

- [x] 1.1 `make composer ARGS='require endroid/qr-code:^6.1'` (design decision 8) → `composer.json`, `composer.lock` (`endroid/qr-code` 6.1.x, `bacon/bacon-qr-code` 3.x and its transitive package); commit body flags the dependency change (AGENTS.md, Security-Sensitive Code). Verify: `make composer ARGS='show endroid/qr-code'` prints the installed 6.1.x version and `requires php ^8.4`; `make composer ARGS='audit'` reports no advisory; `docker compose exec -T php php -m | grep -x gd` prints `gd`; `make check` still green before any code is written. Done: `composer.json` (`endroid/qr-code: ^6.1`), `composer.lock` (endroid/qr-code 6.1.3, bacon/bacon-qr-code v3.1.1, dasprid/enum 1.0.7); `composer show` prints 6.1.3 with `php ^8.4` and `bacon/bacon-qr-code ^3.0`; `composer audit`: no advisories; `php -m` lists `gd`; `make check` green (559 tests).
- [ ] 1.2 `src/Link/Qr/QrFormat.php` (enum `svg|png`: `mimeType()`, `extension()`, static `fromQuery(?string)` defaulting to `svg` — a backed enum's own `from()` is final) and `src/Link/Qr/QrCodeRenderer.php` (`render(string $url, QrFormat $format): string` over the library's builder: size 512, margin 16, error correction Medium, round-block mode Margin — design decision 3; before writing, verify the builder's constructor parameters against `vendor/endroid/qr-code/src/Builder/Builder.php` and the writer class names under `vendor/endroid/qr-code/src/Writer/`). `tests/Unit/Link/Qr/QrCodeRendererTest.php`: the SVG of `https://example.test/spring-sale` equals `tests/Fixture/qr/spring-sale.svg` byte for byte (fixture rendered once by the same renderer, committed, its root element carries width/height 512); the PNG passes `getimagesizefromstring` with 512 × 512 and `IMAGETYPE_PNG` and starts with the PNG signature; two renders of one URL are identical, a different URL differs; `QrFormat::fromQuery(null)` is `svg` and `fromQuery('png')` is `Png`. Verify: `docker compose exec -T php vendor/bin/phpunit --testsuite Unit --filter Qr` green; failing inputs in the commit body: size 300 fails the PNG dimension case; a changed margin fails the snapshot.

## 2. API operation

- [ ] 2.1 `src/Link/Qr/LinkQrProcessor.php` (`ProcessorInterface`: receives the `LinkResource` the provider loaded and the voter passed, reads `?format` through `QrFormat::fromQuery`, returns a `Response` with the image bytes, `Content-Type`, `Content-Disposition: inline; filename="<slug>.<ext>"`, `Cache-Control: private, max-age=86400` — design decisions 1, 2, 4) and the operation on `src/Link/Api/LinkResource.php`: `Get` at `/links/{id}/qr`, `provider: LinkItemProvider::class`, `security: 'is_granted("LINK_VIEW", object)'`, `write: true`, `processor: LinkQrProcessor::class`, `outputFormats` svg/png, a declared `format` `QueryParameter` with `Choice(['svg', 'png'])`, description stating that `format` selects the image and `Accept` only gates. Before writing, re-read `MainController::__invoke` and `WriteProcessor::process` in `vendor/api-platform/` to confirm the `write`/`processor` behaviour the design relies on (Gate 1 round 1, finding 1). Verify: `make console ARGS='debug:router'` lists the route; `curl` on the dev stack for SVG and PNG (task 3.1 records the exact commands); `GET /api/docs.json` shows the path with the `format` query parameter and a 200 response listing `image/svg+xml` and `image/png` — if API Platform does not derive the two content types from `outputFormats`, add the explicit `openapi` responses (design decision 6) and re-check.
- [ ] 2.2 `tests/Api/Link/LinkQrTest.php` (`LinkApiTestCase`): default SVG with the three headers and a 512-px root element; `?format=png` → `image/png`, 512 × 512, filename `<slug>.png`; two SVG requests byte-identical, another link's differs; `?format=gif` and `?format=` → 422 problem+json with one violation on `format`; `Accept: application/json` → 406 problem+json; the matrix — owner 200, admin 200, stranger 403 problem+json, anonymous 401 (for the link and for `not-a-uuid` / a random uuid alike), `not-a-uuid` and a random uuid 404 for an authenticated caller; an inactive link and an expired link → 200; a deleted link → 404; `GET /api/docs.json` lists `/api/v1/links/{id}/qr` with `format` in `query` and both image content types under the 200 response. Verify: `docker compose exec -T php vendor/bin/phpunit --testsuite Api --filter LinkQr` green; failing inputs in the commit body: removing `security` from the operation makes the stranger case answer 200 (matrix fails); removing `write: true` leaves the processor unrun (the default-SVG header case fails); removing the `Cache-Control` header fails the header case; removing the `Choice` constraint makes `?format=gif` answer 200 SVG (422 case fails).

## 3. Docs

- [ ] 3.1 `docs/how-to/local-development.md` — in "Accounts and the API", two `curl` lines for the QR (`-o spring-sale.svg`, `?format=png -o spring-sale.png`), a sentence on `format`, the private one-day cache and the `gd` requirement for PNG; `docs/explanation/requirements.md` FR-QR-1 refined (422 on an unknown `format`, 406 for a JSON-only `Accept`, `Content-Disposition`, inactive/expired links keep their code; the web UI button stays row 11). Verify: every documented command run in its exact form on the dev stack (token, SVG and PNG downloaded, `file spring-sale.png` reports `PNG image data, 512 x 512`), outputs in the commit body; both files re-read whole after the last edit; `rg -n 'links/{id}/qr' docs openspec/changes/add-qr-codes` shows how-to, brief, proposal and spec agreeing.

## 4. Wrap-up

- [ ] 4.1 `make check` green (cs, stan level 8, all suites); commits per block (`chore(deps):` 1.1, `feat(qr):` 1.2 + 2.x, `docs:` 3.1) with the agent trailer; every guard's commit body names its demonstrated failing input; the dependency and authorization-boundary parts flagged in the commit bodies and in `handoff.md`. Verify: `git log --oneline main..HEAD`; `openspec validate add-qr-codes --strict` passes.
- [ ] 4.2 Green Actions run on the exact branch head before Gate 2: the user pushes the change branch; the executor queries `https://api.github.com/repos/Azazu/linkboard/actions/runs?branch=change/add-qr-codes` until the run for `git rev-parse HEAD` is `completed` / `success`; URL and SHA recorded in `handoff.md`. Verify: the run's `head_sha` equals the branch head.
- [ ] 4.3 `scripts/pregate-verify.sh gate2 add-qr-codes` passes and `scripts/gate-run.sh add-qr-codes 2 full` is run; findings via `/workflow:fix-findings`, confirmation with `scripts/gate-run.sh add-qr-codes 2 confirm <round>`. Verify: review.md carries a Gate 2 round bound to the requested commit; the merge verifier checks that the last Gate 2 decision reads approved/confirmed and is fresh.

## Post-merge acceptance (not a Gate 2 task)

- After the merge: the user pushes `main`; the executor checks the `main` run via the API before proposing the archive; `/opsx:archive` syncs `qr-codes` (new) and `api-docs` (one modified requirement) and removes row 9 from `openspec/ROADMAP.md`.
